# DESIGN.md: กติกาหน้าจอ kiosk

ไฟล์นี้คือแหล่งอ้างอิงเดียวของหน้าตาแอป **ก่อนแก้ UI ทุกครั้งต้องอ่านไฟล์นี้ก่อน** และเมื่อเกิดกติกาใหม่ต้องแก้ไฟล์นี้ใน commit เดียวกับโค้ด
ค่าทุกตัวในไฟล์นี้คัดมาจากโค้ดจริง (`app/src/main/res/...`) ถ้าไฟล์นี้กับโค้ดขัดกัน ให้แก้อันใดอันหนึ่งให้ตรงกัน อย่าปล่อยให้ขัดกันอยู่

## กติกาถาวรทุกครั้งที่แตะสิ่งที่ผู้ใช้เห็นบนจอ (Poom, 2026-09-23)

ทำตามลำดับนี้เสมอ ไม่มีข้อยกเว้น แม้งานจะดูเล็ก:
1. อ่านไฟล์นี้ (DESIGN.md) และ skill `ux-ui-design` ก่อนเริ่ม
   - ถ้ามี skill ออกแบบอื่นในเครื่องที่เกี่ยว ให้อ่านด้วย ถ้าไม่มีให้บอก
2. **แคปหน้าจอจริงจาก A07 ก่อนแก้** แล้วเขียนวิเคราะห์ว่าเสียตรงไหน
3. แก้
4. ติดตั้ง APK จาก CI ลง A07 แล้ว **แคปหน้าจอจริงอีกครั้ง**
5. เทียบภาพก่อนและหลัง แล้วรายงานจากสิ่งที่เห็นในภาพ
   - **ห้ามรายงานว่าเสร็จโดยอ้างจากโค้ดหรือ test อย่างเดียว**
6. ถ้ามีกติกาการจัดวางใหม่ที่ตัดสินแล้ว ให้เขียนลงไฟล์นี้ทันทีใน commit เดียวกัน เพื่อไม่ให้หลุดในรอบถัดไป

อ่านคู่กับ skill `ux-ui-design` (หลักการทั่วไป: หนึ่งหน้าจอหนึ่งการกระทำหลัก, ออกแบบทุกสถานะ, ภาษาเขียนทางการ, ห้ามสื่อความหมายด้วยสีอย่างเดียว)

## 1. แนวทางโดยรวม

- **pixel art ผสมเดสก์ท็อปยุค 1995:** พื้นเขียวอมฟ้า หน้าต่างสีเทานูน แถบหัวข้อสีกรมท่า และ taskbar อยู่ล่างสุด
- **ทำเองทั้งหมดจากสี่เหลี่ยม:** กรอบใช้ layer-list (`retro_raised`, `retro_sunken`, `retro_titlebar`) ส่วนไอคอนเป็น vector ที่วาดจากช่องสี่เหลี่ยม **ไม่มีไฟล์ภาพ ไม่มี icon pack และไม่มี asset ของ Microsoft**
- **อ่านจากระยะประมาณ 20 cm:** Poom ถือมือถืออ่าน ไม่ได้ยืนดูจากอีกฝั่งห้อง ทุกขนาดในไฟล์นี้ตั้งมาสำหรับระยะนี้
- **ลำดับความสำคัญ:**
  1. จาร์วิสกำลังทำอะไรและได้ยินอะไร
  2. ตัวเลข
  3. ป้ายกำกับ
  4. ข้อมูลวินิจฉัย (ซ่อนไว้ ยกเว้นเปิด debug ผ่าน adb)
- **เครื่องเป้าหมาย:** Samsung A07 ขนาด 720×1600 px ที่ density 1.875 กว้าง 384dp แนวตั้งเท่านั้น
  - **ข้อยกเว้นเดียว (0.57.0, Poom อนุมัติ):** เครื่องเล่นวิดีโอตอน "เต็มจอ" หมุนเป็นแนวนอนได้ (ข้อ 5ซ) ออกจากเต็มจอ ออกจากเครื่องเล่น หรือวิดีโอจบ แอปกลับเป็นแนวตั้งทันที หน้าอื่นทุกหน้าแนวตั้งเสมอ (`screenOrientation="portrait"` ใน manifest)

## 2. จานสี

ค่าจริงอยู่ใน `res/values/colors_retro.xml` และในไอคอน

| ชื่อ | ค่า | ใช้กับ |
|---|---|---|
| `retro_desktop` | `#008080` | พื้นหลังเดสก์ท็อป |
| `retro_face` | `#C0C0C0` | ผิวหน้าต่างและปุ่ม |
| `retro_light` | `#FFFFFF` | ขอบสว่าง (บน/ซ้ายของของนูน) |
| `retro_shadow` | `#808080` | ขอบเงา (ล่าง/ขวาของของนูน) |
| `retro_dark` | `#000000` | เส้นเข้ม |
| `retro_title` | `#000080` | แถบหัวข้อหน้าต่าง |
| `retro_title_text` | `#FFFFFF` | ตัวอักษรบนแถบหัวข้อ |
| `retro_text` | `#000000` | ตัวอักษรหลัก |
| `retro_dim` | `#404040` | ข้อความรอง, หมายเหตุอายุข้อมูล |
| `retro_good` / `retro_bad` | `#008000` / `#800000` | สถานะดี/เสีย (ต้องมีคำกำกับเสมอ) |
| `retro_badge` | `#FFD700` | ป้ายบนแถบหัวข้อ ("เพิ่งอัปเดต" หรือสรุปของการ์ดที่หุบ) |
| `retro_lcd` / `retro_lcd_dim` | `#00E000` / `#00A000` | จอ LCD ของเครื่องเล่นเพลง (ตัวเขียวบนพื้นดำ, 0.53.0) |
| `amp_body` / `amp_face` / `amp_light` / `amp_dark` | `#2C2F40` / `#3C4058` / `#7478A0` / `#0C0D14` | สกินเครื่องเล่นเพลง: ตัวเครื่อง ปุ่ม ขอบสว่าง ขอบมืด (0.55.0) |
| `amp_text` / `amp_led_off` / `amp_gold` / `amp_select` | `#DCE0EE` / `#1C3A1C` / `#D8B800` / `#1E3A8C` | ตัวหนังสือบนสกิน ไฟดับ แถบเลื่อนและอีควอไลเซอร์ แถวที่เล่นหรือเลือกอยู่ |
| `amp_bar_mid` / `amp_bar_high` | `#D8D800` / `#E05000` | กราฟแท่งช่วงกลางและช่วงสูงสุด (ช่วงล่างคือ `retro_lcd`) |
| `dvd_silver` / `dvd_silver_light` / `dvd_silver_dark` | `#C8CCD4` / `#F2F4F8` / `#7C828E` | แผงเงินของเครื่องเล่นวิดีโอ (0.56.0) |
| `dvd_lcd_bg` / `dvd_lcd_text` | `#0E6E86` / `#B8F4FF` | จอสีฟ้าอมเขียวของเครื่องเล่นวิดีโอ |
| `video_scrim` | ดำ 70% | พื้นหลังคำที่วางบนภาพวิดีโอ |

สีในไอคอน (ใช้ซ้ำเท่านั้น ถ้าจะเพิ่มสีใหม่ต้องลงตารางนี้ก่อน):

| สี | ค่า | ไอคอน |
|---|---|---|
| ทอง/แดด | `#FFB000`, `#FFD700`, `#E8A317`, `#8B6914` | ดวงอาทิตย์, ทองแท่ง, เหรียญทั่วไป |
| ดวงอาทิตย์ตก | `#FF6A00` | พระอาทิตย์ตก |
| จันทร์ | `#FFE8A0` | พระจันทร์ |
| จาร์วิส | `#00A0A0`, `#004040` | หน้าจาร์วิส |
| บ้าน | `#B03A2E` (หลังคา), `#000080`, `#008080` | Google Home |
| แบตเตอรี่ | `#2E8B2E`, `#CC0000`, `#FFD700` | ปกติ, ต่ำ, สายฟ้าชาร์จ |
| กระดิ่ง | `#FFB000` / `#8B6914`, ขาว | นาฬิกาปลุก |
| เหรียญ BTC | `#E8891C` / `#A85E0C` | |
| เหรียญ ETH | `#6C71C4` / `#464A8C` | |
| เหรียญ BNB | `#E0B000` / `#9C7A00`, ตัวอักษร `#3A2E00` | |
| เหรียญ XRP | `#505860` / `#2C3238` | |

## 3. ฟอนต์

ทั้งสองฟอนต์เป็น SIL OFL และอยู่ใน repo ที่มาและสัญญาอนุญาตอยู่ใน `NOTICE` และ `licenses/fonts/`

- **Press Start 2P** (`res/font/press_start_2p.ttf`): ใช้กับตัวเลขและอักษรละตินเท่านั้น ฟอนต์นี้ไม่มีภาษาไทย
- **IBM Plex Sans Thai Looped** (`res/font/plex_thai.xml`, Regular และ SemiBold): ใช้กับภาษาไทยและตัวหนังสือเล็กทุกตัว เป็นฟอนต์หลักของทุก TextView
- **การผสมสองฟอนต์ในบรรทัดเดียวต้องทำผ่าน `ui/RetroType.kt` เท่านั้น**
  - ตัวเลขและละตินถูกห่อด้วย span ให้เป็น pixel ย่อเหลือ 0.8 เท่าของขนาดรอบข้าง แล้วเลื่อนลง 0.125 em ให้วางบน baseline เดียวกับภาษาไทย (สองค่านี้วัดจากตัวฟอนต์จริง ไม่ได้เดา)
  - ช่องว่างตรงรอยต่อระหว่างเลข pixel กับคำไทยถูกขยาย 2.2 เท่า ไม่อย่างนั้นเลขจะติดกับคำ
  - ห้ามตั้ง `fontFamily` เป็น pixel ให้กับ TextView ที่มีภาษาไทย เพราะ Android จะ fallback ไปใช้ฟอนต์ไทยของเครื่องแทน
  - ข้อยกเว้นเดียวคือนาฬิกา taskbar ซึ่งเป็นตัวเลขล้วน

## 4. Type scale ตามลำดับความสำคัญ (ระยะ 20 cm, Poom 2026-09-23)

**หลัก: ขนาดคือลำดับความสำคัญ** ข้อมูลที่สำคัญกว่าต้องใหญ่กว่าเสมอ ข้อมูลที่สำคัญน้อยห้ามใหญ่กว่าข้อมูลที่สำคัญกว่า ไม่ว่าจะมีพื้นที่เหลือแค่ไหน
- บทเรียนจาก 0.35.1: ตัวเลขคริปโตอยู่ที่ 15sp เท่ากับราคาทอง และเป็นตัว pixel ทั้งบรรทัด จึงดูใหญ่ที่สุดบนจอ ทั้งที่เป็นข้อมูลที่สำคัญน้อยที่สุด
- ขนาดของข้อมูลบน dashboard อยู่ใน `res/values/type_scale.xml` ที่เดียว layout และโค้ดต้องอ้างชื่อขั้น (`@dimen/type_…`, `sp(R.dimen.type_…)`) ห้ามใส่ตัวเลขเอง
- `TypeScaleTest` ตรวจว่าขั้นเรียงจากใหญ่ไปเล็ก และตรวจว่า view แต่ละตัวอยู่ในขั้นที่กำหนด

| ขั้น | ขนาด | ความสำคัญ | ใช้กับ |
|---|---|---|---|
| 1 `type_now` | 16sp | สิ่งแรกที่คนมองหา | อากาศตอนนี้ (อุณหภูมิและสภาพฟ้า) |
| 2 `type_primary` | 15sp | ตัวเลขหลักของการ์ด | ราคาทอง และข้อความในหน้าต่างจาร์วิส (ล็อกไว้ ห้ามเปลี่ยน) |
| 3 `type_secondary` | 14sp | ตัวเลขประกอบ | ตัวเลขอากาศวันนี้ ราคาน้ำมัน รายการนาฬิกาปลุก |
| 4 `type_minor` | 12sp | ข้อมูลรอง | คริปโต, +/− ของทอง, ชื่อปั๊ม, เวลาพระอาทิตย์, พยากรณ์ 3 วัน |
| 5 `type_label` | 11sp | ป้ายบอกว่าตัวเลขคืออะไร | ป้ายเหนือตัวเลขอากาศ บรรทัดหัวส่วนทอง/น้ำมัน (สีจาง) และชื่อการ์ด (ตัวหนา สีขาว) |
| 6 `type_chrome` | 10sp | ส่วนประกอบของหน้าต่าง | ป้ายบนแถบหัวข้อ (รวมวันที่บนแถบหัวข้อจาร์วิส) แบตเตอรี่และชนิดการเชื่อมต่อใน taskbar |

**ข้อยกเว้นที่มีเหตุผลเฉพาะ (ไม่อยู่ในลำดับ):**
- ปุ่ม "หยุดปลุก" 18sp ตัวหนา เพราะเป็นปุ่มที่ต้องหาเจอในความมืด ไม่ใช่ข้อมูล
- นาฬิกา taskbar 12sp (Press Start 2P) ตัว pixel จึงใหญ่ใกล้เคียงขั้น 3 ของตัวไทย (ข้อ ง: เดิม 9sp เล็กเกินไป)
- สถานะในแถบหัวข้อจาร์วิส 12sp และข้อมูลวินิจฉัย 8sp (ซ่อนไว้)

**ตัวเลข pixel:** `RetroType.pixelify` วาดตัว Press Start 2P ที่ 0.8 เท่าของขนาดขั้น ขั้นจึงเทียบกันได้ตรง ๆ
- หมายเหตุอายุข้อมูล "(3 นาทีก่อน)" เป็น 0.75 เท่า สี `retro_dim`

**ห้าม:**
- เพิ่มขนาดใหม่โดยไม่ลงตารางนี้
- ลดขนาดเพื่อให้ข้อความพอดีการ์ด ถ้าไม่พอดี ให้แก้ถ้อยคำหรือการจัดวางก่อน
  - การลดคริปโตเป็น 12sp ทำเพราะความสำคัญของข้อมูล ไม่ได้ทำเพื่อให้พอดี

## 5. การ์ด (หน้าต่าง)

**โครงสร้างทุกการ์ดเหมือนกัน:**
- กรอบนอกเป็น `retro_raised` padding 5dp และห่างการ์ดข้างบน 7dp
- แถบหัวข้อเป็น `retro_titlebar` padding 6/2dp มีไอคอน 13dp (จาร์วิส 15dp) แล้วเว้น 5dp ก่อนชื่อ
- ชื่อการ์ด 11sp ตัวหนา สีขาว **บรรทัดเดียวเสมอ** (`maxLines=1`, `ellipsize=end`) เพราะแถบหัวข้อที่ขึ้นบรรทัดใหม่จะไม่ดูเป็นแถบหัวข้อ
- ข้อมูลอยู่ใน `retro_sunken` ห่างแถบหัวข้อ 3dp padding 7dp

**ความสูง: แบ่งพื้นที่ (0.42.0, Poom 2026-09-23 จากภาพจริงบน A07):**
- **การ์ดอื่นสูงเท่าเนื้อหาพอดี ไม่รับส่วนแบ่งพื้นที่ว่าง** (`card_stack` เป็น `wrap_content` และไม่มี weight)
- **พื้นที่ที่เหลือทั้งหมดเป็นของการ์ดจาร์วิส** (`layout_height=0dp`, `layout_weight=1`)
  - **เหตุผล:** ข้อความที่จาร์วิสพูดคือหัวใจของระบบ คำตอบยาวต้องอ่านได้หลายบรรทัดโดยไม่ต้องเลื่อน
- **การ์ดจาร์วิสอยู่ที่เดิมเสมอ** คือเหนือ taskbar ขอบล่างไม่ขยับ ห้ามหุบ
  - **ต่ำสุด 156dp** (ขนาดใน 0.30.3 ที่ Poom ยืนยันว่าเหมาะแล้ว) เป็น `minHeight`
  - ขอบบนขยับได้ตามการ์ดข้างบน: การ์ดหุบ จาร์วิสได้พื้นที่เพิ่ม
- `CardBoard.fit` คิดพื้นที่ของกองจากขอบล่างของจาร์วิส ลบ 156dp และช่องห่าง 7dp ถ้าการ์ดที่เปิดอยู่รวมกันจะดันจาร์วิสต่ำกว่า 156dp ให้หุบการ์ดตามกติกาเดิม
- `JarvisSpaceTest` ตรวจกติกานี้จาก layout
- **ที่มา (วัดจากภาพ 0.41.0 บน A07 ความสูงจอ 1600 px):** พื้นที่ว่างใต้บรรทัดสุดท้ายของแต่ละการ์ด (รวมขอบการ์ดราว 22 px)
  - อากาศ 63 px
  - ทองและน้ำมัน 111 px (ราว 46 px เป็นแถวที่ 3 ของหน้าน้ำมันที่ต้องจองไว้)
  - คริปโต 34 px
  - จาร์วิส: ว่างเหนือและใต้ข้อความบรรทัดเดียว 90 + 97 px
- **ห้าม:** ใส่ weight ให้การ์ดใน `card_stack` อีก เพราะพื้นที่ที่แบ่งไปจะกลายเป็นช่องเทาว่างใต้บรรทัดสุดท้าย

**ทุกสถานะต้องมีข้อความ:**
- กำลังโหลด: "กำลังโหลด…"
- ไม่มีข้อมูล: "ข้อมูลไม่พร้อม"
- ข้อมูลเก่า: แสดงค่าเดิมพร้อมอายุข้อมูล
- ส่วนเสริมที่ไม่มีข้อมูล เช่น แถวพระอาทิตย์ ให้ซ่อน (GONE) ไม่ใช่แสดงแถวว่าง
- ถ้าดึงข้อมูลล้มเหลว ห้ามล้างจอ ให้คงค่าเดิมไว้

**การ์ดจาร์วิส: ข้อความยาวต้องอ่านได้ครบ (2026-09-23)**
- **เลื่อนด้วยนิ้วได้:** พื้นที่คำตอบเป็น ScrollView มีแถบเลื่อนแบบ retro ทางขวาแสดงตลอด (thumb เป็น `retro_raised`, ร่องเป็น `retro_scroll_track` `#A8A8A8`, กว้าง 6dp) เพื่อบอกว่ามีข้อความต่อด้านล่าง
- **เลื่อนตามเสียงเอง:** ระหว่างพูด จอเลื่อนเองให้บรรทัดที่กำลังพูดอยู่ราว 1/3 จากขอบบน (`ui/SpeechFollow.kt`) เพราะคนกำลังฟังอยู่ ไม่ได้เอื้อมมาแตะจอ
- **แตะแล้วหยุดตาม:** ถ้ามีคนแตะหรือเลื่อนเอง จอจะหยุดตามเสียงสำหรับคำตอบนั้นทันที เพราะจอที่เลื่อนหนีบรรทัดที่กำลังอ่านอยู่แย่กว่าจอที่ไม่ขยับเลย คำตอบใหม่จะเริ่มจากบนสุดและกลับมาตามเสียงอีกครั้ง
- **ข้อความค้างบนจอ:** คงไว้ 60 วินาทีหลังพูดจบ (`FadingLine`) และทุกครั้งที่แตะจะนับใหม่ (`READING_HOLD_MS` 30 วินาที)
- **การจัดแนว:** ข้อความสั้นที่พอดีการ์ดจัดกึ่งกลางเหมือนเดิม ข้อความที่ยาวเกินการ์ดชิดซ้ายและเริ่มจากบน เพราะย่อหน้าภาษาไทยที่จัดกึ่งกลางอ่านต่อบรรทัดยาก
- **ห้ามลดขนาดตัวอักษรเพื่อให้พอดีการ์ด** ให้เลื่อนแทน
- **ขนาดการ์ดต้องไม่ขึ้นกับความยาวข้อความ:** พื้นที่เลื่อนต้องเป็น `ui/RestingScrollView` (มี `minHeight` เป็นขนาดพัก) ห้ามใช้ ScrollView ธรรมดาในการ์ดที่เป็น `wrap_content` + weight
  - บทเรียนจาก 0.30.0: การ์ดจาร์วิสสูงตามคำตอบ แล้วบีบการ์ดอากาศ ทอง และคริปโตจนข้อมูลหาย

**กองการ์ด: หุบ คลี่ และเรียงลำดับ (0.32.0):** ตรรกะอยู่ใน `ui/CardBoard.kt` (มี test) ส่วน `MainActivity.renderCards` ทำหน้าที่ย้าย view อย่างเดียว

- **อะไรนับว่าอัปเดต:** สิ่งที่คนเห็นแล้วรู้ว่าเปลี่ยน กำหนดไว้ที่ `DashboardState.cardFacts` ที่เดียว ถ้าดึงข้อมูลใหม่มาแล้วได้ค่าเดิม ไม่นับเป็นอัปเดต

  | การ์ด | นับว่าอัปเดตเมื่อ | หุบหลังไม่มีอัปเดต |
  |---|---|---|
  | อากาศ | อุณหภูมิเปลี่ยนเป็นเลขเต็มอีกค่า หรือคำบอกสภาพฟ้าเปลี่ยน (30.2 → 30.4 ไม่นับ) | 60 นาที |
  | ทองและน้ำมัน | ราคาใดราคาหนึ่ง (ทองรูปพรรณ, ทองแท่ง, น้ำมันถูกสุดแต่ละชนิด) **ขยับตั้งแต่ 1%** | 2 ชั่วโมง |
  | คริปโต | ราคาเหรียญใดเหรียญหนึ่ง (เป็นดอลลาร์) **ขยับตั้งแต่ 3%** ต่ำกว่านี้ไม่นับ | 60 นาที (และหุบตั้งแต่เริ่ม ข้อ ข) |

- **ทอง น้ำมัน คริปโต (Poom, 2026-09-23) ใช้ `ui/MoveTracker`:**
  - **เทียบกับ:** ราคา ณ ตอนที่การ์ดนั้นมีข่าวครั้งล่าสุด ไม่ได้เทียบกับรอบดึงก่อนหน้า เพราะถ้าเทียบรอบต่อรอบ ราคาที่ค่อย ๆ ไหลทีละนิดจะไม่มีวันนับ
  - **ช่วงเวลา:** ไม่มีหน้าต่างเวลาตายตัว ตรวจทุกครั้งที่ดึงข้อมูล (นาทีละครั้ง) ขยับจากจุดอ้างอิงถึงเกณฑ์เมื่อไหร่ก็นับ ไม่ว่าจะใช้เวลานานเท่าไร
  - มีข่าวแล้ว ราคาทุกตัวของการ์ดนั้นกลายเป็นจุดอ้างอิงใหม่
  - ราคาที่เพิ่งปรากฏ (เช่น เหรียญใหม่ขึ้นมาติด 4 อันดับ) ถือเป็นจุดอ้างอิงเงียบ ๆ ไม่นับเป็นข่าว
  | นาฬิกาปลุก | ตั้ง เปิดหรือปิดการปลุก หรือเริ่ม/หยุดดัง | 10 นาที |
  | อุปกรณ์ในบ้าน (0.45.0) | ไฟ ช่องของสวิตช์ หรือปลั๊กใน allowlist ดวงใดเปิดหรือปิด หรือกลับมาออนไลน์ (ไม่นับอายุข้อมูล) ซ่อนทั้งการ์ดจนกว่า broker จะมีอุปกรณ์ให้แสดง (ข้อ 5ง) | 60 นาที |

- **โครงสร้าง dashboard (0.36.0, Poom ให้ตัดสินตามหลัก UX/UI):**
  - **ก. ลำดับตายตัว:** อากาศ → นาฬิกาปลุก → ทองและน้ำมัน → คริปโต (`CardBoard(fixedOrder = true)` เรียงตามลำดับที่ลงทะเบียนใน `setUpCards`)
    - **เหตุผล:** จอที่มองจากระยะไกลถูกอ่านด้วยตำแหน่ง ถ้าการ์ดสลับที่ตามข่าว คนต้องมองหาทุกครั้ง ข่าวจึงบอกด้วยการ์ดที่คลี่ออกและป้าย "เพิ่งอัปเดต" แทนการย้ายที่
    - เรียงตามความสำคัญ: อากาศใช้ทุกวัน นาฬิกาปลุกเป็นสิ่งเดียวที่คนต้องลงมือทำ ส่วนราคาเป็นข้อมูลดูผ่าน ๆ
    - ขณะนาฬิกาปลุกดัง การ์ดนาฬิกาปลุกยังขึ้นบนสุดทันที (ปักหมุด)
  - **ข. คริปโตหุบเป็นค่าเริ่มต้น** (`openOnFirst = false`) แถบแสดงสรุปหนึ่งบรรทัด จะคลี่เมื่อเหรียญใดขยับตั้งแต่ 3% หรือเมื่อแตะ
    - **เหตุผล:** เป็นข้อมูลที่สำคัญน้อยที่สุดแต่กินพื้นที่มากที่สุด ยังคงแบบ 2×2 ไว้ตอนคลี่
  - **ค. ซ่อนการ์ดบ้าน** จนกว่าจะมีข้อมูลจริง แล้วจะกลับมาเองโดยไม่ต้องแก้จอ (Poom อนุมัติ 2026-09-23) ตั้งแต่ 0.45.0 การ์ดนี้คือ "อุปกรณ์ในบ้าน" จาก eWeLink และขึ้นเมื่อ `HomeCard.parse` ไม่เป็น null (ข้อ 5ง)
    - **เหตุผล:** แถบที่กดแล้วบอกว่า "ยังไม่ได้เชื่อมต่อ" ใช้พื้นที่โดยไม่มีประโยชน์
  - **ง. นาฬิกา taskbar ใหญ่ขึ้นจาก 9sp เป็น 12sp**
    - **เหตุผล:** เวลาเป็นสิ่งที่คนมองหาบ่อยที่สุด ขนาดเดิมเล็กกว่าข้อมูลที่สำคัญน้อยกว่า
- **อากาศเปิดเสมอ** (`alwaysOpen`) ไม่หุบแม้ไม่มีข่าวนาน
- **ห้ามตัดข้อมูลเพราะการ์ดเปิดพร้อมกันเกินจอ (`CardBoard.fit`, 0.36.0):** ทุกครั้งที่จัดการ์ด จะวัดความสูงจริงของแต่ละการ์ดทั้งตอนเปิดและตอนหุบ ถ้ารวมกันเกินพื้นที่ ให้หุบการ์ดที่ข่าวเก่าที่สุดก่อน
  - ไม่หุบเด็ดขาด: อากาศ และนาฬิกาปลุกที่กำลังดัง
  - **การ์ดที่แตะเปิด: คงไว้ได้ใบเดียว คือใบที่แตะล่าสุด** (`CardBoard.MAX_TOUCH_KEPT`, 0.53.1) ลำดับการหุบเมื่อที่ไม่พอ: การ์ดที่ไม่ได้แตะ ข่าวเก่าสุดก่อน → การ์ดที่แตะเปิดใบอื่น ข่าวเก่าสุดก่อน → ใบที่แตะล่าสุด → การ์ดที่ค้างเปิด (เพลง)
  - บทเรียนจาก 0.53.0 บน A07: Poom แตะเปิดทุกการ์ด ทุกใบจึงได้สิทธิ์ "เพิ่งแตะ" ไม่มีใบให้หุบ จาร์วิสถูกดันเหลือราว 58dp (log `fit` เป็น `oO` ทุกใบ) ขนาดจริงวันนั้น อากาศ 442 px กับแถบ 77 px ต่อใบ รวมกับปลุกที่ดังยังต่ำกว่า 1,196 px ที่มี จึงไม่ต้องหุบอากาศหรือปลุก (`CardBoardTest` ใช้ขนาดชุดนี้)
  - บทเรียนจาก 0.36.0 บน A07: แตะเปิดคริปโตแล้ว บรรทัดพยากรณ์ของอากาศกับแถว E20 ถูกตัดครึ่ง
  - **วัดความสูงให้ถูก (0.38.1):** ก่อนวัดต้อง `forceLayout()` ทุก view ในการ์ด แล้ว `requestLayout()` กองการ์ดหลังวัด
    - Android เก็บผลวัดตาม measure spec ไว้ (`mMeasureCache`) และ spec ที่ `fit` ใช้ไม่ใช่ spec ที่ layout ใช้ ถ้าไม่บังคับ จะได้ความสูงเก่าตอน "กำลังโหลด…" กลับมา
    - บทเรียนจาก 0.36.1 บน A07: log ของ `fit` บอกว่าการ์ดอากาศสูง 161 px แต่บนจอสูง 495 px จึงไม่หุบอะไรเลย และการ์ดถูกตัดซ้ำ
    - log `KioskScreen: fit avail=… id:เปิด/หุบ…` เขียนทุกครั้งที่ตัวเลขเปลี่ยน ไว้ตรวจเรื่องนี้
- **เนื้อหาในการ์ดชิดบน** (`gravity="top"`) พื้นที่ที่เหลือจาก weight จึงอยู่ใต้เนื้อหา ไม่ใช่ช่องว่างระหว่างแถบหัวข้อกับข้อมูล เพราะตาเริ่มอ่านจากใต้แถบหัวข้อ (คริปโตแบบ 2×2 ยังจัดกึ่งกลาง)
- **ห้ามกระพริบ:** จะแตะ view เฉพาะเมื่อสถานะเปลี่ยนจริง ป้าย "เพิ่งอัปเดต" ขึ้นทันที
  - โหมดเรียงตามข่าวแบบเดิม (อัปเดตล่าสุดอยู่บน จัดใหม่ไม่ถี่กว่านาทีละครั้ง) ยังอยู่ใน `CardBoard` และมี test แต่ kiosk ไม่ได้ใช้แล้ว
- **"เพิ่งอัปเดต":** ขึ้นที่ขวาของแถบหัวข้อเป็นสีทอง `retro_badge` นาน 10 นาทีหลังอัปเดต
  - ข้อมูลชุดแรกหลังเปิดแอปหรือติดตั้งใหม่ทำให้การ์ดเปิด แต่ไม่ขึ้นป้าย เพราะเวลานั้นไม่มีอะไรเป็นข่าว
- **การ์ดที่หุบ:** เหลือแถบหัวข้อ ตรงที่เดียวกับป้ายแสดงสรุปหนึ่งบรรทัด เช่น "30°C แดดจัด", "ทองแท่ง 68,250 บ.", "BTC +1.8%", "ถัดไป 6:30 AM"
  - แตะแถบเพื่อคลี่ได้ 2 นาที โดยไม่นับเป็นอัปเดต จึงไม่กระโดดขึ้นบนใต้นิ้ว
- **ปักหมุด:** ขณะนาฬิกาปลุกดัง การ์ดนาฬิกาปลุกเปิดและอยู่บนสุดทันที โดยไม่รอรอบนาที
- **การ์ดที่ยังไม่มีอะไรให้แสดงต้องซ่อนทั้งการ์ด (GONE):** นาฬิกาปลุกเมื่อยังไม่มีการปลุก และ Google Home เมื่อยังเชื่อมต่อไม่ได้ (`hidden()` ใน `renderCards`)
- **เพิ่มการ์ดใหม่:** เพิ่ม view ใน `card_stack` แล้วลงทะเบียนใน `setUpCards` **ตามตำแหน่งที่ต้องการบนจอ** พร้อมเวลาหุบ ส่งค่าที่นับว่าอัปเดต แล้วเพิ่มแถวในตารางนี้

**การ์ดนาฬิกาปลุก:**
- ซ่อนทั้งการ์ดเมื่อยังไม่มีการปลุกเลย
- รายการการปลุกแสดงเวลาแบบ 12 ชั่วโมงเหมือนนาฬิกา taskbar ตามด้วยชื่อ **รอบการปลุก** ("ทุกวัน", "ครั้งเดียว", "จันทร์–ศุกร์" ฯลฯ, `AlarmBook.repeatText` คำเดียวกับแผงควบคุม, 0.42.0 Poom) และ "เปิด"/"ปิด" เป็นคำ ไม่ใช้สีอย่างเดียว เช่น "11:00 AM · ทุกวัน · เปิด"
- ขณะดังมีปุ่ม **"หยุดปลุก"** เต็มความกว้าง สูงอย่างน้อย 56dp ตัวอักษร 18sp ตัวหนา เป็นปุ่มนูนแบบ retro เพราะต้องหาเจอได้ในความมืด
  - ปุ่มจาร์วิสใน taskbar ก็หยุดการปลุกได้เช่นกัน

**การ์ดหลายหน้า (0.38, Poom 2026-09-23):** เนื้อหาในการ์ดแบ่งเป็นหน้า แสดงทีละหน้า ใช้ `ui/PagedPanel` (ตรรกะอยู่ที่ `ui/Pages`, มี test)
- **สไลด์เฉพาะเนื้อหา:** แถบหัวข้อ กรอบ และตำแหน่งการ์ดในกองอยู่กับที่
- **ใช้การสไลด์ ไม่ใช่แท็บปุ่ม (Poom 2026-09-24, 0.53.2):** ปัดซ้ายขวาบนเนื้อหาเพื่อเปลี่ยนหน้า **ห้ามมีแถวแท็บหรือปุ่มเลือกหน้ากินความสูง** เพราะความสูงนั้นถูกเอาไปจากการ์ดจาร์วิส
  - Poom สั่งแบบสไลด์ไว้ตั้งแต่แรก แต่รอบ 0.38 ทำเป็นแท็บ property sheet สูง 40dp + ช่องห่าง 4dp
- **บอกจำนวนหน้าด้วยสี่เหลี่ยมเล็กบนแถบหัวข้อของการ์ด** (`PagedPanel.attachIndicator`) จึงไม่เพิ่มความสูงเลย: ■ หน้าที่แสดงอยู่ (ทึบขาว) □ หน้าอื่น (ขอบขาว) ขนาด 7dp ห่างกัน 4dp อยู่ระหว่างชื่อการ์ดกับป้ายมุมขวา
  - แยกด้วยรูปทรง (ทึบ/กลวง) ไม่ใช่สีอย่างเดียว ถ้ามีหน้าเดียวจะไม่มีสี่เหลี่ยม
  - แตะที่สี่เหลี่ยมเพื่อไปหน้าถัดไปได้ด้วย `contentDescription` บอก "หน้า 1 จาก 2: ทอง กำลังแสดง, น้ำมัน มีข้อมูลใหม่ แตะเพื่อเปลี่ยนหน้า หรือปัดซ้ายขวา"
- **ความสูงคงที่:** หน้าที่ไม่ได้แสดงเป็น INVISIBLE จึงยังจองที่ไว้ การ์ดสูงเท่าหน้าที่สูงที่สุดเสมอ เปลี่ยนหน้าแล้วการ์ดไม่ขยับ
- **เปลี่ยนหน้า:** ปัดเนื้อหาไปทางซ้าย/ขวา หรือแตะสี่เหลี่ยมบนแถบหัวข้อ
  - นับเป็นการปัดเมื่อเลื่อนเกิน 2 เท่าของ touch slop และเลื่อนแนวนอนมากกว่าแนวตั้ง 1.5 เท่า
  - บน dashboard ไม่มีอย่างอื่นที่ปัดแนวนอน จึงไม่ชนกัน
  - เปลี่ยนหน้าแล้วนับเป็นการแตะการ์ด: การ์ดเปิดค้าง 2 นาที และถ้าเป็นใบที่แตะล่าสุด `CardBoard.fit` จะหุบใบอื่นก่อน
- **ข่าวรายหน้า:** แต่ละหน้ามีข่าวของตัวเอง (ทองกับน้ำมันใช้ `MoveTracker` แยกกัน ขยับตั้งแต่ 1%)
  - หน้าที่มีข่าวจะถูกเปิดขึ้นมาแสดง เพราะการ์ดคลี่ขึ้นมาเพราะข่าวนั้น
  - ยกเว้นมีคนเลือกหน้าไว้ภายใน 2 นาที: หน้าที่เลือกไว้คงอยู่ และหลังสี่เหลี่ยมของหน้าที่มีข่าวมีคำว่า "ใหม่" (10sp สีทอง `retro_badge`) จนกว่าจะเปิดดู เป็นคำ ไม่ใช่สีอย่างเดียว
  - ป้าย "เพิ่งอัปเดต" บนแถบหัวข้อยังเป็นของทั้งการ์ด และนับจากข่าวของหน้าใดหน้าหนึ่ง
  - ราคาชุดแรกของแต่ละหน้าไม่นับเป็นข่าว (เช่นน้ำมันมาถึงช้ากว่าทอง)
- **หน้าที่ไม่มีข้อมูล** เช่น broker รุ่นเก่าที่ไม่ส่งราคาน้ำมัน: หน้านั้นหายไปพร้อมสี่เหลี่ยมของมัน (`setPageAvailable`)
- **การ์ดทองและน้ำมัน:** หน้า "ทอง" (หัวส่วนและ 2 แถว) กับหน้า "น้ำมัน" (หัวส่วนและ 3 แถว) เมื่อเปิดสูง 262 px บนจอ A07 (0.53.2 ไม่มีแถวแท็บแล้ว เดิม 344 px ประหยัด 82 px ≈ 44dp ซึ่งกลายเป็นพื้นที่ของจาร์วิส)
- **ทำให้การ์ดอื่นมีหลายหน้า:** ห่อหน้าใน `<com.mammonrn.phoneaikiosk.ui.PagedPanel>` ใส่ `android:tag="id|ชื่อหน้า"` ให้แต่ละหน้า วาง `LinearLayout` เปล่าบนแถบหัวข้อของการ์ด (เช่น `gold_pages_dots`) แล้วเรียก `attachIndicator` (`PagedSlideTest`)
  - หน้าที่สามคือ child ตัวที่สาม ไม่ต้องแก้โค้ดของ PagedPanel
  - ส่งข่าวของแต่ละหน้าด้วย `news(id, signature, now)`
  - ตัวอย่างที่เผื่อไว้: อากาศข้างนอก/ในบ้าน, ปุ่มไฟจากแต่ละระบบใน Google Home
- **การ์ดจาร์วิสไม่ใช้ระบบนี้** และห้ามแตะ

**การ์ดอากาศ (0.35.0):**
- **แหล่งข้อมูล:** Open-Meteo แหล่งเดิม ใช้โมเดล **ECMWF** (`models=ecmwf_ifs025`) ส่วน UV มาจาก best_match ในคำขอเดียวกัน เพราะ ECMWF ไม่ให้ UV
- **ลำดับเนื้อหา:**
  1. บรรทัดหัว (ขั้น 1, 16sp): อุณหภูมิตอนนี้และสภาพฟ้า เช่น "31.5°C แดดจัด"
  2. **ตัวเลขของวันนี้เท่านั้น** เป็นตาราง **3 คอลัมน์เท่ากัน กี่แถวก็ได้** (0.36.0) ป้าย (ขั้น 5 จาง) อยู่บนค่า (ขั้น 3) ได้แก่ สูง/ต่ำ · โอกาสฝน · ลม กม./ชม. · UV (พร้อมคำระดับ ต่ำ/ปานกลาง/สูง/สูงมาก/อันตราย) · PM2.5
     - **UV คือค่าของชั่วโมงนี้ ไม่ใช่ค่าสูงสุดของวัน (0.42.0):** เดิมแสดง "UV 8 สูงมาก" ตอนกลางคืน เพราะใช้ `uv_index_max` ตอนนี้ใช้ค่ารายชั่วโมงของ best_match ในคำขอเดิม และ**หลังพระอาทิตย์ตก (`is_day` = 0) ไม่แสดงช่อง UV** ค่าสูงสุดของวันยังส่งให้จาร์วิสตอบเมื่อถูกถาม (`uv_max`)
     - ช่องสุดท้ายของแถวที่ไม่เต็มจะกินคอลัมน์ที่เหลือ (`span`) คำระดับยาว ๆ เช่น "เริ่มมีผลต่อสุขภาพ" จึงมีที่พอและไม่ถูกตัด
     - ค่าที่ไม่มีให้ตัดช่องนั้นทิ้ง ห้ามใส่ขีดหรือศูนย์
     - ไม่แสดงความชื้นแล้ว เพราะไม่อยู่ในรายการที่ Poom กำหนด
  3. แถวพระอาทิตย์ขึ้น/ตก
  4. **พยากรณ์ 3 วันข้างหน้า** เป็นประโยคภาษาพูด 1–2 บรรทัด (ขั้น 4, `maxLines=2`) เขียนด้วยกฎตายตัวใน `server/kiosk_broker/forecast.py` ห้ามเป็นตารางหลายวัน
     - **ต้องมีตัวเลข (0.42.0):** จำนวนวันที่มีฝน ชื่อวัน และโอกาสสูงสุด เช่น "3 วันข้างหน้า มีฝน 2 วัน (ศ. ส.) โอกาสสูงสุด 57% ส่วนใหญ่ช่วงบ่าย อุณหภูมิใกล้เคียงเดิม" เดิม "มีฝนบางวัน" อย่างเดียวอ่านเหมือนเดิมทั้งที่ข้อมูลเปลี่ยน
     - "โอกาสฝน" ในตารางคือของ**วันนี้** ส่วนประโยคพยากรณ์คือ**พรุ่งนี้ถึงอีก 3 วัน** สองค่านี้จึงเปลี่ยนไม่พร้อมกันได้ ไม่ใช่บั๊ก
  - **แถบหัวข้อบอกที่มาของตำแหน่ง (0.42.0):** "อากาศ · เชียงราย · ตำแหน่งจริง" เมื่อใช้ตำแหน่งของเครื่อง หรือ "· ตำแหน่งสำรอง" เมื่อ broker ใช้ค่าสำรอง (มหาวิทยาลัยแม่ฟ้าหลวง) เป็นคำ ไม่ใช่พิกัด
- **PM2.5 (Poom อนุมัติ 2026-09-23):**
  - มาจาก Open-Meteo Air Quality (ข้อมูล CAMS) ผ่านแผง `air` ของ broker อายุ cache 30 นาที
  - แสดงเป็น "PM2.5 มคก./ลบ.ม." เหนือ "8.8 ดีมาก" คือตัวเลขพร้อมคำระดับ ไม่ใช้สีอย่างเดียว
  - คำระดับใช้เกณฑ์กรมควบคุมมลพิษ พ.ศ. 2566: ≤15 ดีมาก · ≤25 ดี · ≤37.5 ปานกลาง · ≤75 เริ่มมีผลต่อสุขภาพ · มากกว่านั้น มีผลต่อสุขภาพ
    - เกณฑ์นี้เป็นค่าเฉลี่ย 24 ชั่วโมง แต่ค่าบนจอเป็นค่ารายชั่วโมง คำระดับจึงบอกสภาพตอนนี้ ไม่ใช่ดัชนีทางการรายวัน
  - **ไม่มีข้อมูล** (broker รุ่นเก่า, แหล่งล่ม, ค่าเก่ากว่า 3 ชั่วโมง): ไม่มีช่อง PM2.5 เลย ตัวเลขอากาศช่องอื่นไม่เปลี่ยน
  - ถ้าไม่มีข้อมูลอากาศแต่มีค่าฝุ่น ช่องฝุ่นยังแสดง
- **ที่มาข้อมูล:** ดูหมวด "ที่มาข้อมูล" ในแผงควบคุม (ข้อ 5ก)

**การ์ดทองและน้ำมัน (หมวดสินค้าโภคภัณฑ์) เป็นตาราง ไม่ใช่ย่อหน้า (0.34.0):**
- ชื่อการ์ดเป็น "ทองและน้ำมัน · ทอง 96.5%" ความบริสุทธิ์อยู่บนแถบหัวข้อเป็นคำพร้อมตัวเลข จึงไม่สับสนกับ % การเปลี่ยนแปลงของราคา
- **แบ่งเป็นสองส่วน:** ทองอยู่บน น้ำมันอยู่ล่าง แต่ละส่วนขึ้นด้วย**บรรทัดหัวข้อ**สีจาง 11sp ที่บอกว่าตัวเลขคืออะไร เช่น "ทองคำ บาทละ · +/− เทียบครั้งก่อน" และ "น้ำมันถูกสุด บาท/ลิตร · ราคากรุงเทพฯ 23 ก.ย."
  - บอกครั้งเดียวที่หัวส่วน แทนบรรทัดหมายเหตุใต้ตัวเลข จึงไม่เพิ่มบรรทัด
  - หัวส่วนน้ำมันบอกอายุข้อมูลเฉพาะตอนแหล่งล่ม (stale) เพราะวันที่ประกาศบอกอยู่แล้วว่าเป็นราคาวันไหน ถ้าใส่ทั้งคู่ หัวข้อยาวจนถูกตัด (เห็นบน A07 ใน 0.35.0)
- **แต่ละแถวเป็น 3 คอลัมน์** (`TableLayout`): ชื่อ | ราคา | สิ่งที่ตามมา
  - **ราคาชิดขวาในคอลัมน์ของตัวเอง** ตัวเลขทุกแถวจึงตรงกันเป็นแนว
  - ห้ามรวมชื่อกับราคาเป็นข้อความเดียว เพราะจุดเริ่มของราคาจะขึ้นกับความยาวชื่อ และตัวเลขในชื่อ (95, E20) จะติดกับราคา
- **ทอง:** "รูปพรรณ | 68,950 | +0.07%" ชื่อและราคา 15sp ส่วน % 13sp สีจาง
- **น้ำมัน:** แสดง 3 ชนิดที่คนเติมมากที่สุด คือ ดีเซล, โซฮอล์ 95, E20 ขนาด 14sp
  - คอลัมน์ราคาคือ **ราคาถูกสุดเสมอ**
  - หลังราคาเป็นชื่อปั๊มที่ขายราคานั้น ส่วนปั๊มใน 3 อันดับที่แพงกว่าให้บอกส่วนต่าง เช่น "ปตท. บางจาก เชลล์ +0.30" ดูแล้วรู้ทันทีว่าใครถูกสุดและแพงกว่ากี่บาท
- **แถวจัดตาม baseline ไม่ใช่กึ่งกลาง** (`TableRow.isBaselineAligned`) เพราะตัวไทยกับตัวเลข pixel สูงไม่เท่ากัน ถ้าจัดกึ่งกลาง ตัวเลขจะลอยสูงกว่าคำ (เห็นบน A07 ใน 0.34.0)
  - เซลล์ไม่มี padding ด้านบน การ์ดจึงไม่สูงกว่าแบบย่อหน้าเดิม
- **ทุกเซลล์เป็นบรรทัดเดียว** (`maxLines=1`, `ellipsize=end`) ห้ามขึ้นบรรทัดกลางคำไทย ถ้าชื่อปั๊มยาวเกิน ให้ตัดท้ายด้วย "…" ไม่ใช่ขึ้นบรรทัดใหม่
- **สถานะ:**
  - แหล่งล่ม: แสดงค่าเดิมพร้อมอายุข้อมูลในบรรทัดหัวข้อ
  - ไม่เคยมีข้อมูล: "น้ำมัน: ข้อมูลไม่พร้อม" ในบรรทัดหัวข้อ
  - broker รุ่นเก่าที่ยังไม่ส่งข้อมูลน้ำมัน: ซ่อนส่วนน้ำมัน (GONE)
- **สรุปตอนหุบ:** "ทองแท่ง 68,150 บ. · ดีเซล 40.69"

**ทางออก:** `exit_corner` ขนาด 72dp มุมขวาล่าง แตะ 10 ครั้งเพื่อออก ห้ามย้าย ห้ามย่อ และห้ามวางของที่กดได้ไว้ใต้มัน

**ปุ่มใน taskbar (0.61.0, Poom: ออกแบบใหม่, `ui/TaskbarButtons`):** แบบ A เป็นค่าเริ่มต้น — ปุ่มจาร์วิสแบบปุ่ม Start ปี 1995 ไอคอนจาร์วิส 18dp + "ถามจาร์วิส" ตัวหนา 13sp (เขียนบนปุ่มว่าทำอะไร) และปุ่มแผงควบคุมเป็นไอคอน 18dp เหนือคำ "แผงควบคุม" 9sp บรรทัดเดียว กว้าง 64dp (ตอนกว้างตามคำ เหลือแค่ "แผง") · แบบ B (ไอคอนล้วน) และ C ("● พูดกับจาร์วิส" ปุ่มกว้าง) มีไว้ให้ Poom เทียบ เปิดได้เฉพาะ debug `TEST_TASKBAR --ei style 1|2` แล้วเปิดหน้าหลักใหม่ · ทั้งสามแบบ **ไม่ทำให้ taskbar สูงขึ้น** (การ์ดจาร์วิส 394px = 210dp บน A07) และ `exit_corner` ตำแหน่งเดิมทุกพิกเซล [585,1465][720,1600] ตรวจด้วย uiautomator ทั้งสามแบบ

## 5ก. แผงควบคุม (Control Panel)

**0.61.0 (Poom 2026-09-25): โฟลเดอร์แทนหัวหมวด** (`SettingsActivity.TILES`, แทน `GROUPS` เดิม) หน้าเดียว 3 ช่องต่อแถว ไม่ต้องเลื่อน:
แถว 1 **ไฟในบ้าน · WiFi · ไฟฉาย** (ของที่กดเปิดปิดทันที อยู่เดี่ยว ไม่เข้าโฟลเดอร์) · แถว 2 **สื่อ (5)** (เพลง วิดีโอ วิทยุ กล้อง บันทึกเสียง) · **เครื่องมือ (5)** (นาฬิกาปลุก จับเวลา เครื่องคิดเลข โน้ต ระดับน้ำ) · จัดการไฟล์ · แถว 3 ยืนยันตัวตน · **"ที่มาข้อมูล" สุดท้ายเสมอ**
- **ไอคอนโฟลเดอร์ = ภาพย่อ 2×2 ของแอปข้างใน** (22dp ต่อรูป) ชื่อมีจำนวน "สื่อ (5)" เครื่องอ่านหน้าจออ่านรายชื่อข้างในและ "แตะเพื่อเปิด"
- **เปิดเป็นป๊อปอัปทับหน้าเดิมที่มืดลง** (`panel_scrim` #99000000) หน้าต่างกรอบนูน แถบกรมท่ามีชื่อโฟลเดอร์และปุ่ม X · ปิดได้ 3 ทาง: X, แตะนอกกรอบ, Back · เปิดแอปจากป๊อปอัปแล้วป๊อปอัปปิดเอง ปุ่มปิดของแอปกลับมาแผงควบคุม (ไม่ใช่กลับเข้าโฟลเดอร์) · log `KioskPanel: folder open/closed`
- **ช่องในแถวเดียวกันสูงเท่ากัน** (ชื่อสองบรรทัดเช่น "เครื่องเล่น เพลง" ไม่ทำให้ช่องข้างๆ เตี้ยกว่า) ช่องว่างท้ายแถวที่ไม่เต็มเป็น `Space` สูง match_parent — **ห้ามเป็น View เปล่าหรือสูง 0:** View เปล่ากินที่ทั้งป๊อปอัป และแถวที่มีลูกไม่ match_parent ทุกตัวจะเตี้ยเป็น 0 (เจอบน A07 ทั้งสองแบบ 0.61.0)

**ปุ่มปิด (มุมขวาบน) ของทุกแอป กลับไปหน้าที่เปิดแอปนั้นมา (0.61.0, Poom, `ui/Origin`):** เปิดจากแผงควบคุม → กลับแผงควบคุม / จากตัวจัดการไฟล์ (หรือ Drive) → กลับหน้านั้น / จากการ์ดหน้าแรกหรือคำสั่งเสียง → หน้าแรก เครื่องอ่านหน้าจออ่านว่า "ปิด กลับ…" ตามที่ไปจริง ปุ่ม "กลับหน้าหลัก" ด้านล่างยังไปหน้าแรกตามคำบนปุ่ม และ Hey Jarvis ยังปิดทุกหน้า (ข้อ 11)

**หน้าที่:** หน้าตั้งค่าสไตล์ Windows 95 สำหรับสิ่งที่พูดด้วยเสียงไม่สะดวก หมวดตอนนี้: นาฬิกาปลุก, ยืนยันตัวตน, ที่มาข้อมูล, WiFi, ไฟฉาย, จัดการไฟล์ (0.44.0, ข้อ 5ค), ไฟในบ้าน (0.47.0, ข้อ 5ง), เครื่องคิดเลข (0.52.0, ข้อ 5จ), เครื่องเล่นเพลง (0.53.0, ข้อ 5ฉ), เครื่องเล่นวิดีโอ (0.56.0, ข้อ 5ซ)
- **ทุกฟีเจอร์ใหม่เป็นหน้าจอในแอปนี้ ไม่ใช่แอปแยก** (Poom 2026-09-24) เข้าจากไอคอนในแผงควบคุม และถูก `KioskScreens` ปิดเองเมื่อได้ยิน Hey Jarvis (ข้อ 11)

**การเข้าและออก:**
- **เข้า:** กดปุ่มแผงควบคุมใน taskbar ข้างปุ่มจาร์วิส (ชิดซ้าย ห่างจาก `exit_corner` ที่มุมขวา) กว้าง 64dp ในแบบ A (48dp ในแบบ B/C)
- **ออก ทำได้ 3 ทาง:**
  - ปุ่ม "กลับหน้าหลัก" เต็มความกว้าง สูง 56dp อยู่ล่างสุดเสมอ
  - ปุ่ม X บนแถบหัวข้อ ขนาด 48×48dp (0.54.0; เดิม 44×40dp ต่ำกว่าขั้นต่ำ)
  - ปุ่ม Back ของเครื่อง จะถอยทีละหน้า คือ แก้ไข → รายการ → แผงควบคุม → หน้าหลัก
- **ไม่หลุดจาก kiosk:** เป็น activity ของแอปเราเอง จึงอยู่ใน lock task และการกลับหน้าหลักจะเปิด `MainActivity` ใหม่ทุกครั้ง แทนที่จะพึ่ง task stack
- **การ์ดจาร์วิสไม่ขยับ:** หน้าตั้งค่าเป็นหน้าต่างของมันเอง ไม่แตะ layout หน้าหลัก

**รูปแบบ:**
- หน้าต่างนูนบนพื้นเดสก์ท็อป แถบหัวข้อกรมท่า และปุ่มนูนที่ยุบลงเมื่อกด (`retro_button`)
- ช่องกรอกเป็นพื้นขาวลึกลง (`retro_field`)
- ช่องติ๊กวาดเอง (`ic_pixel_check_on/off`)
- ตัวเลือกที่เลือกอยู่เป็นพื้นกรมท่า ตัวอักษรขาว
- ข้อความใช้ภาษาเขียนทางการ ปุ่มบอกสิ่งที่จะเกิด เช่น "บันทึก", "ยกเลิก", "เพิ่มการปลุก", "กลับหน้าหลัก"
- **ภาษาเขียนทางการทุกหน้า (Poom 0.64):** บนจอไม่มี ครับ/ค่ะ/นะ/ได้เลย/หน่อย/โอเค/อีกที/…เอง เสียงจาร์วิสยังพูดกันเองตามเดิม ประโยคจาก broker ที่ใช้ร่วมกับเสียง (เช่น ผลกดไฟ) ผ่าน `ui/ScreenWords.formal` ก่อนขึ้นจอ หน้าปฏิทินใช้ข้อความของตัวเองเมื่อผิดพลาด `ScreenWordsTest` อ่าน string ทุกตัวและข้อความไทยในโค้ดทุกบรรทัดนอกส่วนเสียง

**ขนาดเป้าที่กด:** ทุกปุ่มสูงอย่างน้อย 48dp ปุ่มหลักสูง 56dp ปุ่มตั้งเวลา ▲/▼ กว้าง 64dp สูง 48dp และกดค้างเพื่อเลื่อนต่อเนื่องได้ ทุกขนาดมาจาก `ui/UiScale.kt` (ข้อ 5ช)

**หน้านาฬิกาปลุก:**
- แต่ละรายการมี:
  - ช่องติ๊กเปิด/ปิด พร้อมคำ "เปิด"/"ปิด"
  - เวลา 12 ชั่วโมงแบบเดียวกับนาฬิกา taskbar
  - ชื่อ
  - รอบการปลุก ("ทุกวัน", "จันทร์–ศุกร์", "ครั้งเดียว" ฯลฯ)
  - ปุ่ม "แก้ไข" และ "ลบ"
- **การลบ:** ย้อนกลับไม่ได้ จึงถามยืนยันในแถวเดียวกันก่อน ("ลบการปลุกนี้ใช่หรือไม่" [ลบ] [ยกเลิก])
- **ตอนยังไม่มีการปลุก:** บอกวิธีเพิ่ม ทั้งแบบกดปุ่มและแบบพูด
- **หน้าแก้ไข:**
  - เวลาใช้ปุ่ม ▲/▼ แยกชั่วโมงและนาที
  - ชื่อ (ไม่บังคับ ไม่เกิน 20 ตัวอักษร)
  - "ปลุกเมื่อใด" มีตัวเลือก ทุกวัน / เลือกวัน (จ. ถึง อา. ปุ่มละ 48dp) / ครั้งเดียว
  - ข้อผิดพลาดแสดงในหน้าเดียวกันด้วยสี `retro_bad` พร้อมคำอธิบาย
- **ค่าเริ่มต้น:** การปลุกที่ตั้งด้วยเสียงเป็น "ทุกวัน" (Poom) ส่วน "ครั้งเดียว" จะปิดตัวเองหลังดัง

**หมวด "ที่มาข้อมูล" (0.36.0):** ไอคอนหนังสือ `ic_pixel_sources` เป็นหน้าอ่านอย่างเดียว บอกแหล่งและสัญญาอนุญาตของตัวเลขทุกชุดบนจอ ได้แก่ Open-Meteo (CC BY 4.0, ECMWF), ฝุ่นจาก CAMS/Copernicus, OpenStreetMap (ODbL), ทอง, น้ำมัน และคริปโต
- CC BY 4.0 และ ODbL กำหนดให้ให้เครดิต หน้านี้คือเครดิตนั้น อยู่ห่างจากจอหลักหนึ่งแตะ
- ข้อความอยู่ใน `R.array.data_sources` ต้องตรงกับ `CREDITS` ใน `server/kiosk_broker/dashboard.py` ทุกครั้งที่เพิ่มแหล่งข้อมูล

**WiFi (0.42.0, Poom เลือก):** เปิดแผง WiFi ของระบบทับ kiosk
- แอปตั้งค่าของระบบ (`com.android.settings`) **ถูกเพิ่มเข้า allowlist ของ lock task เฉพาะตอนกดปุ่มนี้** แล้วถูกเอาออกทันทีที่หน้าจอของ kiosk กลับมา (`settings/WifiPanel.restore` ใน `onResume` ของทั้งแผงควบคุมและหน้าหลัก)
- ยังอยู่ใน lock task ตลอด Home และ Recents ใช้ไม่ได้ กด Back กลับ kiosk
- ความเสี่ยงที่ Poom ยอมรับ: จากแผงนี้กดเข้าหน้าตั้งค่าอื่นของเครื่องได้ระหว่างที่เปิดอยู่

**ไฟฉาย (0.42.0):** ไอคอนเป็นสวิตช์ กดแล้วเปิดหรือปิด ป้ายใต้ไอคอนบอกสถานะจริงจากระบบ "ไฟฉาย: เปิด" / "ไฟฉาย: ปิด" / "ไฟฉาย: ใช้ไม่ได้" ไม่ต้องใช้สิทธิ์ และถ้ากล้องยืนยันตัวตนเปิด ระบบจะปิดไฟฉายเองและป้ายจะเปลี่ยนตาม

**เพิ่มหมวดใหม่:** เพิ่มรายการใน `CATEGORIES` ของ `settings/SettingsActivity.kt` (ไอคอนวาดเองขนาด 48dp, ชื่อหรือฟังก์ชันที่คืนชื่อตามสถานะ และสิ่งที่จะทำ) แล้วเพิ่มหัวข้อในส่วนนี้

## 5ข. ยืนยันตัวตน (0.37.0, Poom 2026-09-23)

**หน้าที่:** ยืนยันว่าเป็น Poom ก่อนเข้าถึงข้อมูลส่วนตัว (อีเมล ปฏิทิน ในส่วนที่ 2) หน้าจอเดียว `auth/VerifyActivity` ใช้ทั้งยืนยัน ลงทะเบียนใบหน้า และตั้งรูปแบบ

**การตัดสินใจของ Poom:**
- ลงทะเบียนใบหน้า **Poom คนเดียว** จึงมีข้อมูลใบหน้าชุดเดียว ไม่ใช่รายชื่อหลายคน
- **รูปแบบ (pattern) 3×3 เป็นทางสำรอง** เมื่อกล้องจำหน้าไม่ได้
- **ผ่านแล้วเปิดสิทธิ์ 2 นาที** แล้วปิดเอง (`AccessGrant` ใช้นาฬิกาที่ไม่ขึ้นกับเวลาเครื่อง และเก็บในหน่วยความจำเท่านั้น รีสตาร์ทแอปแล้วสิทธิ์ปิด)

**ลำดับการยืนยัน:**
1. **ใบหน้า:** "มองกล้อง" → "กะพริบตา 1 ครั้ง" → "ลืมตาแล้วมองกล้อง" ต้องจำหน้าได้ทั้งก่อนและหลังกะพริบ และต้องเป็นใบหน้าเดียวกันที่ถูกติดตามอยู่ (`BlinkCheck`)
2. **ถ้าจำไม่ได้** (ไม่ตรง 8 เฟรม, ไม่ผ่านใน 20 วินาที หรือเปิดกล้องไม่ได้) และตั้งรูปแบบไว้ → เปลี่ยนเป็นช่องลากรูปแบบเองพร้อมบอกเหตุผล ถ้าไม่ได้ตั้งรูปแบบ → "ยืนยันตัวตนไม่สำเร็จ"
3. ปุ่ม **"ใช้รูปแบบแทน"** อยู่ตลอดระหว่างใช้กล้อง และ **"ยกเลิก"** อยู่ทุกหน้า
4. ไม่มีใครแตะเกิน 90 วินาที หรือหน้าต่างไม่อยู่บนจอ (จอดับ) → ยกเลิกและปิดกล้องเอง
5. ผลแสดง 1.5 วินาทีแล้วปิดหน้าต่าง

**ความปลอดภัยที่บังคับด้วยโค้ด:**
- **ไม่เก็บภาพ:** เฟรมจากกล้องอยู่ในหน่วยความจำเท่านั้น แปลงเป็นตัวเลข 128 ตัวแล้วทิ้งภาพทันที (`FaceScanner`) ภาพ preview บนจอเป็น surface สด ไม่ใช่ภาพที่บันทึก
- **เก็บเฉพาะตัวเลข:** ตัวเลขจากใบหน้า 5 ชุด และ hash ของรูปแบบ (PBKDF2-HMAC-SHA256 + salt สุ่ม) เข้ารหัส AES-GCM ด้วยกุญแจใน Android Keystore (`SecretBox`) เก็บใน `noBackupFilesDir` ซึ่ง Android ไม่สำรองขึ้น cloud
- **กล้องเปิดเฉพาะตอนหน้าต่างนี้อยู่บนจอ** และปิดทันทีเมื่อเปลี่ยนเป็นช่องลากรูปแบบ
- **เปลี่ยนข้อมูลต้องผ่านก่อน:** ถ้ามีใบหน้าหรือรูปแบบอยู่แล้ว การลงทะเบียนใบหน้าใหม่หรือเปลี่ยนรูปแบบต้องยืนยันตัวตนก่อนเสมอ
- **ลบต้องยืนยันตัวตนก่อน (0.42.0, Poom แทนกติกาเดิม "ลบได้ทันที"):** กดลบ → ถามยืนยันในที่เดิม → "ยืนยันตัวตนแล้วลบ" เปิดหน้ายืนยันตัวตน ผ่านแล้วจึงลบ ไม่ผ่านขึ้น "ยังไม่ได้ลบ เพราะยืนยันตัวตนไม่สำเร็จ" การลบปิดสิทธิ์ที่เปิดอยู่ด้วย
  - **ทางออกเมื่อกล้องเสียและลืมรูปแบบ (Poom เลือก):** ปุ่ม "ยืนยันตัวตนไม่ได้" ถาม VPS (`POST /v1/auth/reset`) ลบได้เฉพาะเมื่อ Poom รัน `allow-auth-reset` บน VPS ภายใน 10 นาที และใช้ได้ครั้งเดียว ต้องมีทั้ง VPS และตัวเครื่อง
  - ช่องโหว่ที่รู้: คนอื่นลบทั้งสองอย่างแล้วลงทะเบียนหน้าตัวเองได้ ส่วนที่ 2 จึงต้องผูก broker กับ `enrollmentId` ที่ Poom อนุมัติบน VPS ก่อนเปิดข้อมูลส่วนตัว
- **ลากผิด:** ผิดติดกัน 5 ครั้งต้องรอ 30 วินาที ผิดต่อเพิ่มเป็นสองเท่าทุกครั้ง สูงสุด 15 นาที จำนวนครั้งเก็บไว้ในไฟล์ ปิดเปิดแอปใหม่ก็ไม่รีเซ็ต
- **log:** เขียนแค่ผลและวิธี (`KioskAuth: pass method=face best=0.62`) ห้ามมีภาพ ตัวเลขจากใบหน้า หรือรูปแบบ

**ขีดจำกัด (บอกตรง ๆ):** การกะพริบตากันภาพถ่ายนิ่งได้ แต่กันวิดีโอของ Poom ที่กะพริบตาไม่ได้ และคนหน้าคล้ายมากอาจผ่าน เกณฑ์ความเหมือน 0.40 ตั้งเข้มกว่าค่าแนะนำของ OpenCV (0.363)

**หน้าตา:**
- หน้าต่างแบบเดียวกับแผงควบคุม ไอคอน `ic_pixel_face` บนแถบหัวข้อ
- ข้อความสถานะ 16sp ตัวหนา ขั้นตอนละหนึ่งประโยค เป็นคำสั่งที่ทำตามได้ทันที
- ภาพกล้อง 240×320dp อยู่ในกรอบ sunken ช่องลากรูปแบบ 280dp
- จุดของช่องลากเป็นสี่เหลี่ยมนูนแบบ 1995 จุดที่เลือกและเส้นเป็นสีกรมท่า ลากผิดเป็นสีแดง `retro_bad` และมีข้อความบอกด้วย ไม่ใช้สีอย่างเดียว
- ลากผ่านจุดกลาง (เช่น 0 → 2) นับจุดกลางด้วย เหมือนรูปแบบของ Android

**แผงควบคุม หมวด "ยืนยันตัวตน":** สถานะใบหน้า (พร้อมวันที่ลงทะเบียน) และรูปแบบ, ปุ่มลงทะเบียน/เปลี่ยน, ปุ่มลบ, "ทดสอบการยืนยันตัวตน", เวลาที่สิทธิ์ยังเปิดอยู่ (นับถอยหลังทุกวินาที) และ "ปิดสิทธิ์ทันที"
- ไอคอนในแผงควบคุมเรียง **3 ไอคอนต่อแถว** กว้างเท่ากัน ขึ้นแถวใหม่เมื่อเกิน (แถวเดียวแบบเดิมล้นจอเมื่อมีไอคอนที่ 3)

## 5ค. จัดการไฟล์ (File manager, 0.44.0, Poom 2026-09-24)

**หน้าที่:** ดูไฟล์และโฟลเดอร์ในเครื่อง ดูรายละเอียดไฟล์ แตกไฟล์ zip คัดลอก ย้าย เปลี่ยนชื่อ ลบ ค้นหาตามชื่อ และเปิดดู NAS ในบ้านแบบอ่านอย่างเดียว เป็นหน้าจอของแอปนี้ (`files/FilesActivity`) เข้าจากไอคอน "จัดการไฟล์" ช่องที่ 6 ของแผงควบคุม ไม่มีคำสั่งเสียง

**ไม่หลุดจาก kiosk:**
- ไม่เปิดอะไรนอกแอปเลย ไม่มี "เปิดด้วย…" ไม่ใช้ตัวเลือกไฟล์ของระบบ ไม่เปิดหน้าตั้งค่าสิทธิ์ (lock task จะปฏิเสธ และปุ่มที่กดแล้วไม่เกิดอะไรแย่กว่าไม่มีปุ่ม) `FileManagerTest` ตรวจว่า `startActivity` เปิดได้เฉพาะหน้าจอของแอปนี้: หน้าหลัก เครื่องเล่นเพลง เครื่องเล่นวิดีโอ และตัวดูรูป
- **แตะไฟล์ (0.59.0):** เพลงเปิดในเครื่องเล่นเพลง วิดีโอเปิดในเครื่องเล่นวิดีโอ รูปเปิดในตัวดูรูป **แบบไฟล์เดียว** (ข้อ 5ฌ) ไฟล์อื่นเปิดหน้ารายละเอียด **แตะค้าง = รายละเอียด** ของทุกแถว (คัดลอก ย้าย เปลี่ยนชื่อ ลบ) ใต้รายการมีบรรทัดบอกไว้
- Hey Jarvis ปิดหน้านี้ งานที่ค้างถูกหยุดและลบส่วนที่ทำไม่เสร็จ

**หน้าต่าง:** แบบเดียวกับแผงควบคุม (แถบหัวข้อกรมท่า ไอคอนแฟ้มเหลือง `ic_pixel_folder` ตั้งแต่ 0.59.0 ปุ่ม X 44×40dp ปุ่ม "กลับหน้าหลัก" 56dp ล่างสุด) Back ถอยทีละชั้น: รายละเอียด → โฟลเดอร์ → โฟลเดอร์ข้างบน → รายการที่เก็บไฟล์ → แผงควบคุม ส่วน X และปุ่มล่างกลับหน้าหลักและปิดแผงควบคุมที่อยู่ข้างใต้ด้วย

**หน้าแรก:** ปุ่มนูนใหญ่ (สูงอย่างน้อย 64dp ไอคอน 40dp) ต่อที่เก็บหนึ่งที่: "หน่วยความจำเครื่อง" (บรรทัดสองบอกพื้นที่ว่าง), การ์ด SD ถ้ามี, และ "NAS (อ่านอย่างเดียว)" **ไม่มีทางขึ้นไปเหนือราก** ("/" และ "/storage" เป็นของระบบ)

**โฟลเดอร์มาตรฐาน (0.59.0, Poom ส่งภาพอ้างอิงตัวจัดการไฟล์ปี 1995):** ทุกหน้าที่แสดงโฟลเดอร์ใช้ส่วนเดียวกันคือ `files/FolderBrowser` ทั้งตัวจัดการไฟล์ (เครื่อง การ์ด SD และ NAS) และหน้า "เพิ่ม" ของเครื่องเล่นทั้งสอง ห้ามเขียนรายการโฟลเดอร์แยกอีก วาดเองทั้งหมด ไม่มีชื่อหรือภาพจากต้นแบบ
- **ไม่มีต้นไม้โฟลเดอร์:** แตะโฟลเดอร์ = เปิดโฟลเดอร์นั้นเต็มความกว้าง แบบแอปมือถือ
- **แถวเครื่องมือ:** ปุ่ม "▲ ขึ้นหนึ่งชั้น" 48dp เสมอ (ไม่พึ่งปุ่มย้อนกลับของ Android อย่างเดียว เพราะอยู่ใน lock task) ต่อด้วยปุ่มของหน้านั้น ("ค้นหา" ในเครื่อง, "ลองใหม่" "แก้ไขการตั้งค่า" บน NAS) ที่ชั้นบนสุดของตัวจัดการไฟล์ปุ่มนี้กลับไปหน้าเลือกที่เก็บ ในหน้า "เพิ่ม" เป็นสีจางกดไม่ได้
- **breadcrumb:** "เครื่อง › Music › 2024" พื้นขาว สูง 48dp แต่ละชื่อเป็นเป้ากดกว้างอย่างน้อย 48dp แตะชื่อไหนกลับไปโฟลเดอร์นั้นทันที ชื่อสุดท้ายคือที่อยู่ ตัวหนา กดไม่ได้ ยาวเกินจอเลื่อนซ้ายขวาได้ และเลื่อนไปท้ายเองทุกครั้ง จึงเห็นโฟลเดอร์ปัจจุบันเสมอ
- **หัวคอลัมน์:** ชื่อ / ประเภท (64dp) / ขนาด (64dp ชิดขวา) บนแถบนูนแบบหัวตาราง ชื่อได้ที่เหลือ ตัดตรงกลางเพื่อเก็บต้นชื่อและนามสกุล
- **แถว:** สูงอย่างน้อย 48dp (บรรทัดเดียว) ไอคอน 32dp ต่างกันที่รูปร่าง (ข้อ 6) ชื่อ 15sp ประเภทและขนาด 13sp โฟลเดอร์ไม่มีขนาด
- **แถบสถานะล่างสุด** สามช่องลึก: "ว่าง 39.6/49.5 GB" (NAS: "อ่านได้อย่างเดียว"), "12 รายการ", "เลือก 5 · 59.1 MB" (สั้นให้พอดีช่อง ตัวยาวกว่านี้ถูกตัดบน A07)
- แถวที่เลือกเป็นพื้นกรมท่า ไอคอนจึงมีพื้นเทาอ่อนรองไว้ ไม่งั้นโน้ตสีกรมท่าจะหายไปในแถว (เห็นบน A07)
- **เลือกหลายรายการ** (หน้า "เพิ่ม"): ทุกแถวไฟล์มีช่องติ๊ก ☐ `ic_pixel_check_off` / ☑ `ic_pixel_check_on` **ต่างกันที่รูปร่าง (มีเครื่องหมายถูกหรือไม่) ไม่ใช่แค่สี** แถวที่เลือกเป็นพื้นกรมท่าตัวขาว แถบกรมท่าบนรายการ "เลือกแล้ว N รายการ" กับ [เลือกทั้งหมด][ไม่เลือก] ที่เลือกไว้ยังอยู่เมื่อเดินไปโฟลเดอร์อื่น
- อ่านโฟลเดอร์บน worker เสมอ คำตอบของโฟลเดอร์ที่ออกไปแล้วถูกทิ้ง log มีแค่จำนวน ห้ามชื่อหรือ path

**โฟลเดอร์ที่ระบบไม่อนุญาต (ห้ามปล่อยให้งง):**
- `Android/data` และ `Android/obb` ของทุกที่เก็บ **แสดงในรายการ ไม่ซ่อน** เป็นไอคอนแม่กุญแจ `ic_pixel_lock` พร้อมคำ "ระบบไม่อนุญาตให้เปิด" (คนที่มองหาจะคิดว่าไฟล์หายถ้าซ่อน) แตะแล้วขึ้นหน้าอธิบายว่าเป็นข้อมูลของแต่ละแอป Android ไม่ให้แอปอื่นเปิดไม่ว่าจะให้สิทธิ์ใด และไฟล์ยังอยู่ครบ
- โฟลเดอร์อื่นที่อ่านไม่ได้: หน้าเดียวกัน ข้อความ "ระบบไม่อนุญาตให้อ่านโฟลเดอร์นี้"
- **ยังไม่ได้สิทธิ์ "เข้าถึงไฟล์ทั้งหมด":** ที่เก็บขึ้นเป็นแม่กุญแจ แตะแล้วบอกว่าให้จากหน้าจอนี้ไม่ได้ พร้อมคำสั่ง adb ที่ใช้ให้สิทธิ์ (ข้อความนี้ไม่ใช่ความลับ)
- การค้นหาข้ามสองโฟลเดอร์นี้

**รายละเอียด:** (แตะค้างที่แถว) ไอคอน 40dp กับชื่อเต็ม 16sp ตัวหนา (ขึ้นบรรทัดได้) แล้วเป็นคู่ป้าย (12sp จาง) เหนือค่า (15sp): ประเภท ขนาด แก้ไขล่าสุด อยู่ที่ และ "ข้างใน" (จำนวนรายการของโฟลเดอร์ หรือจำนวนไฟล์และขนาดเมื่อแตกของ zip นับบน worker ระหว่างนั้นขึ้น "กำลังโหลด…")
- ปุ่ม: zip มี **"แตกไฟล์" เต็มความกว้าง 56dp เป็นปุ่มหลัก** จากนั้นคู่ปุ่ม 48dp [คัดลอก][ย้าย] และ [เปลี่ยนชื่อ][ลบ]
- zip ที่ตรวจแล้วรู้ว่าแตกไม่ได้ (ไม่ปลอดภัย เสีย ตั้งรหัสผ่าน): ช่อง "ข้างใน" บอกเหตุผล และปุ่ม "แตกไฟล์" เป็นสีจางกดไม่ได้ ไม่ชวนให้กดแล้วล้ม
- ปุ่มย้อนกลับในหน้าย่อยเขียน "◄ กลับ" แบบเดียวกับ "◄ แผงควบคุม"
- rar/7z/tar/gz: บอกว่า "ยังแตกไฟล์ประเภทนี้ไม่ได้ รองรับเฉพาะ zip" (Poom: รูปแบบอื่นค่อยเพิ่มเมื่อเจอไฟล์จริง)

**ลบต้องถามยืนยันเสมอ:** กล่องในที่เดิมบอกชื่อ และถ้าเป็นโฟลเดอร์บอกจำนวนรายการข้างใน "…เมื่อลบแล้วจะกู้คืนไม่ได้" ปุ่ม [ลบถาวร][ยกเลิก] ไม่มีทางลบโดยไม่ผ่านกล่องนี้ และไม่มีถังขยะ (Android ไม่มีถังขยะกลางสำหรับไฟล์ทุกชนิด)

**ไม่เขียนทับอะไรเลย:** คัดลอก ย้าย แตกไฟล์ ที่เจอชื่อซ้ำจะตั้งชื่อ "ชื่อ (2).นามสกุล" แล้วบอกในข้อความผล "(ตั้งชื่อใหม่ เพราะมีชื่อเดิมอยู่แล้ว)" ไม่ถาม เพราะไม่มีอะไรเสีย

**คัดลอกและย้าย:** กดแล้วเข้าโหมดเลือกปลายทาง แถบกรมท่าตัวขาวบนสุด "เลือกโฟลเดอร์ปลายทางสำหรับคัดลอก “x”" พร้อม [วางที่นี่][ยกเลิก] เดินเข้าโฟลเดอร์ได้ตามปกติ ระหว่างนั้นแสดงเฉพาะโฟลเดอร์ ไม่มีปุ่มค้นหา และ NAS ไม่อยู่ในรายการ (อ่านอย่างเดียว เป็นปลายทางไม่ได้) ย้ายโฟลเดอร์ไปไว้ในตัวเองถูกปฏิเสธพร้อมเหตุผล

**งานยาว:** หน้าความคืบหน้า: ชื่องาน 16sp ตัวหนา, แถบความคืบหน้าแบบ 1995 (กรอบลึก ไส้กรมท่า) สูง 28dp, "12 จาก 40 ไฟล์ · 120 MB จาก 300 MB", ปุ่ม "หยุด" 56dp Back ก็หยุด
- **หยุดแล้วไม่เหลือของครึ่ง ๆ:** ไฟล์หรือโฟลเดอร์ที่คัดลอกไม่เสร็จถูกลบ ต้นฉบับไม่เปลี่ยน การย้ายลบต้นฉบับหลังคัดลอกครบเท่านั้น การลบที่หยุดกลางทางบอกตรง ๆ ว่า "บางรายการอาจถูกลบไปแล้ว"
- ผลงานขึ้นเป็นกล่องบนสุดของหน้าถัดไปหนึ่งครั้ง ผิดพลาดเป็นสี `retro_bad` พร้อมคำอธิบาย

**zip:**
- แตกลงโฟลเดอร์ใหม่ข้าง zip ชื่อตาม zip ("photos.zip" → "photos" หรือ "photos (2)")
- **ความปลอดภัย:** ตรวจชื่อทุก entry ก่อนเขียนอะไร entry ที่เป็น path สัมบูรณ์ มีอักษรไดรฟ์ หรือปีนด้วย ".." ทำให้ไม่แตกเลยสักไฟล์ ("ไม่ปลอดภัย") ตรวจซ้ำด้วย canonical path ตอนเขียน และ **ห้ามปิด ZipPathValidator ของ Android 14+** (test ตรวจว่าไม่มีโค้ดไหนเรียก `clearCallback`/`setCallback`)
- ล้มกลางทาง: ลบโฟลเดอร์ที่แตกไปครึ่งหนึ่ง ได้ทั้งหมดหรือไม่ได้เลย
- เหลือพื้นที่ต่ำกว่า 200 MB ระหว่างแตก (zip ที่แจ้งขนาดต่ำกว่าจริง) หยุดเอง
- ชื่อไทยใน zip จาก Windows ที่ไม่ได้ติดธง UTF-8 ลองเปิดด้วย Windows-874 ต่อ
- zip ที่ตั้งรหัสผ่าน และไฟล์เสีย มีข้อความของตัวเอง

**ค้นหา:** ค้นจากโฟลเดอร์ที่อยู่ลงไปทุกชั้น ("ค้นหาใน … และโฟลเดอร์ย่อย") ตามชื่อบางส่วน ไม่สนตัวพิมพ์ แสดงอย่างมาก 200 รายการ ถ้ามีมากกว่าบอกให้ใช้คำที่เจาะจงขึ้น ระหว่างค้นขึ้นจำนวนโฟลเดอร์ที่ตรวจแล้ว ไม่พบบอกคำที่ค้นและทางลองต่อ บรรทัดสองของผลคือที่อยู่ของไฟล์

**NAS (Poom: อ่านอย่างเดียวก่อน):**
- ต่อผ่าน SMB2/3 ด้วย smbj (pure Java ไม่มีโค้ดเนทีฟ) NTLM เท่านั้น
- **อ่านอย่างเดียวในโค้ด:** เปิดไฟล์ด้วย GENERIC_READ + FILE_OPEN ไม่มี mkdir/rm/rename/write ฝั่ง NAS (test ตรวจ) ทำได้แค่เปิดดู ดูรายละเอียด และ "คัดลอกลงเครื่อง" ซึ่งใช้โหมดเลือกปลายทางเหมือนคัดลอก
- **ฟอร์มตั้งค่า:** ที่อยู่ (รับ "192.168.1.20", "nas.local:4445", "smb://nas/Media", "\\nas\Media"), ชื่อโฟลเดอร์ที่แชร์, ชื่อผู้ใช้ (ไม่บังคับ = ผู้เยี่ยมชม รับ "DOMAIN\user"), รหัสผ่าน ช่องสูง 52dp ข้อผิดพลาดของฟอร์มบอกทีละข้อใต้ช่องและไม่ล้างสิ่งที่กรอก
  - **รหัสผ่านเดิมไม่ถูกนำกลับขึ้นจอ แม้เป็นจุด** เว้นว่าง = ใช้รหัสเดิม
  - ลบการตั้งค่าต้องถามยืนยันในที่เดิม
  - ข้อความผิดพลาดของฟอร์มหายทันทีที่เริ่มพิมพ์แก้ (บน A07 เห็น "กรุณากรอกที่อยู่ NAS" ค้างอยู่ทั้งที่กรอกแล้ว)
- **เก็บอย่างปลอดภัย:** ทั้งชุด (ที่อยู่ ผู้ใช้ รหัสผ่าน) เข้ารหัส AES-256-GCM ด้วยกุญแจใน Android Keystore (`SecretBox` ตัวเดียวกับยืนยันตัวตน) เก็บใน `noBackupFilesDir` ไม่ถูกสำรองขึ้นคลาวด์
- **ห้าม log:** log เขียนได้แค่ชนิดปัญหา เช่น `problem=LOGON_FAILURE` ห้ามที่อยู่ ชื่อผู้ใช้ รหัสผ่าน หรือ message ของ exception (smbj ใส่ชื่อเครื่องไว้ในนั้น) `NasConfig.toString()` แสดงแค่ชื่อแชร์ smbj เองเขียน log ไม่ได้เพราะไม่มี SLF4J provider ในแอป
- **ต่อไม่ได้ต้องบอกสาเหตุที่คนอ่านเข้าใจ** (ข้อความสีแดงพร้อมปุ่ม [ลองใหม่][แก้ไขการตั้งค่า]): ไม่ได้ต่อเครือข่าย / แอปยังไม่ได้สิทธิ์เครือข่ายในบ้าน / ไม่พบเครื่องตามที่อยู่ / NAS ไม่ตอบ (ปิด คนละเครือข่าย ไฟร์วอลล์) / ชื่อผู้ใช้หรือรหัสผ่านผิด / บัญชีถูกปิดหรือล็อก / ไม่พบชื่อแชร์ / ไม่มีสิทธิ์ / ไม่พบไฟล์ / NAS ไม่ใช้ SMB2-3 / อื่น ๆ
- **เปิดการเชื่อมต่อใหม่ทุกครั้งที่ทำอะไรกับ NAS แล้วปิดทันที** (เปิดโฟลเดอร์หนึ่งครั้ง คัดลอกหนึ่งไฟล์) ห้ามเก็บการเชื่อมต่อค้างไว้ข้ามหน้า
  - บทเรียนจาก 0.44.0 บน A07: การเชื่อมต่อที่ค้างไว้ค้างที่คำขอที่สองจนครบ timeout 20 วินาทีของ smbj ส่วนการเชื่อมต่อใหม่ตอบใน 0.1 วินาที และ NAS ที่หลับก็ตัดการเชื่อมต่อที่ค้างไว้อยู่แล้ว
- **คัดลอกจาก NAS อ่านเท่าขนาดไฟล์พอดีด้วย offset ห้ามอ่าน "จนหมด"** การอ่านเลยท้ายไฟล์ทำให้ server ตอบเป็นข้อความ error และถ้า smbj อ่านข้อความนั้นไม่ออก (เจอกับ server ทดสอบ: ได้ครบทุกไบต์แล้วรอจน timeout) สำเนาที่เสร็จแล้วจะกลายเป็นล้มเหลว หาสาเหตุด้วยการรัน smbj บนคอมกับ server เดียวกัน (`Buffer$BufferException: Underflow` ใน `SMB2Error.readErrorContext`)

**สิทธิ์:** `MANAGE_EXTERNAL_STORAGE` ให้ผ่าน adb appops ตอน provision (TESTING.md) ส่วนสิทธิ์เครือข่ายในบ้าน (`NEARBY_WIFI_DEVICES` แบบ `neverForLocation` บน Android 16, `ACCESS_LOCAL_NETWORK` บน API 37+) แอปให้ตัวเองในฐานะ Device Owner เหมือนไมค์

**ขนาด APK:** smbj กับ BouncyCastle เพิ่มราว 3.5 MB (ตัดไฟล์ข้อมูล post-quantum ของ BouncyCastle ออกแล้ว) ห่างเพดาน 34 MB ของ CI ไม่มาก ไลบรารีใหม่รอบถัดไปต้องวัดก่อนเพิ่ม

## 5ง. อุปกรณ์ในบ้าน (eWeLink, 0.45.0 อ่าน · 0.46.0 สั่งด้วยเสียง · 0.47.0 หลอดไฟและหน้าตั้งค่า, Poom 2026-09-24)

**สถานะต้องตรงกับความจริง (0.53.3, Poom):** ไฟที่ถูกเปลี่ยนจากทางอื่น (ตารางเวลาในแอป eWeLink, สวิตช์ที่ผนัง) ต้องขึ้นบนจอภายใน ~2-3 นาที
- broker เก็บสถานะบ้านไว้ 120 วินาที (`HOME_TTL`, เดิม 600) มือถือขอทุก 60 วินาที รายชื่อบ้านและห้องเก็บไว้ 1 ชั่วโมง การอ่านหนึ่งรอบจึงเป็น 1 ครั้งต่อบ้าน ใช้โควตาอย่างมาก ~21,600 ครั้งต่อเดือน (จอเปิดตลอด) ต่ำกว่าจุดหยุดของ broker 40,000 จากโควตา 50,000
- **ตารางเวลาอ่านไม่ได้:** `ewelink-raw` บน production (2026-09-24) ทั้ง 3 อุปกรณ์ไม่มีตารางเวลาใน params ตารางเก็บที่ cloud ของ eWeLink Poom ให้ตัดเรื่องแสดงและแก้ตารางเวลาออก โค้ดที่เผื่อไว้ถูกลบแล้ว
- **สัญญาณ WiFi (0.53.4):** หน้าไฟในบ้านแสดงใต้ชนิดและห้องของแต่ละอุปกรณ์ "สัญญาณ WiFi: อ่อน (-74 dBm)" เกณฑ์ (broker `ewelink.signal_word`) จากตาราง Wi-Fi Signal Strength Basics ของ MetaGeek: ≥ -67 ดี · -68 ถึง -70 พอใช้ (-70 คือขั้นต่ำที่ส่งข้อมูลได้เชื่อถือได้) · -71 ถึง -80 อ่อน · ต่ำกว่า -80 อ่อนมาก ออฟไลน์อยู่ต่อท้าย "ค่าที่อ่านได้ครั้งล่าสุด"
- **สั่งไฟ แตะไฟ และถามสถานะไฟ อ่านสดก่อนเสมอ** (ไม่เก่ากว่า 20 วินาที `lights.FRESH_SECONDS`) ถามสถานะแล้วอ่านไม่ได้ จาร์วิสตอบ "ตอนนี้อ่านสถานะไฟไม่ได้ครับ" ไม่ตอบจากค่าเก่า

**หน้าที่:** ดูแวบเดียวว่าไฟดวงไหนเปิดอยู่ ในการ์ดสุดท้ายของกองการ์ด (เหนือจาร์วิส) **สั่งไฟด้วยเสียง** และ**ตั้งค่าในแผงควบคุม หน้า "ไฟในบ้าน"** (Poom: ส่วนใหญ่สั่งด้วยเสียง หน้านั้นเป็นที่ตั้งค่า ไม่ใช่ที่ใช้งานประจำ) การ์ดบนหน้าหลักจึง **ไม่มีปุ่ม**

**การ์ดบนหน้าหลัก (0.47.0 / 0.48.0, Poom 2026-09-24):**
- **ทุกดวงเป็นไอคอน pixel art ตามด้วยชื่อ แถวละ 3 ดวง** (Poom เลือกจากสองแบบ: ทุกดวง หรือเฉพาะดวงที่เปิด) อย่างมาก 9 ดวง ที่เหลือเป็น "และอีก N ดวง"
- ไอคอน 24dp (16×16 pixel) ชื่อขั้น 3 (14sp) บรรทัดเดียวตัดท้าย ชื่อของดวงที่เปิดตัวหนาสีดำ ดวงอื่นสีจาง
- **แตะเพื่อสั่ง (0.48.0):** ช่องละดวงสูง 48dp (เป้ากดขั้นต่ำ)
  - **ช่องที่สั่งได้เป็นปุ่มนูน** `retro_button` แตะแล้วสั่งสถานะตรงข้ามกับที่เห็น ผ่าน `/v1/home/switch` ซึ่งตรวจทุกด่านเหมือนเสียง: สวิตช์ฉุกเฉิน จำกัดความถี่ allowlist ห้ามอุปกรณ์ต้องห้าม และสถานะจริงตอนนั้น (ถ้าอยู่สถานะนั้นแล้วไม่สั่งซ้ำ)
  - **ระหว่างรอ** ชื่อเปลี่ยนเป็น "กำลังสั่ง…" ช่องอื่นกดไม่ได้ และหน้าต่างจาร์วิสขึ้น "กำลังสั่ง ชื่อ…" **ไอคอนไม่เปลี่ยนจนกว่า broker จะตอบ** และเปลี่ยนตามผลจริงเท่านั้น
  - **ผล** ขึ้นในหน้าต่างจาร์วิส ("เปิดไฟหน้าบ้านแล้วครับ", "Light2 ออฟไลน์อยู่ครับ…") ไม่ใช่ในการ์ด การ์ดจึงไม่สูงขึ้น
  - **ช่องที่สั่งไม่ได้แบน** (ออฟไลน์ ไม่อยู่ใน allowlist สวิตช์ฉุกเฉินปิดอยู่) แตะแล้วหน้าต่างจาร์วิสบอกเหตุผล เช่น "Light2 ออฟไลน์อยู่ครับ จึงสั่งไม่ได้" หรือ "… ยังไม่ได้รับอนุญาตให้สั่งครับ ตั้งได้ที่แผงควบคุม › ไฟในบ้าน"
- **ไม่มีคำบอกสถานะ ไม่มีบรรทัด "จาก eWeLink"** บรรทัดจางใต้หลอดขึ้นเฉพาะเมื่อมีดวงที่ไม่พอที่ หรือข้อมูลเก่า ("ข้อมูลเมื่อ N นาทีก่อน")
- สรุปตอนหุบ: "เปิดอยู่ 2 จาก 5"
- **เหตุผล (ภาพจริงบน A07):**
  - 0.46.0 ไทล์ 6 ดวงสูง 497 px ดันจาร์วิสเหลือราว 168dp
  - 0.45.0 แถวคำ 4 ดวงสูง 203 px
  - ค่าของ 0.47.0 อยู่ในรายงานรอบนั้น

**กติกาไอคอนสถานะ (Poom 2026-09-24, 0.48.0 ขยายเป็น 5 แบบ):** วาดเองจากสี่เหลี่ยม ไม่ลอกไอคอนของแบรนด์ใด **สามสถานะต่างกันที่รูปร่าง ไม่ใช่สีอย่างเดียว** (ข้อ 8) กติกาเดียวกันทุกไอคอน:
- **เปิด:** ส่วนที่ทำงานเป็นสีเหลือง `#FFD700` **และมีเครื่องหมายเพิ่มนอกตัววัตถุ** (รัศมี ลม เสาอากาศ)
- **ปิด:** สีเทา ไม่มีเครื่องหมายเพิ่ม
- **ออฟไลน์ / ไม่ทราบสถานะ:** กลวง ไม่มีพื้น ขอบสีเทา และมีกากบาทสีดำแบบเดียวกันทุกไอคอน

| ไอคอน | ไฟล์ `ic_pixel_<ชื่อ>_<on\|off\|offline>` | เปิด | ปิด |
|---|---|---|---|
| หลอดไฟ | `bulb` | แก้วเหลือง มีรัศมี 7 จุด | แก้วเทา |
| พัดลม | `fan` | ใบพัดเหลือง มีจุดลมที่มุม | ใบพัดเทา |
| แอร์ | `aircon` | ไฟแสดงเหลือง มีลมเย็นเป็นเส้นเหลืองใต้เครื่อง | ไฟแสดงเทา ไม่มีลม |
| ทีวี | `tv` | จอเหลืองมีภาพสีขาว มีเสาอากาศ | จอเทา ไม่มีเสาอากาศ |
| สวิตช์ | `switch` | ปุ่มโยกขึ้นสีเหลือง มีรัศมีข้างแผง | ปุ่มโยกลงสีขาว |

- 4 ไอคอนใหม่สร้างจากรูปทรงด้วยสคริปต์ที่ต่อจุดเป็นสี่เหลี่ยม `HomeCardTest` ตรวจว่าทุกไอคอนมีครบ 3 สถานะ เหลืองมีเฉพาะตอนเปิด ตอนเปิดมีส่วนเพิ่มมากกว่าตอนปิด และตอนออฟไลน์มีกากบาท
- **เลือกไอคอนได้ทีละดวง** ที่แผงควบคุม › ไฟในบ้าน (ใต้ชื่อของแต่ละดวง: ปุ่ม 5 ปุ่ม ขนาด 52×48dp ปุ่มที่เลือกอยู่เป็นพื้นกรมท่า) เก็บบน VPS (`ewelink_icons.json`)
- **ยังไม่ได้เลือก → ตามประเภท (Poom):** ช่องของสวิตช์ไฟใช้ไอคอนสวิตช์ ปลั๊กและไฟใช้หลอดไฟ

ไอคอนหลอดไฟ (0.47.0):

| สถานะ | ไฟล์ | รูป |
|---|---|---|
| เปิด | `ic_pixel_bulb_on` | แก้วสีเหลือง `#FFD700` (สีเดียวกับดวงอาทิตย์) มี**รัศมี** 7 จุดรอบหลอด ขอบดำ |
| ปิด | `ic_pixel_bulb_off` | แก้วสีเทา `#808080` ขอบดำ ไม่มีรัศมี |
| ออฟไลน์ / ไม่ทราบสถานะ | `ic_pixel_bulb_offline` | **หลอดกลวง** ขอบสีเทาแทนสีดำ และ**มีกากบาทสีดำ**ทับ |

- **ออฟไลน์ห้ามดูเหมือนปิด:** ไม่รู้สถานะคือไม่รู้ การวาดเป็นหลอดเทาคือการบอกว่าปิด ซึ่งอาจไม่จริง อุปกรณ์ออนไลน์ที่ไม่ส่งสถานะก็ใช้รูปเดียวกัน
- เครื่องอ่านหน้าจอได้ยินชื่อพร้อมสถานะเป็นคำ ("Light2 ออฟไลน์") จาก `HomeCard.spoken`
- `HomeCardTest` ตรวจว่าเหลืองมีเฉพาะรูปเปิด รัศมีมีเฉพาะรูปเปิด และรูปออฟไลน์กลวงและมีกากบาท

**หน้า "ไฟในบ้าน" ในแผงควบคุม (0.47.0):** ที่ตั้งชื่อ อนุญาตหรือหยุดการสั่งทีละดวง และดูสถานะ **ไม่มีปุ่มเปิดปิดไฟ** เพราะการใช้งานคือเสียง
- กล่องละอุปกรณ์ (`retro_sunken`): บรรทัดจาง "ปลั๊ก · ห้องนอน · ออฟไลน์" / "สวิตช์ 3 ช่อง · Livingroom · ออนไลน์"
- **ต่อหนึ่งดวง** (ปลั๊กหรือไฟ หรือแต่ละช่องของสวิตช์) **แถวเดียว** (0.54.1, Poom อนุมัติ 2026-09-24; เดิมมี 3 แถว):
  - ซ้าย: ชื่อ (`TEXT_ITEM`) และบรรทัดจางใต้ชื่อ "ช่อง 1 · เปิดอยู่ · ชื่อที่ตั้งเอง" โดย "เปิดอยู่" เป็นตัวหนาสีเข้ม
  - ขวา: ช่องติ๊ก **"อนุญาตให้สั่ง"** และปุ่ม **"ตั้งชื่อ"** สูง 48dp (`TOUCH`) กว้างตามคำ
  - ใต้แถว: คำเตือนสีแดงถ้ามี ("ยังไม่ใช้งาน…", ชื่อซ้ำ) แล้วตามด้วยไอคอนบนหน้าหลัก
- **"ออฟไลน์" เขียนที่เดียว** คือบรรทัดของอุปกรณ์ ("ปลั๊ก · ห้องนอน · ออฟไลน์") ส่วนแต่ละดวงไม่ซ้ำคำนี้ และบรรทัดสัญญาณ WiFi ของอุปกรณ์ที่ออฟไลน์ต่อท้ายว่า "ค่าที่อ่านได้ครั้งล่าสุด" (`CompactRowsTest`)
- **สวิตช์หลายช่อง:** ชื่อของสวิตช์เอง (ใช้สั่งทุกช่องพร้อมกันด้วยเสียง) อยู่แถวเดียวกับปุ่ม "เปลี่ยนชื่อสวิตช์" แล้วตามด้วยทีละช่อง
- **หน้าตั้งชื่อ:**
  - ช่องกรอกพื้นขาว สูง 48dp ยาวได้ไม่เกิน 40 ตัว
  - บอกชื่อจาก eWeLink หรือ "eWeLink ไม่ได้ส่งชื่อมา"
  - บรรทัดช่วย "จาร์วิสจะฟังชื่อนี้…"
  - ปุ่ม [บันทึก][ยกเลิก] 56dp และ "ลบชื่อที่ตั้งเอง" (ย้อนได้ จึงไม่ถามซ้ำ)
  - Back ถอยกลับไปรายการ
- **บันทึกแล้วแสดงเฉพาะตามที่ VPS ตอบ:** ข้อความของ broker ขึ้นใต้หัวรายการ ("บันทึกชื่อแล้วครับ สั่งด้วยเสียงได้ทันที") ถ้าผิดพลาด ข้อความสีแดงขึ้นในหน้าตั้งชื่อ และสิ่งที่พิมพ์ไว้ไม่หาย
- **ชื่อซ้ำ:** broker ไม่รับชื่อที่จาร์วิสแยกไม่ออกจากชื่ออื่น ("ไฟหน้าบ้าน" กับ "หน้าบ้าน" นับว่าซ้ำ) และชื่อที่ซ้ำอยู่แล้วจากแอป eWeLink ขึ้นเป็นคำเตือนสีแดงใต้ชื่อ "ชื่อซ้ำกับ … จาร์วิสจะแยกไม่ออก กรุณาเปลี่ยนชื่อ"
- **ช่องที่ยังไม่ใช้งาน** (ข้อถัดไป): ขึ้นในหน้านี้เพื่อให้ตั้งชื่อได้ พร้อมบรรทัดสีแดง "ยังไม่ใช้งาน กรุณาตั้งชื่อก่อนจึงจะสั่งได้" ช่องติ๊กจางและกดไม่ได้จนกว่าจะตั้งชื่อ
- สวิตช์ฉุกเฉิน `ewelink-control` **อยู่บน VPS เท่านั้น** มือถือที่หายจึงยกเลิกการหยุดไม่ได้ ถ้าหยุดอยู่ หน้านี้บอกเป็นบรรทัดสีแดง

**จำนวนช่องจริงของสวิตช์ (Poom 2026-09-24):**
- **ปัญหา:** Switch1 ในแอป eWeLink มี 3 ช่อง แต่ API ส่ง `switches` มา 4 ตัว และสั่งช่องที่ 4 แล้วไม่มีผลกับอะไร
- **จำนวนช่องมาจาก uiid ตาม UIIDProtocol.md** ("Dual-/Three-/Four-channel Plug" คือ 2/3/4 และ "Dual-/Three-/Four-channel Switch" คือ 7/8/9) broker ตัดส่วนเกินทิ้งตั้งแต่ตอนอ่าน (`ewelink.CHANNELS_BY_UIID`) ช่องที่ไม่มีจริงจึงไม่ขึ้นในรายการ ไม่ขึ้นบนการ์ด และสั่งด้วยเสียงไม่ได้
- **uiid ที่เอกสารไม่บอกจำนวนช่อง:** ช่องที่ยังไม่มีชื่อ (ทั้งชื่อของเราและชื่อจาก eWeLink) ถือว่ายังไม่ใช้งาน ไม่ขึ้นบนการ์ด และสั่งไม่ได้ ต้องตั้งชื่อก่อน

**ข้อมูลมาจาก broker เท่านั้น:** มือถือได้ชื่อ ห้อง ประเภท ออนไลน์ เปิด/ปิด และสิทธิ์ ผ่าน `/v1/dashboard` กับ `/v1/home/devices` **ไม่มี token ไม่มี id อุปกรณ์** อุปกรณ์รู้จักกันด้วย key ทึบที่ broker ทำจาก id ด้วยความลับของ broker และเลขช่อง ชื่อและสิทธิ์เก็บบน VPS (`HomeCardTest`, `HomeSettingsTest` ตรวจ)
- แสดงและสั่งได้เฉพาะไฟ สวิตช์ไฟ และปลั๊ก (Poom 2026-09-24 อนุมัติปลั๊ก Light1 และ Light2) บนการ์ดหน้าหลักขึ้นเฉพาะปลั๊กที่อยู่ใน allowlist
- กล้อง ประตู กุญแจ โรงรถ รั้ว ม่าน สัญญาณเตือน **ไม่ขึ้นที่ไหนและสั่งไม่ได้เสมอ**

**สั่งด้วยเสียง:**
- broker ตัดสินใจในโค้ด ไม่ใช่โมเดล (`lights.py`) และตอบจากผลจริงของ eWeLink
- **เปิด/ปิด ต่างกันแค่สระเดียว (0.47.1, บั๊กจริงบน production 2026-09-24):** Poom พูด "เปิดไฟหน้าบ้าน" สองครั้ง ตัวถอดเสียงเขียนเป็น "ปิดหน้าบ้าน" (11 ตัวอักษร ตาม `chars=11` ใน log) และไฟถูกสั่งปิดทั้งสองครั้ง
  - คำที่ได้ยินผิดสลับกันจะเป็นคำขอให้ไฟอยู่ในสถานะที่เป็นอยู่แล้วเสมอ เพราะคนสั่งเพื่อให้สถานะเปลี่ยน
  - **ก่อนสั่ง broker อ่านสถานะใหม่ (ไม่เกิน 20 วินาที)** ถ้าไฟทุกดวงที่สั่งอยู่ในสถานะนั้นแล้ว **ไม่สั่งซ้ำเงียบ ๆ แต่ถามกลับ** เช่น "ไฟหน้าบ้านปิดอยู่แล้วครับ ต้องการเปิดไฟหน้าบ้านใช่ไหมครับ"
    - ตอบ "ใช่" / "ครับ" / "ได้" → สั่งสถานะตรงข้าม (สิ่งที่ตั้งใจ)
    - ตอบ "ไม่" / "ยกเลิก" → ไม่ทำอะไร
    - สั่งใหม่ให้ชัด → ทำตามคำสั่งใหม่
  - "ทั้งหมด" ที่บางดวงอยู่ในสถานะนั้นแล้ว: สั่งเฉพาะดวงที่ต้องเปลี่ยน และบอกว่า "ส่วน … เปิดอยู่แล้ว"
  - **ภายใน 30 วินาทีหลังสั่ง พูด "ไม่ใช่" / "ผิด" / "ย้อนกลับ" = ย้อนคำสั่งนั้น** ("ขอโทษครับ เปิดไฟหน้าบ้านแล้วครับ") ต้องเป็นคำตอบสั้น ๆ ประโยคยาวที่บังเอิญมีคำว่า "ไม่ใช่" ไม่นับ
  - คำตอบขึ้นต้นด้วยคำกริยาเสมอ ("ปิดไฟหน้าบ้านแล้วครับ") ให้คนได้ยินและทักท้วงทัน
  - อ่านสถานะใหม่ไม่ได้ → ไม่เดาสถานะ และสั่งตามที่ได้ยิน (ยังย้อนด้วย "ไม่ใช่" ได้)
  - **ที่เจอจากการทดสอบบนเส้นทางจริง (0.47.2, A07 2026-09-24):**
    - **"ไฟ" คำเดียวไม่ใช่ชื่อ:** ไฟดวงที่ชื่อ "ไฟ" ทำให้ "เปิดไฟ…" ที่ถอดเสียงเพี้ยน ("เปิดไฟ แบนยุมไฟ") ไปสั่งดวงนั้น คำกว้าง ๆ (ไฟ หลอด หลอดไฟ โคม) ไม่นับเป็นชื่อ และหน้า "ไฟในบ้าน" ไม่รับชื่อแบบนี้
    - **คำกริยาซ้ำ 3 ครั้งขึ้นไป** ("เปิดไฟ เปิดไฟ เปิดไฟ เปิดไฟ" ตอนพูด "ปิดไฟ") คือตัวถอดเสียงพูดคำใบ้ซ้ำ ไม่ใช่คน → ตอบ "ผมได้ยินไม่ชัดครับ ยังไม่ได้สั่งไฟ" และเอา "เปิดไฟ"/"ปิดไฟ" ออกจากคำใบ้
    - **คำตอบสั้นต้องผ่านตัวกรองเสียง:** "ใช่ครับ" ถูกทิ้งเพราะ Groq ไม่มั่นใจ ตอนที่จาร์วิสรอคำตอบเรื่องไฟ คำตอบสั้นไม่เกิน 24 ตัวผ่านได้ (`speech_gate` "answer")
    - **ย้อนคำสั่งได้ภายใน 60 วินาที** (30 วินาทีไม่พอ เพราะคำตอบของจาร์วิสใช้เวลาหลายวินาที)
- คำกำกวม → ถามกลับ ไม่เดา
- คำตอบของโมเดลที่อ้างว่าเปิดหรือปิดไฟแล้วถูกแทนด้วย "ผมยังไม่ได้สั่งไฟครับ…" เสมอ
- หลังสั่งสำเร็จ broker ส่ง `home_updated` มือถือจะถามการ์ดใหม่ทันที

**สถานะของการ์ด:**
- ยังไม่เชื่อมบัญชี / ไม่มีอุปกรณ์ / broker รุ่นเก่า → ซ่อนทั้งการ์ด (GONE)
- ดึงไม่ได้แต่มีค่าเก่า → แสดงค่าเก่าพร้อมอายุ
- ดึงไม่ได้และไม่มีค่าเก่า → ซ่อน

**ความถี่:**
- broker ถาม eWeLink ใหม่ทุก 10 นาที ถ้าล้ม รอ 5 นาทีก่อนถามใหม่
- ผลการสั่งที่ยืนยันแล้วเขียนทับสถานะในแคชทันที
- สั่งไฟจำกัด 10 ครั้งต่อนาที หน้าตั้งค่าจำกัด 30 คำขอต่อนาที

**debug:** `TEST_HOME_CARD` ใส่ข้อมูลตัวอย่างแบบบ้านของ Poom (เปิด ปิด และออฟไลน์) เพื่อดูการ์ดบน A07 (TESTING.md)

## 5จ. เครื่องคิดเลขวิศวกรรม (0.52.0, Poom 2026-09-24)

**หน้าที่:** คำนวณด้วยการกดปุ่ม (Poom: ไม่มีคำสั่งเสียง) และเครื่องมือไฟฟ้า เป็นหน้าจอของแอปนี้ `calc/CalculatorActivity` เข้าจากไอคอน `ic_pixel_calculator` ในแผงควบคุม ไม่ขอสิทธิ์ ไม่มีไลบรารีเพิ่ม ไม่แตะไมค์หรือลำโพง ไม่เปิดอะไรนอกแอป (`CalculatorScreenTest`)

**หน้าต่าง:** แบบเดียวกับแผงควบคุม (แถบหัวข้อกรมท่า ปุ่ม X 44×40dp ปุ่ม "กลับหน้าหลัก" 56dp ล่างสุด) ใต้แถบหัวข้อเป็นแท็บ 4 อัน สูง 48dp: "คำนวณ" · "ไฟฟ้า" · "โซลาร์" (0.62.0) · "ประวัติ" แท็บที่เลือกอยู่เป็นพื้นกรมท่าตัวขาว Back: → แผงควบคุม (หน้าไฟฟ้าและโซลาร์ไม่มีรายการให้ถอยกลับ) Hey Jarvis ปิดหน้านี้ตามข้อ 11

**หน้าคำนวณ:**
- **จอแสดงผลเป็นตัวเลขและสัญลักษณ์ล้วน จึงใช้ Press Start 2P ทั้งบรรทัด** (ข้อยกเว้นแบบเดียวกับนาฬิกา taskbar ข้อ 3): โจทย์ 13sp ชิดขวา ตัดด้านหน้าถ้ายาว ผลลัพธ์ 20sp
  - ผลลัพธ์ไม่เกิน 16 ตัวอักษร (`CalcEngine.MAX_CHARS`) ถึงไม่ต้องลดขนาดตัวอักษร: 10 หลักนัยสำคัญ เลขชี้กำลังยาวจะตัดหลักของตัวเลขลงแทน
  - ค่าตั้งแต่ 1E10 ขึ้นไปหรือต่ำกว่า 1E-6 แสดงเป็นเลขยกกำลังสิบแบบ "1.5E-7" ปุ่ม EXP ใส่ "E"
  - บรรทัดบนสุด 12sp สีจาง (ภาษาไทย): "มุม: องศา" หรือ "มุม: เรเดียน" และ "หน่วยความจำ: …" เมื่อมีค่า
  - ข้อผิดพลาดเป็นคำไทย 15sp ตัวหนาสี `retro_bad` แทนผลลัพธ์ ("หารด้วยศูนย์ไม่ได้", "ค่านี้อยู่นอกขอบเขตของฟังก์ชัน", "ผลลัพธ์ใหญ่เกินกว่าจะแสดงได้", "โจทย์ยังไม่ครบ กรุณาตรวจอีกครั้ง") ไม่แสดง NaN หรือ Infinity
- **แป้น 5 คอลัมน์ 9 แถว** แบ่งความสูงที่เหลือเท่ากัน ป้ายปุ่มเป็น Press Start 2P ตัวเลข 16sp ฟังก์ชัน 12sp (ป้าย 4 ตัวอักษรขึ้นไป 10sp)
  - สีตัวอักษรตามเครื่องคิดเลข 1995: เครื่องหมายและหน่วยความจำสีกรมท่า ล้างสีแดง ความหมายอยู่ที่ป้ายเสมอ และทุกปุ่มมี `contentDescription` ภาษาไทย
  - **"=" คือการกระทำหลัก:** กว้าง 3 ช่อง พื้นกรมท่า ตัวขาว 18sp อยู่มุมขวาล่างใต้นิ้วโป้ง
  - Press Start 2P ไม่มีเครื่องหมายลบยาว (−) และตัวยก ʸ จึงใช้ "-" และ "x^y" (ตรวจ cmap ของฟอนต์แล้ว)
  - Press Start 2P วาด × และ ÷ สูงแค่ครึ่งหนึ่งของตัวเลข (เห็นบน A07) สองปุ่มนี้จึงใช้ Plex ตัวหนา 24sp
- องศาเป็นค่าเริ่มต้น sin/cos/tan ที่มุมพิเศษได้ค่าตรง (sin 180° = 0 ไม่ใช่ 1.2E-16) tan 90° เป็นข้อผิดพลาด
- หน่วยความจำ มุม และประวัติ 30 รายการล่าสุด อยู่รอดหลังปิดเปิดแอป (SharedPreferences `calculator`)

**หน้าประวัติ:** ใหม่สุดอยู่บน แถวละปุ่มนูนสูงอย่างน้อย 56dp โจทย์ 11sp สีจาง ผลลัพธ์ 15sp ชิดขวา แตะแล้วผลลัพธ์กลับไปอยู่ในหน้าคำนวณ "ล้างประวัติ" ถามยืนยันในที่เดิมก่อน ตอนว่างบอกว่าประวัติมาจากปุ่ม =

**หน้าไฟฟ้า (0.61.0, Poom: ปัดเปลี่ยนหน้าเหมือนหน้าโซลาร์):** 5 เครื่องมือเป็นสไลด์ (กฎของ V = IR, อนุกรม/ขนาน, รหัสสี, ไฟ AC, สายไฟ) ปัดซ้ายขวาเปลี่ยนหน้า ■ □ บอกหน้าในแถบหัวข้อ **ใช้ component เดียวกับหน้าโซลาร์ (`calc/SlideDeck`) ไม่มีโค้ดปัดชุดที่สอง** ไม่มีรายการปุ่มเลือกเครื่องมือและปุ่ม "◄ ไฟฟ้า" อีกแล้ว แต่ละสไลด์ยังเลื่อนขึ้นลงได้ สูตรและผลลัพธ์เดิมไม่เปลี่ยน
- **ทุกช่องมีหน่วยกำกับ:** ช่องกรอกสูง 52dp ตามด้วยปุ่มหน่วยกว้าง 52dp (หน่วยที่ใช้อยู่เป็นพื้นกรมท่า) ผลลัพธ์ทุกบรรทัดมีหน่วยและคำนำหน้า SI ("4.7 kΩ")
- **ตรวจค่าที่ใส่:** ค่าที่เป็นไปไม่ได้ (ติดลบ ศูนย์ในที่ที่ห้าม PF เกิน 1) ขึ้น "ผิดพลาด: …" สีแดงและไม่คำนวณ ค่าที่แปลกแต่เป็นไปได้ (แรงดันเกิน 100 kV ฯลฯ) คำนวณให้พร้อม "โปรดตรวจ: …" ข้อความหายทันทีที่แก้ช่องใดช่องหนึ่ง
- ปุ่ม [คำนวณ] 56dp กว้างสองส่วน คู่กับ [ล้างค่า]
- **รหัสสีตัวต้านทาน (IEC 60062:2016):** สีทุกสีมีชื่อไทยกำกับเสมอ ไม่สื่อด้วยสีอย่างเดียว ตัวเลือกสีแสดงเฉพาะสีที่ใช้ได้ในแถบนั้น สีขาวและเงินมีขอบลึกเพื่อให้เห็นบนพื้นเทา ค่าที่ต้องใช้หลักมากกว่าจำนวนแถบบอกว่า "ปัดเป็น …"
- **ขนาดสายไฟ:** คิดจากแรงดันตกเท่านั้น ด้วยความต้านทานสายทองแดง IEC 60228 class 2 ที่ 70°C หน้าจอบอกแหล่งอ้างอิง และบอกเป็นสีแดงว่า**ยังไม่ได้ตรวจพิกัดกระแส** ต้องตรวจกับตารางของ วสท. เพราะหาตารางพิกัดกระแสฉบับที่ตรวจได้ไม่เจอ (2026-09-24) จึงไม่ใส่ตัวเลขจากความจำ

**หน้าโซลาร์เซลล์ (0.62.0, Poom 2026-09-25: "ใช้แบบสไลด์ตามกฎเดิม ไม่ใช่แท็บปุ่ม"):** แท็บ "โซลาร์" ในแถวแท็บเดิม (ไม่เพิ่มความสูง) ข้างในเป็นเครื่องมือทีละสไลด์ `calc/SolarPages` สูตรและการตรวจค่าทั้งหมดอยู่ใน `calc/Solar` (Kotlin ล้วน, `SolarTest`) คำทั้งหมดอยู่ใน `strings_solar.xml`
- **สไลด์ตามกฎการ์ดหลายหน้า (ข้อ 5):** ปัดซ้ายขวาบนเนื้อหาเพื่อเปลี่ยนสไลด์ นับเป็นการปัดเมื่อเลื่อนเกิน 2 เท่าของ touch slop และแนวนอนมากกว่าแนวตั้ง 1.5 เท่า ไม่มีแถวแท็บหรือปุ่มเลือกหน้า มีแต่สี่เหลี่ยม ■ □ ขนาด `PagedPanel.SQUARE_DP` ห่าง 4dp บนแถบหัวข้อของหน้าต่าง ระหว่างชื่อหน้าต่างกับปุ่ม X แสดงเฉพาะตอนอยู่แท็บโซลาร์ แตะสี่เหลี่ยมเพื่อไปสไลด์ถัดไป
  - `PagedPanel` สร้างหน้าจาก XML เท่านั้น หน้านี้สร้างในโค้ด จึงเขียนกฎเดียวกันซ้ำใน `SolarPages.SlideFrame` ไม่ใช่กฎใหม่
  - ทุกสไลด์บอก "หน้า n จาก m · ปัดซ้ายหรือขวาเพื่อเปลี่ยนหน้า" ใต้ชื่อ เพราะการปัดมองไม่เห็น
  - ค่าที่กรอกไว้อยู่ครบเมื่อเปลี่ยนสไลด์ หรือเมื่อกดตัวเลือกบนสไลด์
- **ออนกริด/ออฟกริด** เลือกที่สไลด์แรก ค่าเริ่มต้นออนกริด: ขนาดแผง · อินเวอร์เตอร์ · การต่อสตริง · สายไฟ · คืนทุน (5 สไลด์) ออฟกริดเพิ่ม แบตเตอรี่ และ ชาร์จคอนโทรลเลอร์ (7 สไลด์) สี่เหลี่ยมเปลี่ยนตาม
- **สไลด์เรียงตามลำดับงาน:** ช่องกรอก (ป้ายมีหน่วยในวงเล็บ ช่องสูง 48dp) → [คำนวณ] 56dp กว้างสองส่วน คู่กับ [ล้างค่า] (กลับเป็นค่าตั้งต้น) → กล่องผล → "ผลเป็นค่าประมาณ การติดตั้งจริงควรให้ช่างไฟฟ้าตรวจ" ตัวหนาท้ายทุกสไลด์
- **กล่องผล:** บรรทัดคำตอบ 16sp ตัวหนา ตามด้วยบรรทัดประกอบ 15sp แล้ว "สูตร: …" 13sp สีจาง แล้ว "โปรดตรวจ: …" สีแดง แล้วหมายเหตุค่าตั้งต้น สีจาง ถ้าคำตอบคือ "ไม่มีขนาด/จำนวนที่ใช้ได้" บรรทัดคำตอบเป็นสีแดงและบอกเป็นคำ
- **สถานะ:** ว่าง = ยังไม่มีกล่องผล มีคำอธิบายใต้ชื่อสไลด์ / ข้อมูลไม่ครบหรือผิด = "ผิดพลาด: …" สีแดง บอกชื่อช่องและเหตุผล เช่น "ชั่วโมงแดด ต้องมากกว่า 0", "กรุณากรอกไฟที่ใช้ต่อวัน", "Vmp ต้องน้อยกว่า Voc" / ผล = กล่องผล ข้อความหายทันทีที่แก้ช่องใดช่องหนึ่ง ไม่แสดง NaN หรือ Infinity (ทุกช่องมีช่วงที่ยอมรับใน `Solar.RANGES` ซึ่งกันผลล้นไว้ และตรวจผลว่าเป็นตัวเลขจำกัดก่อนแสดง)
- **ตัวเลข:** มีจุลภาคคั่นหลักพัน ตัดศูนย์ท้าย ("2,777.78") พิมพ์ "150,000" ได้ค่าแสนห้าหมื่น ไม่ใช่ 150 จุลภาคเดี่ยวที่ไม่ใช่หลักพัน ("4,5") เป็นจุดทศนิยม
- **สูตรและค่าตั้งต้น (แก้ได้ทุกค่า รอ Poom ยืนยัน):**
  - ขนาดแผง: W = kWh/วัน × 1000 ÷ (ชั่วโมงแดด × (1 − สูญเสีย)) จำนวนแผงปัดขึ้น ค่าตั้งต้น แดด 4.5 ชม. สูญเสีย 20% แผง 550 W
  - แบตเตอรี่: Wh = kWh/วัน × 1000 × วัน ÷ (DoD × ประสิทธิภาพ) Ah = Wh ÷ V ค่าตั้งต้น ลิเธียม DoD 90% ตะกั่วกรด 50% ประสิทธิภาพอินเวอร์เตอร์และแบตเตอรี่รวม 90% 1 วัน ระบบ 12/24/48 V (ค่าตั้งต้น 48)
  - อินเวอร์เตอร์: ต่อเนื่อง = ขณะทำงาน × 1.25 กระชาก = ขณะสตาร์ท × 1.25 ไม่กรอกขณะสตาร์ทใช้ค่าขณะทำงานพร้อมคำเตือนเรื่องมอเตอร์
  - ชาร์จคอนโทรลเลอร์: MPPT I = W แผง ÷ V แบตเตอรี่ × 1.25 / PWM I = Isc × สตริงขนาน × 1.25 ผลบอกชื่อชนิดเสมอ
  - การต่อสตริง: Voc เย็น = Voc × (1 + k_Voc × (T ต่ำสุด − 25)) สูงสุด = V สูงสุด ÷ Voc เย็น ปัดลง / Vmp ร้อน = Vmp × (1 + k_Vmp × (T แผง − 25)) ต่ำสุด = V MPPT ต่ำสุด ÷ Vmp ร้อน ปัดขึ้น ค่าตั้งต้น k_Voc −0.28 k_Vmp −0.35 %/°C ต่ำสุด 10°C แผงร้อนสุด 70°C
    - แยกสัมประสิทธิ์ของ Vmp (หรือ Pmax) จากของ Voc เพราะ Vmp ลดเร็วกว่า ใช้ค่าของ Voc จะได้จำนวนแผงขั้นต่ำน้อยเกินจริง
  - สายไฟ (ฝั่งไฟตรง): มม.² = 2 × L × I × 0.0175 ÷ (V × %ตก) ปัดขึ้นเป็นขนาดมาตรฐาน 1.5–120 บอก %ตกของขนาดที่เลือก ถ้า 120 ยังไม่พอ บอกเป็นคำพร้อม %ตกของ 120 และทางแก้ ทุกผลมีคำเตือนพิกัดกระแสของ วสท. แบบเดียวกับหน้าไฟฟ้า
  - คืนทุน: ต่อปี = kWh/วัน × 365 × ค่าไฟ ต่อเดือน = ต่อปี ÷ 12 ปี = ค่าติดตั้ง ÷ ต่อปี ค่าไฟตั้งต้น 4.2 บาท/หน่วย ไม่รวมการเสื่อมของแผงและค่าบำรุงรักษา เกิน 25 ปีมีคำเตือน

**แท็บ "หน่วย" (0.61.0, Poom)** (`calc/Units`, `calc/UnitsPages`) 8 สไลด์ของ `SlideDeck`: ความยาว พื้นที่ น้ำหนัก ปริมาตร อุณหภูมิ ความเร็ว พลังงาน กำลัง กรอกค่า เลือกหน่วยของค่านั้น (ปุ่มกดค้าง 3 ต่อแถว) แล้ว**เห็นค่าในทุกหน่วยของหมวดพร้อมกัน** ไม่ต้องเลือกปลายทาง หน่วยไทยตามมาตราชั่งตวงวัด (วา ศอก คืบ เส้น / ไร่ งาน ตารางวา / ชั่ง ตำลึง บาท-น้ำหนัก 15 g ไม่ใช่บาททอง / ถัง 20 ลิตร) ค่าติดลบ (ยกเว้นความเร็ว) และอุณหภูมิต่ำกว่าศูนย์สัมบูรณ์บอกเป็นคำ ไม่มีผล ค่าที่กรอกและหน่วยจำแยกตามหมวด แท็บเครื่องคิดเลขเป็น 6 อัน: คำนวณ · ไฟฟ้า · โซลาร์ · หน่วย · ราคา · ประวัติ

**แท็บ "ราคา" (0.61.0, Poom): แปลงค่าเงิน + โลหะและแร่** (`calc/Money`, `calc/RateStore`, `calc/RatesPages`) สองสไลด์ของ `SlideDeck` ตัวเดียวกับหน้าโซลาร์และไฟฟ้า
- **แหล่งข้อมูล (ฟรี ไม่ต้องสมัคร ไม่มี key ต่อตรงจากมือถือ ไม่ผ่าน broker):** 1) exchange-api ของ fawazahmed0 ผ่าน jsDelivr 2) ข้อมูลเดียวกันที่ currency-api.pages.dev (ลิงก์สำรองตาม README) 3) Frankfurter (อัตราอ้างอิงธนาคารกลางยุโรป ~30 สกุล ไม่มีโลหะ) ลองตามลำดับ ใช้ตัวแรกที่ตอบถูก ดึงวันละครั้ง (เปิดแท็บเมื่อข้อมูลเก่าเกิน 24 ชั่วโมง) หรือกด "อัปเดตราคาตอนนี้"
- **ใต้ตัวเลขเสมอ:** ชื่อแหล่ง วันที่ของข้อมูลตามแหล่ง และเวลาที่ดึง พร้อมอายุ ("3 ชั่วโมงที่แล้ว") ออฟไลน์ใช้ค่าที่เก็บไว้ใน `files/rates.json` พร้อมบอกว่าเป็นข้อมูลเก่า ไม่มีข้อมูลเลย = ไม่แสดงตัวเลขใดๆ และบอกว่าทำไม **ห้ามตัวเลขเดา**
- **แปลงค่าเงิน:** แปลงผ่านอัตราต่อ 1 ดอลลาร์ของทั้งสองสกุล (สกุลไหนเป็นฐานก็ได้) ปุ่มสกุลแสดง **ธงชาติเป็นอีโมจิ (ข้อยกเว้นของข้อ 8 ตามที่ Poom ขอ)** + ชื่อประเทศไทย + ชื่อสกุลไทย + รหัส ชื่อมาจากข้อมูล locale ของระบบ ไม่พิมพ์รายการเอง ค้นหาได้ทั้งไทย อังกฤษ รหัส ☆/★ ปักหมุดไว้บนสุด (ค่าเริ่มต้น THB USD) ค่าเริ่มต้น THB → USD (Poom) ปุ่ม "⇅ สลับ"
- **โลหะและแร่ 10 อันดับ ทองคำก่อน (Poom):** 1 ทองคำ 2 ทองแดง 3 เงิน 4 แพลทินัม 5 แพลเลเดียม — ตามมูลค่าตลาดจาก assetmarketcap.com หมวด commodities (อ่าน 2026-09-25) · 6 แร่เหล็ก 7 อะลูมิเนียม 8 นิกเกิล 9 สังกะสี 10 ลิเทียม — ตามมูลค่าการผลิตต่อปี (Statista 2023: แร่เหล็กมีมูลค่าตลาดโลหะสูงสุดปี 2022; ปริมาณการผลิต USGS ผ่าน Visual Capitalist 2023) **ไม่มีแหล่งฟรีที่จัด 10 อันดับด้วยเกณฑ์เดียว** จึงใช้สองเกณฑ์นี้ และหน้าจอบอกไว้ใต้รายการ
  - ราคาฟรีมีเฉพาะ XAU XAG XPT XPD ตัวอื่นขึ้น "ยังไม่มีแหล่งราคาฟรี" ไม่มีตัวเลข
  - หน่วย ออนซ์ทรอย (31.1034768 g) กรัม กิโลกรัม บาททอง (15.244 g) ทองคำเลือก 99.99% (× 0.9999) หรือ 96.5% (× 0.965) แสดงเป็นสกุลใดก็ได้ หมายเหตุ "ราคาตลาดโลกอ้างอิง ไม่ใช่ราคาซื้อขายหน้าร้าน"
  - ไอคอน `ic_metal_*` วาดเองทุกตัว 16×16 ต่างกันที่รูปร่าง: แท่งคางหมู ขดลวด เหรียญ แผ่นแบน ข้าวหลามตัด ก้อนหิน กระป๋อง เหรียญมีรู แผ่นลอน แบตเตอรี่
- debug `TEST_RATES --es fail jsdelivr,pages` ทำให้แหล่งที่ระบุล้มเหลว เพื่อดูว่าสลับไปแหล่งถัดไป

## 5ฉ. เครื่องเล่นเพลง (0.53.0, Poom 2026-09-24: "หน้าตาคล้าย Winamp")

**หน้าที่:** เล่นเพลงจากในเครื่องและจาก NAS รายการเพลง เล่นต่อเนื่อง เล่นซ้ำ สุ่ม ปรับเสียง เล่นต่อในเบื้องหลัง คุมจากการ์ดบนหน้าหลัก และสั่งด้วยเสียง
- **รูปแบบไฟล์ (Poom):** FLAC, ALAC, WAV, MP3, AAC, Opus ถอดรหัสด้วย MediaCodec ของ Android ทั้งหมด **ยกเว้น ALAC บน A07:** Android 16 ของเครื่องนี้ไม่มีตัวถอดรหัส ALAC (มีแค่ MP3, AAC, FLAC, Opus, Vorbis, PCM ตรวจจาก media_codecs.xml เมื่อ 2026-09-24) ไฟล์ ALAC จึงเล่นไม่ได้ ตั้งแต่ 0.56.0 หน้าจอบอกว่า "เล่นเพลง … ไม่ได้ เพราะเครื่องนี้ไม่มีตัวถอดรหัส ALAC" แล้วข้ามไปเพลงถัดไป (ก่อนหน้านี้เวลาเดินแต่ไม่มีเสียง) การรองรับ ALAC ต้องใช้ไลบรารีถอดรหัสเพิ่ม ซึ่งขัดกับการตัดสินเรื่องไลบรารี จึงรอ Poom ใช้ Media3 ExoPlayer อย่างเดียว ไม่มี decoder extension และไม่มีไลบรารี 22 MB DSD/APE/WavPack แสดงในรายการ NAS เป็นสีจางพร้อมคำ "ไม่รองรับไฟล์ DSD" ไม่ซ่อน
- **ตัวเล่นอยู่ใน `media/MusicService` (foreground service ชนิด mediaPlayback) ไม่ใช่ในหน้าจอ** ปิดหน้าเครื่องเล่นด้วย Back, X หรือ Hey Jarvis เพลงเล่นต่อ (ข้อ 12)
- **จาร์วิสพักผ่าน `WakePause` เดิม:** hold ตอนตั้งใจเล่น (รวมช่วง buffer ระหว่างเพลง) ต่ออายุทุก 30 วินาที ปล่อยตอนหยุดชั่วคราวและหยุด ถูกสั่งเงียบเพราะถามด้วยปุ่มจะหยุดชั่วคราวแต่ไม่ปล่อย hold แล้วเล่นต่อเมื่อจบรอบ ระหว่างเพลงดังคำปลุกปิด คำสั่งเสียงจึงต้องกดปุ่มจาร์วิสก่อน (ตามการตัดสินเดิมของข้อ 12)
- **ไม่ log ชื่อเพลงหรือ path:** `logcat -s KioskMusic:I` มีแค่สถานะและชนิดข้อผิดพลาด

**หน้าต่าง:** กรอบแผงควบคุม (แถบหัวข้อ `ic_pixel_music`, X 48×48dp, "กลับหน้าหลัก" 56dp) แท็บ "กำลังเล่น" · "คลังเพลง" 48dp เปิดที่คลังเพลงถ้ายังไม่มีรายการ

**"กำลังเล่น" ในบรรยากาศ Winamp (0.55.0, Poom ส่งภาพอ้างอิง):**
- **วาดเองทั้งหมด (กติกาของ Poom):** ไม่ใช้ไฟล์สกินจริง ไม่มีโลโก้ Winamp, Nullsoft หรือแบรนด์ใด ไม่ใช้ภาพจากหน้าจอต้นแบบ สีเป็นของเราเอง (`amp_*`) ภาพอ้างอิงอยู่ใน `docs/refs/` บนเครื่องของ Poom และไม่ถูก commit
- **สามหน้าต่างซ้อนกัน:**
  1. ตัวเครื่อง
  2. อีควอไลเซอร์
  3. รายการเพลง
  - หน้าต่าง 2 กับ 3 **ใช้พื้นที่ใต้ตัวเครื่องทีละหน้าต่าง** เปิดหน้าต่างหนึ่งแล้วอีกหน้าต่างปิด เพราะบนจอนี้ถ้าเปิดพร้อมกัน รายการจะเหลือสูงแค่แถบหัว (เห็นบน A07) ปุ่ม EQ / รายการ มีไฟในปุ่มบอกว่าอันไหนเปิดอยู่
- **ตัวเครื่อง:**
  - จอดำซ้าย (`AMP_LCD_W` 150dp) มีเวลา Press Start 2P 22sp (`AMP_TIME`) แตะเพื่อสลับเวลาที่เล่นไปกับเวลาที่เหลือ ("-1:23")
  - ใต้เวลามีกราฟแท่ง 19 แท่ง แท่งละ 8 ช่อง (`SpectrumView`) เขียว → เหลือง → ส้ม
  - ขวามี 3 แถว:
    1. ชื่อ "13. ศิลปิน - ชื่อเพลง (2:50)" เลื่อนเมื่อยาว (marquee)
    2. กล่อง kbps และ kHz
    3. mono / stereo ตัวที่ใช้อยู่สว่าง อีกตัวจาง
  - ใต้ 3 แถวนั้นมีศิลปิน · อัลบั้ม และภาพปก 32dp ถ้าไฟล์มี
  - แถวถัดมามีแถบระดับเสียง (ทอง) แถบสมดุลซ้ายขวา (เขียว เข้ากลางเองเมื่อใกล้กลาง) และปุ่ม EQ กับปุ่ม "รายการ" ระหว่างลาก บรรทัดชื่อบอกค่า ("ระดับเสียง 80%", "สมดุล: ซ้าย 30%", "ไปที่ 1:05") แบบต้นแบบ
  - แถบตำแหน่งเต็มความกว้าง ลากได้
  - ปุ่ม 6 ปุ่ม สูง 64dp มีคำใต้รูปเสมอ: ก่อนหน้า · เล่น · หยุดพัก · หยุด · ถัดไป · เปิดไฟล์ (เปิดไฟล์พาไปคลังเพลงในโหมดเพิ่ม)
  - แถวล่างสุดมี "■ สุ่ม" และ "■ ซ้ำ: ปิด / ทั้งรายการ / เพลงเดียว" ไฟในปุ่มสว่างเมื่อเปิด และเครื่องอ่านหน้าจอได้ยิน "(เปิดอยู่)"
  - **สุ่ม + ซ้ำทั้งรายการ (0.61.0, Poom): สุ่มลำดับใหม่ทุกรอบ** ไม่วนลำดับเดิม ทุกเพลงเล่นครบหนึ่งครั้งต่อรอบ และเพลงที่เพิ่งจบไม่เปิดรอบใหม่ (เมื่อมีสองเพลงขึ้นไป) กด "ถัดไป" เองที่ท้ายรายการก็เริ่มรอบใหม่แบบสุ่มใหม่เช่นกัน (`PlayQueue.next`, `ShuffleEndTest`)
- **ข้อมูลไฟล์มาจากไฟล์จริง:**
  - kHz และจำนวนช่องเสียงอ่านจาก `player.audioFormat`
  - kbps มาจากไฟล์ ถ้าไฟล์ไม่บอก (FLAC, WAV, ALAC) ใช้ขนาดไฟล์หารความยาว
  - ชื่อ ศิลปิน อัลบั้ม และภาพปกอ่านจาก tag ในไฟล์ (`mediaMetadata`) แล้วค่อยใช้ของ MediaStore
  - ที่ไฟล์ไม่บอกแสดงเป็น "—" ไม่เดา
- **อีควอไลเซอร์ (`media/fx/Eq.kt`, `media/fx/AudioFx.kt`):**
  - **ทำเองใน Media3** เพราะ Media3 ไม่มีอีควอไลเซอร์ และ audiofx ของ Android ให้ย่านตามที่เครื่องกำหนด (Samsung ส่วนใหญ่มี 5 ย่าน) ไม่ใช่ 10 ย่านนี้
  - 10 ย่าน (60, 170, 310, 600, 1K, 3K, 6K, 12K, 14K, 16K Hz) เป็น peaking filter ตาม RBJ cookbook กว้าง 1 octave ±12 dB บวกพรีแอมป์ ±12 dB
  - ทำงานกับ PCM 16-bit ใน `DefaultAudioSink` จึงมีผลกับเสียงจริงทุกเครื่อง
  - ไฟล์ทุกจำนวนช่องเสียงผ่านอีควอไลเซอร์ (เจอไฟล์ 12 ช่องบน A07) รูปแบบที่รับไม่ได้ผ่านไปเฉย ๆ ไม่ทำให้เพลงเล่นไม่ได้ (`AudioFxTest`)
  - ปุ่มที่มี: "■ เปิด", เส้นโค้งรวม (`EqCurveView`), "ชุดค่า ▼" (Flat, Rock, Pop, Jazz, Classical, เสียงร้อง, เบสหนัก) และแถบเลื่อน 11 อันมีชื่อใต้แต่ละอัน
  - **ไม่มีปุ่ม AUTO** เพราะทำจริงไม่ได้ในรอบนี้ ไม่ทำปุ่มหลอก
  - **แถบเลื่อน 11 อันกว้างอันละ 30dp ต่ำกว่า 48dp** จึงทำทั้งแผงเป็นพื้นที่สัมผัสเดียว:
    - คอลัมน์ที่นิ้วแตะคือแถบที่จับไว้ตลอดการลาก
    - ลากขึ้นลงเพื่อตั้งค่า ปัดเป็นเต็ม dB และใกล้ 0 จะเข้า 0
    - ระหว่างลาก ใต้แผงบอกย่านและค่า ("3K +5 dB") ถ้านิ้วตกผิดคอลัมน์จะเห็นทันที
  - ค่าเก็บใน SharedPreferences "music"
- **กราฟแท่ง (`media/fx/Spectrum.kt`):**
  - อ่านจากเสียงที่ตัวเล่นส่งออกลำโพง หลังผ่านอีควอไลเซอร์ **ไม่ใช่ไมค์ และไม่ใช้ `Visualizer`** (ต้องใช้สิทธิ์ RECORD_AUDIO)
  - FFT 1024 จุด หน้าต่าง Hann แบ่งเป็น 19 แท่งแบบ log ตั้งแต่ 50 Hz ถึง 16 kHz ช่วง -60 ถึง 0 dB
  - **ตรงกับเสียงที่ได้ยิน:** `Tap` เก็บตัวอย่างพร้อมตำแหน่งเริ่มของ stream (`positionOffsetUs`) หน้าจอขอตัวอย่าง ณ ตำแหน่งที่กำลังเล่น ไม่ใช่ตัวอย่างที่เพิ่งถอดรหัส ซึ่งเร็วกว่าเสียงเท่ากับ buffer
  - แท่งขึ้นทันทีและลงช้า ๆ ตอนหยุดชั่วคราวแท่งนิ่ง
  - เครื่องที่ปิด animation ของระบบจะวาดแค่ 2 ครั้งต่อวินาที และชื่อเพลงไม่เลื่อน
- **รายการเพลง:**
  - แถว 48dp: "12. ศิลปิน - ชื่อเพลง" และความยาวชิดขวา เพลงที่เล่นอยู่มีพื้น `amp_select`, ตัวหนา และ "►"
  - ช่องค้นหาในรายการ
  - ปุ่ม: เพิ่ม · ลบ · เลือก · เรียง · ล้าง
    - เพิ่ม = คลังเพลงในโหมดเพิ่ม มีแถบบอกว่า "แตะเพลงเพื่อเพิ่มเข้ารายการ" พร้อมปุ่ม "เสร็จ" และปุ่ม "เพิ่มทั้งหมดที่แสดง"
    - ลบหรือเลือก = โหมดเลือก ช่อง □/■ นำหน้าแถว และปุ่ม "เลือกทั้งหมด", "ไม่เลือกเลย", "ลบที่เลือก (n)", "เสร็จ"
    - เรียง = ชื่อเพลง / ศิลปิน / ความยาว / ยกเลิก
    - ล้าง = ถามยืนยันในแถวเดียวกันก่อน
  - เพลงที่เล่นอยู่ยังเล่นต่อได้ระหว่างแก้รายการ (`PlaylistTest`)
- **บันทึกไว้ใช้ครั้งหน้า (`media/Session.kt`):**
  - เก็บรายการ เพลงที่เล่น ตำแหน่ง สุ่ม และเล่นซ้ำไว้ใน `files/music_session.txt` (ไฟล์ของแอปเอง ไม่ log)
  - บันทึกทุก 5 วินาทีระหว่างเล่น และทุกครั้งที่หยุดหรือแก้รายการ
  - เปิดครั้งหน้ารายการกลับมาในสถานะหยุด กดเล่นแล้วเล่นต่อจากตำแหน่งเดิม
  - "หยุด" (ไม่ใช่หยุดพัก) คือเริ่มเพลงใหม่ตั้งแต่ต้น
- **เล่นทีละเสียง:** เริ่มเพลงแล้ววิดีโอที่เล่นอยู่หยุดพัก และกลับกัน ตัวเล่นที่หยุดไปแล้วจะไม่ถูกปลุก
- **ข้อจำกัดในการทดสอบ:** กราฟแท่งวาดใหม่ตลอดเวลา `uiautomator dump` จึงตอบ "could not get idle state" บนหน้านี้ ตรวจด้วยภาพหน้าจอและ `dumpsys activity top` แทน (TESTING.md)
- **คลังเพลง:** แถวเลือกแหล่ง "แหล่งเพลง  ● ในเครื่อง  ○ NAS" เป็นปุ่มตัวเลือกแบบ Win95 ไม่มีพื้นกรมท่า กว้างตามคำ สูง 48dp (0.54.1, Poom: ไม่ให้เห็นแถบกรมท่าซ้อนสองแถวใต้แท็บ) ในเครื่องมาจาก MediaStore พร้อมช่องค้นหา (ชื่อ ศิลปิน อัลบั้ม) และ "เล่นทั้งหมด" NAS เปิดดูแบบอ่านอย่างเดียว ◄ ขึ้นหนึ่งระดับ [เล่นโฟลเดอร์นี้] [เพิ่มเข้าคลังสำหรับสั่งด้วยเสียง] โฟลเดอร์ที่เพิ่มจะถูกอ่านรายชื่อครั้งเดียว เก็บชื่อไว้ในแอป (ชื่อเพลง = ชื่อไฟล์, ศิลปิน = ชื่อโฟลเดอร์)
- ทุกสถานะมีคำ: กำลังอ่าน / ไม่พบเพลงในเครื่อง (บอกรูปแบบที่รองรับและที่ต้องวางไฟล์) / ไม่พบเพลงที่ตรงกับคำค้นหา / ยังไม่ได้ตั้งค่า NAS (บอกที่ตั้ง) / เปิด NAS ไม่ได้ / ไฟล์เล่นไม่ได้ (สีแดง แล้วข้ามไปเพลงถัดไป ไม่วนซ้ำ)

**การ์ด "เครื่องเล่นเพลง" บนหน้าหลัก:** ลงทะเบียนเป็นการ์ดสุดท้าย จึงอยู่ติดเหนือจาร์วิสใต้นิ้วโป้ง แสดงเมื่อมีเพลงเล่นอยู่หรือหยุดชั่วคราว ซ่อนเมื่อหยุด
- ชื่อเพลง · ศิลปิน (`type_secondary` 14sp) แล้วปุ่มคำ 48dp [หยุดชั่วคราว|เล่นต่อ] [ถัดไป] แตะชื่อเพลงหรือแถบหัวข้อเปิดเครื่องเล่น
- **`CardBoard.holdOpen`:** ระหว่างแสดง การ์ดนี้เปิดค้างและไม่ถูกหุบ แต่ไม่กระโดดขึ้นบนสุดแบบ pinned การ์ดอื่นหุบให้แทน จาร์วิสจึงไม่ต่ำกว่า 156dp (`MusicScreenTest`)

**สั่งด้วยเสียง:** broker แยกคำสั่งในโค้ด (`server/kiosk_broker/music.py`) ส่ง {command, query} ให้เครื่อง เครื่องหาเพลงและทำก่อนพูด (`media/MusicVoice`) หาไม่เจอพูดว่า "ไม่พบเพลง "…" ในเครื่องครับ" ไม่บอกว่าเปิดแล้ว การค้นหาไม่สนเว้นวรรค ตัวพิมพ์ และวรรณยุกต์ และยอมผิดหนึ่งตัวอักษรต่อห้าตัว

**0.59.0:** แท็บ "คลังเพลง" (เพลงทั้งเครื่องจากดัชนีของ Android) เปลี่ยนเป็นแท็บ "Playlist" และปุ่ม "เพิ่ม"/"เปิดไฟล์" เปิดหน้าเลือกไฟล์จากโฟลเดอร์ ดูข้อ 5ฌ

## 5ช. ขนาดมาตรฐานของแผงควบคุม (0.54.0, Poom 2026-09-24: "ปรับทุกหน้าในแผงควบคุมตามหลัก UX/UI")

**ไฟล์เดียว:** `app/src/main/java/com/mammonrn/phoneaikiosk/ui/UiScale.kt`
- ทุกความสูง ระยะห่าง ไอคอน และขนาดตัวอักษรบนหน้าจอของแผงควบคุมต้องเรียกชื่อจากไฟล์นี้ ห้ามเขียนตัวเลขตรงที่ใช้ หน้าจอที่นับรวมมี:
  - ทุกไฟล์ใน `settings/` (แผงควบคุม นาฬิกาปลุก ยืนยันตัวตน ที่มาข้อมูล ไฟในบ้าน)
  - `files/FilesActivity`
  - `calc/CalculatorActivity` และ `calc/ElectricalPages`
  - `media/MusicActivity`
  - `auth/VerifyActivity`
- `UiScaleTest` จะล้มเมื่อเจอ `dp(ตัวเลข)`, `textSize = ตัวเลข` หรือ `text(…, ตัวเลขf)` ในไฟล์เหล่านี้ และล้มเมื่อค่าในไฟล์ผิดกติกาด้านล่าง
- ตัวอักษรข้อมูลบนหน้าหลักยังใช้ `type_scale.xml` (ข้อ 4) ตามเดิม

**ความสูงของสิ่งที่กด:**

| ชื่อ | dp | ใช้กับ |
|---|---|---|
| `TOUCH` | 48 | ทุกปุ่ม ปุ่มเลือก แท็บ ช่องกรอก และปุ่ม X (ขั้นต่ำ) |
| `PRIMARY` | 56 | การกระทำหลักหนึ่งอย่างต่อหน้า รายการเมนู (เช่นเครื่องมือไฟฟ้า) และ "กลับหน้าหลัก" |
| `ICON_BUTTON` | 64 | ปุ่มที่มีไอคอนอยู่เหนือคำ (◄◄ ► ■ ►► ของเครื่องเล่นเพลง) |
| `ROW` | 56 | ความสูงขั้นต่ำของแถวรายการสองบรรทัด (ไฟล์ เพลง) |

- ปุ่มคำทุกปุ่มกว้างตามคำ มีระยะในซ้ายขวา 12dp และกว้างอย่างน้อย 48dp (`minWidth`) ไม่ตั้งความกว้างเป็นตัวเลข

**ระยะห่าง (ตาราง 4dp):**

| ชื่อ | dp | ใช้กับ |
|---|---|---|
| `SPACE_XS` | 4 | ภายในกลุ่มเดียวกัน เช่น ป้ายกับช่องกรอก หรือไอคอนกับคำ |
| `SPACE_S` | 8 | ระหว่างปุ่มที่ติดกัน (ป้องกันกดพลาด) และระยะในของกล่องหรือช่องกรอก |
| `SPACE_M` | 12 | ระหว่างกลุ่มในหน้าเดียวกัน |
| `SPACE_L` | 16 | ก่อนการกระทำหลัก และระหว่างส่วนที่ต่างกัน |
| `FRAME` | 8 | รอบหน้าต่างบนพื้นเดสก์ท็อป |
| `WINDOW_INSET` | 4 | ภายในขอบนูนของหน้าต่าง |

**ไอคอน:** 16 / 24 / 32 / 48dp (`ICON_S` ถึง `ICON_XL`) คือภาพพิกเซล 16×16 ที่ขยายเป็นขั้นละ 8dp

**ตัวอักษรตามลำดับความสำคัญ (sp, ขั้นต่ำ 13 ที่ระยะ 20 cm):**

| ลำดับ | ชื่อ | sp | ใช้กับ |
|---|---|---|---|
| 1 | `TEXT_DISPLAY` | 24 | ค่าใหญ่หนึ่งเดียวของหน้า เช่น เวลาตอนตั้งปลุก |
| 2 | `TEXT_VALUE` | 18 | ค่าที่เป็นใจความของแถว เช่น เวลาปลุกในรายการ หรือเวลาเพลง |
| 3 | `TEXT_HEADING` | 16 | หัวข้อหน้า คำบนปุ่มหลัก และข้อความในช่องกรอก |
| 4 | `TEXT_ITEM` | 15 | บรรทัดหลักของรายการหรือผลลัพธ์ |
| 5 | `TEXT_BASE` | 14 | เนื้อความ คำบนปุ่ม แท็บ และชื่อหน้าต่าง |
| 6 | `TEXT_NOTE` | 13 | หมายเหตุ คำแนะนำ บรรทัดรอง และป้ายหัวข้อย่อย (ตัวหนา) |

- ปุ่มเครื่องคิดเลขใช้ฟอนต์พิกเซลตามขนาดของตัวเอง:
  - `KEY_TEXT` 16 สำหรับปุ่มตัวเดียว
  - `KEY_WORD` 13 สำหรับปุ่ม 2–4 ตัว (เดิม 10 และ 12)
  - `KEY_SYMBOL` 24 สำหรับ × ÷
  - `KEY_EQUALS` 18 สำหรับ =

**ขอบนูน (แก้ใน 0.54.0):** เดิม `retro_raised` และ `retro_sunken` วาดชั้นหน้าปุ่มทับเงาด้านล่างและขวา ทุกปุ่มจึงไม่เห็นเป็นปุ่มนูน ตอนนี้ชั้นหน้าปุ่มเว้นขอบ 2dp ครบทั้งสี่ด้าน (`UiScaleTest`)

## 5ซ. เครื่องเล่นวิดีโอ (0.56.0, Poom ส่งภาพอ้างอิง PowerDVD)

**หน้าที่:** เล่นวิดีโอจากในเครื่องและจาก NAS บนหน้าจอของแอปเอง เล่นต่อเป็นเสียงเมื่อปิดหน้าจอ คุมจากการ์ดบนหน้าหลัก ข้อ 12 ใช้เหมือนเพลง
- **ข้อจำกัดของ Poom:** ไม่เกิน 720p ไม่รองรับ 4K และ AV1
  - ไฟล์ที่เกิน **ไม่ถูกเล่น** หน้าจอบอกเหตุผลเป็นคำ: "ไม่เล่นไฟล์นี้ เพราะเกินขีดจำกัดของเครื่อง (1920×1080)…"
  - ในรายการ ไฟล์ที่ MediaStore รู้ว่าใหญ่เกินจะมีคำว่า "เกินขีดจำกัด" ติดไว้ ส่วนไฟล์ NAS และ AV1 จะรู้เมื่อเปิดไฟล์ (`VideoRules.refusal`)
  - รูปแบบไฟล์: MP4, M4V, MKV, WebM, 3GP, MOV ถอดรหัสด้วย MediaCodec ของเครื่อง
- **วิดีโอเล่นเฉพาะบนหน้าจอของมัน (0.58.0, Poom อนุมัติ, ยกเลิกการเล่นเบื้องหลังของ 0.56.0):**
  - ออกจากเครื่องเล่น (X, "◄ รายการ", Back, คำถามจาร์วิสที่ปิดหน้าจอ) → หยุดทันที และเก็บตำแหน่งที่ดูค้าง
  - จอดับหรือมีหน้าอื่นมาบัง → หยุดชั่วคราว และเก็บตำแหน่งไว้
  - ทุกกรณีปล่อย hold ของ `WakePause` ทันที คำปลุกกลับมาเอง (ทดสอบด้วยเสียง Hey Jarvis จริง)
  - ตัวเล่นยังอยู่ใน `media/VideoService` ระหว่างเล่นจาร์วิสพักด้วย `WakePause.Source.VIDEO` และการ์ดจาร์วิสบอกว่า "พักระหว่างเล่นวิดีโอ"
  - **เพลงยังเล่นเบื้องหลังได้เหมือนเดิม (ข้อ 12)**
- **ไม่ log ชื่อหรือ path:** `logcat -s KioskVideo:I` ตำแหน่งที่ดูค้างเก็บเป็น hash ของ id ไม่ใช่ชื่อไฟล์

**หน้ารายการ:** กรอบแผงควบคุม แถบหัวข้อ `ic_pixel_video_light`
- แถว "แหล่งวิดีโอ ● ในเครื่อง ○ NAS" แบบเดียวกับคลังเพลง
- แถวละ 56dp: ชื่อ แล้ว "ความยาว · 480p · ดูถึง 12:34"

**หน้าเล่น (บรรยากาศ PowerDVD วาดเอง ไม่มีโลโก้):**
- ภาพอยู่กลางพื้นดำ รักษาสัดส่วน (`AspectFrame`) "เต็มจอ: เปิด" คือขยายจนเต็มจอแล้วตัดขอบ
- **แถบบน:** "◄ รายการ", ชื่อไฟล์, X 48dp (กลับหน้าหลัก) บนพื้น `video_scrim`
- **แผงเงินลอยด้านล่าง (`DvdSkin`, `RingView`):**
  - ซ้าย: + / ลำโพงกับ % / − ปุ่มละ 48dp
  - จอฟ้าอมเขียว: "ไฟล์ 02/08" (แทน TITLE/CH ของต้นแบบ) เวลา "0:00:02" Press Start 2P "จาก 0:00:30" และ "480p · 1.0×"
  - วงแหวน 128dp (`VIDEO_RING`):
    - กลางวงเป็นปุ่มเล่น/หยุดพัก กว้าง 58dp
    - แตะที่ 9 นาฬิกา = ถอย 10 วินาที แตะที่ 3 นาฬิกา = ข้าม 10 วินาที
    - ลากรอบวงเพื่อเลื่อนตำแหน่ง (12 นาฬิกาคือต้นไฟล์) ระหว่างลากจอบอกเวลาที่จะไป ยกนิ้วแล้วค่อยเลื่อน
    - เส้นโค้งบนวงคือตำแหน่งปัจจุบัน
    - พื้นที่แตะเลยขอบวงออกไป 16dp
  - ขวา: เปิดไฟล์ และหยุด ปุ่มละ 64dp มีคำใต้รูป
  - แถวล่าง 64dp มีสองบรรทัดต่อปุ่ม: "ความเร็ว 1.0×" (0.5 → 2.0 วนไป), "เสียง" (แทร็กเสียงในไฟล์), "คำบรรยาย" (แทร็กคำบรรยาย หรือปิด), "เต็มจอ เปิด/ปิด"
  - เมนูแทร็กบอกว่าอันไหนเลือกอยู่ด้วย ● ไฟล์ที่ไม่มีบอกตรง ๆ ว่า "ไฟล์นี้ไม่มีคำบรรยาย"
- **ซ่อนเองหลังไม่ได้แตะ 4 วินาทีระหว่างเล่น** แตะภาพเพื่อแสดงอีกครั้ง หยุดพักแล้วแผงไม่ซ่อน
- **คำบรรยาย:** ตัวหนาสีขาวมีเงาดำ อยู่เหนือแผง เลือกแทร็กไทยก่อน แล้วอังกฤษ แล้วไม่ระบุภาษา (เครื่องไม่เลือกให้เองถ้าไม่บอกภาษา เห็นบน A07)
- **จำตำแหน่งที่ดูค้างของแต่ละไฟล์:** บันทึกทุก 5 วินาทีและตอนหยุด ถ้าเพิ่งเริ่ม (ไม่ถึง 5 วินาที) หรือใกล้จบ (เหลือไม่ถึง 15 วินาที) จะเริ่มจากต้น (`VideoRules.resumeAt`)
- **แนวนอนเฉพาะตอนเต็มจอ (0.57.0, Poom อนุมัติ):**
  - "เต็มจอ: เปิด" → เครื่องหมุนตามเซนเซอร์ได้ (`SCREEN_ORIENTATION_SENSOR`) แนวนอนภาพพอดีจอ แนวตั้งภาพขยายเต็มแล้วตัดขอบ
  - **บั๊กที่แก้ใน 0.58.0 (Poom หมุนเครื่องจริงแล้วภาพไม่หมุน):** ระบบของเครื่องไม่ได้ขวาง (auto-rotate เปิด, `USER_ROTATION_FREE`, policy ไม่ล็อก) log ของ A07 ตอน 20:58:56 แสดงว่าเซนเซอร์บอกแนวนอนแล้ว แต่ WindowManager คำนวณจาก `lastOrientation=PORTRAIT` คือแอปยังขอแนวตั้งอยู่
    - ทางที่ตรงกับอาการ: เปิดเต็มจอค้างไว้ แล้วเลือกวิดีโอเรื่องอื่น หน้าเล่นไม่ได้ขอเซนเซอร์ใหม่ ปุ่มขึ้นว่า "เปิด" แต่ยังเป็นแนวตั้ง
    - ตอนนี้ขอใหม่ทุกครั้งที่หน้าเล่นแสดง และ log ทุกครั้งที่ขอ (`KioskVideo: orientation asked=…`)
    - ไม่แตะค่าระดับทั้งเครื่อง
  - แผงที่ซ่อนอยู่: แตะครั้งแรกแค่เรียกแผงกลับมา ยังไม่กดปุ่ม ปุ่ม "เต็มจอ" ต้องเห็นอยู่ก่อนจึงจะกดได้
  - ปุ่ม "หมุนจอ อิสระ / ล็อก" (64dp ในแถวล่าง) ล็อกไว้ที่ทิศปัจจุบัน (`SCREEN_ORIENTATION_LOCKED`) ใช้ได้เมื่อเต็มจอ นอกนั้นปุ่มจางลง
  - "เต็มจอ: ปิด", ออกไปหน้ารายการ, ออกจากเครื่องเล่น (Back, X, Hey Jarvis) หรือวิดีโอจบหรือถูกหยุด → แนวตั้งทันที
  - หมุนแล้วหน้าจอไม่ถูกสร้างใหม่ (`configChanges` ใน manifest) ภาพ เสียง ตำแหน่ง และคำบรรยายไม่สะดุด
  - แนวนอนแผงลอยกว้าง `VIDEO_PANEL_W` 480dp อยู่กลาง
  - ระหว่างแนวนอนหน้าหลักไม่ได้อยู่หน้าจอ มุมทางออก 10 ครั้งจึงไม่ทำงาน แต่ lock task ยัง LOCKED ตลอด
  - debug: `TEST_VIDEO_ROTATE --es to landscape|portrait` จำลองสิ่งที่เซนเซอร์บอก (ใช้ได้เมื่อเต็มจอและไม่ล็อก)
- **คำสั่งเสียง (0.57.0):** "เปิดวิดีโอ [ชื่อ]", "หยุดวิดีโอ", "เล่นวิดีโอต่อ", "ปิดวิดีโอ" แยกคำในโค้ด (`server/kiosk_broker/video.py`) เครื่องหาไฟล์และทำก่อนพูด (`media/VideoVoice`) หาไม่เจอพูดว่า "ไม่พบวิดีโอ "…" ในเครื่องครับ"
  - **ตั้งแต่ 0.58.0:** คำถามทุกคำถามจบที่หน้าหลัก (ข้อ 11) จึงปิดเครื่องเล่นไปแล้ว และวิดีโอหยุดแล้ว "หยุดวิดีโอ" จึงตอบตามจริงว่า "วิดีโอหยุดอยู่แล้วครับ" ส่วน "เล่นวิดีโอต่อ" เปิดหน้าเล่นเรื่องล่าสุดจากตำแหน่งที่ค้างไว้ ปุ่ม "จาร์วิส" บนแถบบนของเครื่องเล่นเอาออกแล้ว เพราะมีไว้หยุดวิดีโอที่เล่นเบื้องหลัง
  - คำสั่ง "หยุด" ของเพลงหยุดจริงและปล่อย hold (`pauseOnPurpose`) พอจบคำตอบจึงไม่เล่นต่อเอง (แก้ใน 0.57.0)

**บันไดลดภาระเมื่อเครื่องร้อน (`HeatLadder`, `HeatWatch`, ผูกตั้งแต่ตัวเล่นตัวแรกเริ่ม):** อ่าน thermal status ของ Android (0–6)

| ขั้น | status | สิ่งที่ทำ (สะสมจากขั้นก่อน) |
|---|---|---|
| NORMAL | 0–1 | ไม่ลดอะไร (วิดีโอไม่เกิน 720p ตามปกติ) |
| COOLER | 2 moderate | หน้าวิดีโอหรี่เหลือ 70% และกราฟแท่งของเพลงหยุด |
| COOLEST | 3 severe | หน้าวิดีโอหรี่เหลือ 40% |
| PAUSE | 4 critical | เพลงและวิดีโอหยุดพัก ไม่รับคำสั่งเล่น และบอกว่าทำไม |
| STOP | 5–6 | หยุดเล่น |

- **ทำไมไม่ลดความละเอียด:** ไฟล์ในเครื่องหรือบน NAS มีขนาดเดียว ตัวเล่นถอดรหัสให้เล็กลงไม่ได้ ทดสอบบน A07 แล้ว ตั้งเพดานไว้ ไฟล์ 720p ก็ยังเป็น 720p จึงเปลี่ยนเป็นหรี่จอ ซึ่งลดภาระได้จริง เพดาน 480p/360p ยังตั้งไว้ใช้กับไฟล์ที่มีหลายขนาด แต่หน้าจอไม่เคยอ้างว่าลดความละเอียดแล้ว

- ขึ้นขั้นทันที ลงทีละขั้นหลังเย็นลงครบ 60 วินาที เพื่อไม่ให้สลับไปมา
- หยุดพักเพราะร้อนแล้วไม่เล่นต่อเอง ต้องให้คนกดเล่น
- บนภาพมีคำบอกขั้นที่ใช้อยู่ เช่น "เครื่องร้อน จึงหรี่หน้าจอเหลือ 70%…"
- `VideoRulesTest` ตรวจการเดินของบันได

**การ์ดบนหน้าหลัก:** ไม่มีการ์ดของวิดีโอแล้ว (0.58.0) วิดีโอไม่เล่นหลังหน้าหลัก การ์ด "music" คุมเพลงอย่างเดียว

**0.59.0:** หน้ารายการของเครื่องเล่นวิดีโอแสดงวิดีโอใน playlist ที่เลือก ไม่ใช่วิดีโอทั้งเครื่อง มีปุ่ม "Playlist ทั้งหมด" และปุ่มหลัก "เพิ่มวิดีโอ" ดูข้อ 5ฌ

## 5ฌ. Playlist, โฟลเดอร์ และไฟล์เดียว (0.59.0, Poom อนุมัติ)

**แสดงเฉพาะที่ผู้ใช้เลือกเอง:** เครื่องเล่นเพลงและวิดีโอเลิกดึงไฟล์ทั้งเครื่องจาก MediaStore แสดงเฉพาะ playlist ที่สร้างเอง (`media/PlaylistBook`, เก็บใน `files/playlists.txt` ของแอป ไม่ log ชื่อ)
- playlist หลายชุดต่อชนิด ตั้งชื่อ (ห้ามว่าง ห้ามซ้ำในชนิดเดียวกัน ยาวไม่เกิน 40 ตัวอักษร) เปลี่ยนชื่อ ลบได้ **ลบต้องถามยืนยันในที่เดิม** พร้อมบอกว่าไฟล์ในเครื่องไม่ถูกลบ
- หน้า Playlist (`media/PlaylistsPage` ใช้ร่วมทั้งสองเครื่องเล่น): แตะชื่อเพื่อเลือก แถวที่เลือกพื้นกรมท่า ► คือรายการที่ใช้อยู่ แล้ว [เล่น][เพิ่ม][เปลี่ยนชื่อ][ลบ] ปุ่มหลัก "สร้าง playlist ใหม่" 56dp ตั้งชื่อแล้วไปหน้าเพิ่มไฟล์ต่อทันที
- **หน้าเพิ่ม (`media/MediaPicker`):** "จาก ● เครื่อง ○ การ์ด SD ○ NAS" เป็นแถวตัวเลือกแบบเรียบ แล้วโฟลเดอร์มาตรฐาน (ข้อ 5ค) พร้อมช่องติ๊ก ปุ่ม [เพิ่มทั้งโฟลเดอร์นี้][ยกเลิก] และปุ่มหลัก **"เพิ่ม 5 เพลง" ที่ตัวเลขเป็นจำนวนที่เลือกจริง** (บั๊ก 0.58: เพิ่มกี่เพลงก็ขึ้น 1) ไม่ได้เลือกสักรายการปุ่มเป็นสีจาง **ทุกครั้งที่เปิดหน้าเพิ่มใหม่ ตัวเลขทุกจุดเริ่มที่ 0** (บั๊ก 0.59–0.60: แถบบนค้าง "เลือกแล้ว 4" ขณะปุ่มขึ้น "เพิ่ม 0 เพลง" แก้ 0.61.0 ด้วย `FolderBrowser.clearTicks`) เพิ่มทั้งโฟลเดอร์ = ทุกไฟล์ใต้โฟลเดอร์ ลึกได้ 8 ชั้น ไม่เกิน 5000 ไฟล์ ไฟล์ที่มีอยู่แล้วไม่ถูกเพิ่มซ้ำ และบอกผลว่าเพิ่มกี่รายการ มีอยู่แล้วกี่รายการ
  - ชื่อที่ใช้ค้นด้วยเสียงมาจาก tag ของไฟล์ในดัชนีของเครื่องเมื่อมี (อ่านเฉพาะไฟล์ที่เลือก) ไม่มีก็ใช้ชื่อไฟล์
  - NAS ต่อผ่าน `files/NasSource` ใน picker เดียวกัน (อ่านอย่างเดียว) รอบนี้ยังไม่ได้ทดสอบกับ NAS จริง
- **ย้ายของเดิม:** ครั้งแรกที่ 0.59.0 ทำงาน รายการของเครื่องเล่นเพลงเดิม (`music_session.txt`) กลายเป็น playlist **"รายการเดิม"** ครบทุกเพลงตามลำดับเดิม แม้มีเพลงซ้ำ และเป็นรายการที่ใช้อยู่ โฟลเดอร์ NAS ที่เคยเพิ่มเข้าคลังเก่ากลายเป็น "คลัง NAS เดิม"
- การแก้รายการที่กำลังเล่น (เพิ่ม ลบ เรียง ล้าง) บันทึกลง playlist นั้นทันที รายการที่เปิดด้วยเสียงไม่ใช่ playlist
- **ลบหลายเพลงในรายการที่เล่น:** ช่องติ๊ก ☐/☑ แบบเดียวกับหน้าเพิ่ม หัวรายการเป็น "เลือกแล้ว N รายการ" และปุ่ม "ลบ N เพลง"
- **คำสั่งเสียง** "เปิดเพลง/เปิดวิดีโอ ชื่อ…" ค้นเฉพาะใน playlist · **ชื่อที่ถอดเพี้ยน (0.61.0, Poom):** ถ้าค้นแบบตรง/มีอยู่ในชื่อ/ใกล้ ไม่เจอ ให้เทียบความใกล้เคียง (`MusicLibrary.guess`: ระยะแก้คำ คู่ตัวอักษร และพยัญชนะล้วน) ใกล้สุด ≥0.6 และนำอันดับสอง ≥0.15 → เปิดเลยและพูดชื่อจริง "เปิดเพลง “ทะเลสีชมพู” ครับ" · สองเรื่องใกล้พอกัน → ถามกลับ "หมายถึงเพลง “A” หรือ “B” ครับ" · ไม่ใกล้ → ไม่พบเหมือนเดิม (ตัวอย่างจริงจาก A07 "ทรสีชมพู" "ทรสี ชมพุง" "ทรัล สี ชมพูง" → ทะเลสีชมพู ใน `FuzzyTitleTest`) · คำใบ้ STT: ยังไม่ส่งชื่อ playlist เพราะ prompt ของ Groq ใช้ไป 142/150 ตัวอักษร เพิ่มแล้วเกินเพดาน ไม่พบตอบว่า "ไม่พบเพลง “…” ใน playlist ครับ"
- **ภาคต่อ (0.63.0, Poom):** "เปิดวิดีโอ คู่โจร" เมื่อ playlist มี คู่โจร1 และ คู่โจร2 (ชื่อที่ต่างกันแค่เลขท้าย / ภาค N / ตอนที่ N / Part N — ปีสี่หลักไม่นับ) → เปิดภาคที่ดูล่าสุด ถ้าไม่มีเปิดภาคที่ดูค้าง (ภาคหลังสุด) ถ้าไม่มีเปิดภาคแรก และพูดชื่อจริงทุกครั้ง · ขอภาคด้วยเลข = ภาคนั้น · ชื่อใกล้กันที่ไม่ใช่ภาคต่อ ยังถามกลับ (`MusicLibrary.sequel/parts`, `SequelTest`) · ใช้กับวิดีโอ (เพลงยังใช้กฎเดิม)

**ชนิดไฟล์ (`media/MediaKinds`, ที่เดียว):** เพลง mp3 flac wav aac m4a ogg opus wma / วิดีโอ mp4 m4v mkv webm 3gp mov avi mpg mpeg dat vob ts wmv flv / รูป jpg jpeg png gif webp heic heif bmp ตัวจัดการไฟล์และเครื่องเล่นอ่านรายการเดียวกันนี้ (".flac" ที่มีเว้นวรรคก่อนจุดก็เป็น flac)

**จุดเลือกตัวเล่น (`media/PlayerChoice`, ที่เดียว):** ทั้งสอง service ถามที่นี่ก่อนโหลดไฟล์ ไม่มีที่อื่นตัดสิน
- **0.60.0 (Poom ตัดสินตามผล codecprobe): Media3 เป็นตัวเล่นหลัก LibVLC เล่นเฉพาะไฟล์ที่ Media3 เล่นไม่ได้** ข้อ 5ญ
- Media3: mp3 flac wav aac ogg opus และ mp4 m4v mkv webm 3gp mov avi และ m4a ที่ข้างในเป็น AAC (ได้อีควอไลเซอร์ของเราและกราฟแท่ง) **ไฟล์ที่ Media3 เล่นได้ห้ามย้ายไป VLC**
- LibVLC: dat mpg mpeg vob ts wmv wma flv และ **m4a ที่ข้างในเป็น ALAC** ตัดสินจาก codec จริงในไฟล์ (`media/Mp4Sniff` อ่าน moov › trak › stsd) ไม่ใช่จากนามสกุล
- ไฟล์ที่ Media3 เปิดแล้วถอดไม่ได้ (ไม่มีตัวถอดรหัส หรือไม่มีภาพที่แสดงได้) ถูกส่งต่อให้ LibVLC ครั้งเดียว ไม่ข้ามทิ้ง ถ้า LibVLC ก็เล่นไม่ได้จึงขึ้นข้อความ **ห้ามค้าง ห้ามเล่นเงียบ**
- **ไฟล์ใน playlist ที่หายไปจากเครื่อง:** ขึ้น "ไม่พบไฟล์ “…” ในเครื่อง" (ไม่ใช่ "ไฟล์อาจเสีย") แล้วข้ามไปรายการถัดไป ข้อความหายเมื่อรายการว่างหรือผู้ใช้เปลี่ยนเพลง

**ไฟล์เดียวจากตัวจัดการไฟล์ (Poom: "ห้ามเพิ่มเข้า playlist ใดๆ"):**
- เพลง: เครื่องเล่นเพลงเปิดโดยไม่มีแท็บและไม่มีรายการ แทนที่ด้วยกล่องบอกว่ากำลังเล่นไฟล์เดียวและปุ่ม "กลับไปที่รายการเดิม" ก่อนเล่นเก็บภาพรวมของรายการไว้ (`PlayQueue.Snapshot`: เพลง ลำดับสุ่ม ตำแหน่งในรายการ สุ่ม เล่นซ้ำ) กับตำแหน่งในเพลงและ playlist ที่ใช้อยู่ **ปิดหน้าจอ เล่นจบ หรือกดปุ่ม → ทุกอย่างกลับมาเหมือนเดิม** เพลงเดิมหยุดชั่วคราวที่ตำแหน่งเดิม ระหว่างนั้นไฟล์ที่บันทึกไว้ยังเป็นรายการเดิม (ถ้าแอปถูกปิดกลางทาง ไม่มีอะไรหาย)
- วิดีโอ: เปิดหน้าเล่นทันที ปุ่มบนซ้ายเป็น "◄ ตัวจัดการไฟล์" ออกแล้วรายการวิดีโอและลำดับกลับมาเหมือนเดิม
- รูป: `files/ImageViewerActivity` พื้นดำ ทีละรูป ขยายสองนิ้วได้ถึง 8 เท่า ลากได้เมื่อขยาย แตะสองครั้งกลับเต็มรูป **ไม่มีการปัดไปรูปถัดไป** ปุ่ม "ปิด" กลับไปโฟลเดอร์ ถอดรหัสบน worker ไม่เกินสองเท่าของจอ เป็นหน้าจอของแอป lock task จึงยัง LOCKED

## 5ญ. ตัวเล่นสำรอง LibVLC (0.60.0, Poom ตัดสินแล้ว)

**ทำไม:** codecprobe บน A07 (2026-09-24) พบว่าเครื่องไม่มีตัวถอดภาพ MPEG-1/2 ไม่มีตัวถอด ALAC และ Media3 ไม่มีตัวอ่าน ASF (WMV/WMA) ไฟล์ VCD ของ Poom จึงเล่นไม่ได้ Poom เลือกทาง (ข): Media3 เป็นหลัก LibVLC เป็นตัวสำรอง

**build เองแบบ LGPL ล้วน** (`licenses/LIBVLC.md`):
- LibVLC 3.7.6 arm64 อย่างเดียว ตัด debug symbol build ครั้งเดียวด้วย workflow แยก (`.github/workflows/libvlc-lgpl.yml`, `tools/libvlc/build-lgpl.sh`) ในภาพ build ของ VideoLAN
- ตรึงทุกอย่างด้วย commit แล้วเก็บผลเป็น GitHub Release asset **ไม่ commit ไฟล์ .so เข้า git** ตอน build แอป Gradle ดาวน์โหลดจาก release ที่ตรึงไว้ และ**ปฏิเสธถ้า SHA-256 ไม่ตรง**
- contribs build ด้วย `--disable-gpl --disable-gnuv3` ไม่เปิด dvdread/dvdnav โมดูลของ VLC ทุกตัวที่ประกาศตัวเองเป็น GPL ถูกตัดออกก่อนลิงก์ และตรวจ libvlc.so ที่ได้อีกรอบ มี GPL เหลือ build ล้ม
- ประกาศ LGPL อยู่ในหน้า "ที่มาข้อมูล" ข้อความเต็มอยู่ที่ `licenses/LGPL-2.1.txt` และในแอปที่ `assets/licenses/`

**ไฟล์ที่เล่นผ่าน VLC ต้องทำงานเหมือนเดิม:**
- **อีควอไลเซอร์:** ใช้ EQ 10 แบนด์ของ VLC แทนตัวที่เราเขียน (Poom) ค่าที่ผู้ใช้ตั้งไว้ใส่ให้ VLC ตรงแบนด์ (ความถี่ชุดเดียวกัน 60 Hz–16 kHz) preamp และ dB ตามเดิม ปิด EQ = ไม่มี EQ ข้อความใต้ EQ บอกว่าไฟล์นี้ใช้ EQ ของ VLC **ปรับเสียงซ้ายขวาใช้ไม่ได้กับไฟล์ VLC** (VLC ไม่มีให้) — **0.61.0 (Poom): แถบสมดุลต้องบอกว่าใช้ไม่ได้ ห้ามเลื่อนได้แต่ไม่มีผล** ระหว่างเล่นไฟล์ VLC แถบไม่มีปุ่มเลื่อน (ต่างที่รูปทรง ไม่ใช่สีอย่างเดียว) จางลง แตะแล้วบรรทัดชื่อเพลงขึ้น "ปรับเสียงซ้ายขวาไม่ได้ ไฟล์นี้เล่นผ่าน VLC" 3 วินาที และ TalkBack อ่านว่าใช้ไม่ได้ ไฟล์ Media3 กลับเป็นแถบปกติ
- **กราฟแท่ง:** ช่องกราฟขึ้น "ไม่มีกราฟสำหรับไฟล์นี้" **ห้ามทำกราฟขยับหลอก**
- ความยาว ตำแหน่ง เดินหน้า/ถอยหลัง ระดับเสียง ชื่อเพลงจาก tag kbps/kHz/ช่องเสียง มาจาก VLC
- วิดีโอ: กฎไม่เล่นเกิน 720p และไม่เล่น AV1 ตรวจเมื่อรู้ขนาดภาพจาก VLC แล้วปฏิเสธแบบเดียวกัน ความเร็ว เลือกแทร็กเสียง และคำบรรยาย (VLC วาดคำบรรยายบนภาพเอง เลือก/ปิดจากปุ่มเดิม) หมุนจอและเต็มจอใช้หน้าจอเดิม จำตำแหน่งที่ดูค้างใช้ที่เดิม เริ่มเล่นจากตำแหน่งนั้น
- ความร้อน การพักคำปลุก (WakePause) คำสั่งเสียง โหมดไฟล์เดียว และ playlist ใช้ทางเดียวกับ Media3 ทั้งหมด service เลือกเครื่องเล่นให้ หน้าจอไม่ต้องรู้
- **audio focus (0.61.0, Poom: "ทำให้ครบ"):** `VlcDeck` ขอ focus จากเครื่องเหมือน Media3 ตอนเริ่มเล่น แอปอื่นเอาไปถาวร หรือถอดหูฟัง → หยุดชั่วคราวเหมือนผู้ใช้กดหยุด เสียงสั้นของแอปอื่น (เสียงนำทาง สายเรียกเข้า) → หยุดแล้วเล่นต่อเอง เสียงที่ยอมให้หรี่ → เหลือ 20% (เท่า Media3) จาร์วิสพูด → หยุดผ่าน WakePause ทางเดียวกับ Media3 (มีมาตั้งแต่ 0.60.0) ผลพลอยได้: วิดีโอ VLC เริ่มเล่นแล้วเพลงที่เล่นผ่าน Media3 หยุด เหมือนวิดีโอ Media3 ทำอยู่แล้ว
- **จุดเสียในไฟล์:** VCD ที่ rip จากแผ่นมีรอยอาจมีช่วงข้อมูลเสีย VLC จะหยุดและแจ้งว่าไฟล์จบ ทั้งที่ยังเหลืออีกมาก (คู่โจร1 หยุดที่ 4:07 จาก 1:05:07) ถ้าแจ้งจบก่อนความยาวจริงเกิน 10 วินาที ให้เปิดไฟล์ใหม่ถัดจากจุดนั้นแล้วเล่นต่อ ครั้งแรกข้าม 2 วินาที ถ้าจบซ้ำในช่วงเสียเดียวกันให้ข้ามเป็นสองเท่าทุกครั้ง (2, 4, 8, 16, 32 วินาที) โดยนับจากจุดที่ไปถึงไกลที่สุด เพราะช่วงเสียอาจทำให้นาฬิกาของ VLC ถอยหลัง (คู่โจร2 มีช่วงเสีย 12 วินาทีตอนต้นเรื่อง) ทำได้ไม่เกิน 20 ครั้งต่อไฟล์ ไฟล์ที่เสียยาวไปจนจบจึงยังจบได้ (`media/EarlyEnd.kt`)
- **สัดส่วนภาพ VCD/MPG:** VLC ไม่บอกขนาดภาพของ VCD หน้าจอจึงค้างที่ 16:9 และภาพ 4:3 ถูกวาดเล็กอยู่ข้างใน ให้อ่านขนาดและ aspect จาก sequence header ของ MPEG ในไฟล์เอง (`media/MpegSniff.kt`, อ่านไม่เกิน 2 MB แรก) แล้วใช้ขนาดนั้นกับกรอบภาพ
- **เริ่มจากตำแหน่งที่ดูค้าง:** ไม่ใช้ option `:start-time` ของ VLC เพราะ VCD ไม่สนใจค่านี้และเริ่มที่ 0:00 เสมอ (พบบน A07) ให้สั่ง seek ด้วย `time` เมื่อไฟล์เริ่มเล่นและ seek ได้แล้ว ถ้าเล่นต่อจากจุดเสียแล้วไม่ไปข้างหน้ากว่าเดิม ให้ถือว่าจบ ไม่วนซ้ำ
- NAS: VLC เปิดไฟล์บน NAS ผ่าน SMB2 ของตัวเอง รหัสผ่านส่งเป็น option ไม่อยู่ในที่อยู่ ไม่ลง log (ยังไม่ได้ทดสอบกับ NAS จริง)

**เพดาน APK: 120 MiB** (Poom) ตรวจใน CI

## 5ฎ. ลบต้องยืนยันตัวตน (0.60.0, Poom)

- **ทุกทางที่ลบได้ต้องผ่านใบหน้าหรือ pattern ก่อน** (`auth/IdentityGate`, ตัวเดียวกับการลบใบหน้าในหน้ายืนยันตัวตน): ลบไฟล์หรือโฟลเดอร์ในตัวจัดการไฟล์ (ทั้งจากปุ่มในหน้ารายละเอียดและจากเมนูแตะค้าง ซึ่งพาไปหน้าเดียวกัน) ลบการตั้งค่า NAS ลบ playlist และ "ล้าง" playlist
- ลำดับ: ถาม "แน่ใจไหม" ในที่เดิมก่อน แล้วเปิดหน้ายืนยันตัวตน ผ่านจึงลบ ไม่ผ่าน ยกเลิก หรือยังไม่ได้ตั้งใบหน้า/pattern → ไม่ลบ และบอกเหตุผล
- รอบ 0.59.0 และก่อนหน้า ลบไฟล์หรือ playlist ได้โดยไม่ต้องยืนยันตัวตน (ปุ่มลบเดิมถามแค่ "แน่ใจไหม") พบในรอบ 0.60.0 และแก้แล้ว `DeleteGateTest` ตรวจทุกทาง

## 5ฒ. กล้องถ่ายรูป (0.61.0, Poom: แอปพื้นฐานเหมือนมือถือทั่วไป)

- ไม่ใช่ "แอปกล้อง" เดิม (`voice/CameraAppLauncher` เปิด Xiaomi Home ดูกล้องวงจรปิด) หน้านี้คือกล้องของมือถือเอง สำหรับถ่ายรูป ใช้ CameraX ที่มีอยู่แล้วจากหน้ายืนยันตัวตน APK ไม่โตขึ้น
- เปิดจากแผงควบคุม (ไอคอน `ic_pixel_camera`) หน้าต่างเดียว: ภาพจากกล้องเต็มพื้นที่ แถว [สลับกล้อง (ตอนนี้: กล้องหลัง)] [ดูรูปล่าสุด] และปุ่มหลัก "ถ่ายรูป" 56dp ปุ่มบอกเป็นคำ ไม่ใช่ไอคอนอย่างเดียว
- เริ่มที่กล้องหลัง สลับหน้า/หลังได้ เครื่องที่มีกล้องเดียวปุ่มสลับเป็นสีจาง
- รูปไปที่ `DCIM/Kiosk` ผ่าน MediaStore (Android 10+ ไม่ต้องขอสิทธิ์ที่เก็บไฟล์) ตัวจัดการไฟล์เห็น และเปิดด้วยตัวดูรูปเดิมได้ ข้อความยืนยันบอกว่าเก็บที่ไหน
- กล้องเปิดเฉพาะตอนหน้านี้อยู่ข้างหน้า ออกจากหน้า จอดับ หรือ Hey Jarvis ปิดทุกหน้า (ข้อ 11) = ปล่อยกล้องทันที
- log บอกแค่สถานะ (bound/closed/saved/failed และเลนส์) ไม่มีชื่อไฟล์หรือภาพ **ห้ามแคปหน้าจอตอนกล้องเปิด** ตรวจด้วย log

## 5ฐ. บันทึกเสียง (0.61.0, Poom)

**หน้าที่:** บันทึกเสียง หยุดชั่วคราว บันทึกต่อ หยุดและเก็บไฟล์ ฟัง เปลี่ยนชื่อ และลบ เป็นหน้าจอของแอปนี้ (`recorder/RecorderActivity`) เข้าจากไอคอนไมโครโฟน `ic_pixel_mic` ในแผงควบคุม ไม่มีคำสั่งเสียง

**ไฟล์:** เก็บในเครื่องเท่านั้น ที่ `Recordings/Kiosk` ของหน่วยความจำเครื่อง (ตัวจัดการไฟล์แสดง "เครื่อง › Recordings › Kiosk") ไม่ส่งออกจากเครื่อง
- **รูปแบบ:** Opus ใน Ogg (`.ogg`) โมโน 16 kHz 24 kbit/s ราว 11 MB ต่อชั่วโมง Opus ทำมาสำหรับเสียงพูดที่อัตราต่ำ 16 kHz ครอบเสียงพูดทั้งหมด ใช้ตัวเข้ารหัส Opus และตัวเขียน Ogg ของ Android เอง (API 29+, `recorder/MemoWriter`) ตัวเดียวกับที่ MediaRecorder ใช้ข้างใน
  - ไฟล์ Ogg ที่ถูกตัดกลางทาง (แอปตาย) ยังเล่นได้ถึงหน้าสุดท้ายที่เขียน ส่วน MP4 ที่ไม่ได้ปิดเล่นไม่ได้เลย
  - **สำรอง:** ครั้งแรกของ process ลองเข้ารหัสความเงียบ 0.1 วินาที ถ้า Opus/Ogg ใช้ไม่ได้บนเครื่อง ใช้ AAC ใน `.m4a` 32 kbit/s แทน log บอก `KioskRec: rec file open format=ogg|m4a`
  - ทั้ง `.ogg` และ `.m4a` เป็นเพลงใน `media/MediaKinds` ตัวจัดการไฟล์จึงเปิดด้วยเครื่องเล่นเพลงแบบไฟล์เดียว (ข้อ 5ฌ)
- **ชื่อ:** "บันทึก 2026-09-25 14.03" (เรียงตามวันที่ได้ด้วยตัวอักษร) ชนในนาทีเดียวกันเป็น "… (2)" เปลี่ยนชื่อ: ห้ามว่าง ห้ามมี `/ \ : * ? " < > |` ห้ามขึ้นต้นด้วยจุด ไม่เกิน 40 ตัวอักษร (เท่า playlist) ห้ามซ้ำกับการบันทึกอื่นโดยไม่สนตัวพิมพ์ นามสกุลเดิมคงไว้ ข้อผิดพลาดขึ้นใต้ช่องและไม่ล้างสิ่งที่พิมพ์

**ไมโครโฟนตัวเดียว (ตัดสินเอง 0.61.0):** ตัวบันทึก**ไม่เปิดไมโครโฟนเอง** อ่านเฟรมเสียงชุดเดียวกับที่ตัวจับคำปลุกได้ยิน (`voice/MicTap`, 16 kHz โมโน)
- เหตุผล: `VoiceService` ถือ AudioRecord ตลอดเวลา และบทเรียน versionCode 4 คือห้ามเปิดสองตัวพร้อมกัน กติกาการอัดพร้อมกันของ Android 10+ (ใครได้เสียง ใครได้ความเงียบ) ขึ้นกับ source (VOICE_RECOGNITION กับ MIC) ลำดับที่เริ่ม และ HAL ของผู้ผลิต ถ้าพลาดจะได้ไฟล์เงียบ ส่วนทางปิดไมค์ของจาร์วิสแล้วส่งต่อ ทำให้ปุ่มจาร์วิสใช้ไม่ได้ระหว่างบันทึก ซึ่งขัดข้อ 12
- ระหว่างรอบคำถามของจาร์วิส (ฟัง คิด พูด) ไม่มีเฟรมไปถึงตัวบันทึก คำถามจึงไม่อยู่ในไฟล์
- `dumpsys` ของ VoiceService บรรทัด `mic-tap :` บอกว่าตัวบันทึกอ่านอยู่ไหม และนับเฟรม

**คำปลุกพักระหว่างบันทึก (Poom, ล็อกแล้ว):** `WakePause.Source.RECORDER` การ์ดจาร์วิสขึ้น "จาร์วิส · พักระหว่างบันทึกเสียง" (`recorder/RecorderFlow`, มี test)
- จับ hold ตอนเริ่มและตอนบันทึกต่อ ต่ออายุทุก 30 วินาที **ปล่อยทันที**เมื่อหยุด หยุดชั่วคราว ปิดหน้าจอ หรือเกิดข้อผิดพลาด lease ที่หลุดระหว่างยังบันทึกอยู่จะถูกจับใหม่ในรอบต่ออายุถัดไป แอปตาย = lease หมดใน 90 วินาทีตามข้อ 12
- **หยุดชั่วคราว = ปล่อย hold (ตัดสินเอง):** แบบเดียวกับเครื่องเล่นเพลง เพราะตอนหยุดชั่วคราวไม่มีอะไรถูกบันทึกให้คำปลุกทำเสีย และ Poom สั่งให้พักเฉพาะตอนอัด ถ้าพูด Hey Jarvis ตอนหยุดชั่วคราว หน้าจอปิดตามข้อ 11 และไฟล์ถูกเก็บครบ ไม่มีอะไรหาย
- **ปุ่มจาร์วิส (`quietForJarvis`) = หยุดและเก็บไฟล์** เพราะหน้าจอจะถูกปิดตามข้อ 11 อยู่แล้ว ไม่บันทึกต่อเองหลังตอบ
- เริ่มบันทึกแล้วเพลงที่เล่นอยู่หยุดชั่วคราว (เสียงเดียวในห้อง ไม่งั้นเพลงจากลำโพงเข้าไฟล์)

**ออกจากหน้าจอ = หยุดและเก็บไฟล์ทันที ไม่มีวันหาย:** X, "กลับหน้าหลัก", Back และ Hey Jarvis (ข้อ 11) ทุกทางจบที่ `onPause` แบบ finishing แล้วเรียก `VoiceMemo.stop` hold ถูกปล่อยก่อนหน้าถัดไปขึ้น
- **จอดับแล้วยังบันทึกต่อ (ตัดสินเอง):** จอดับไม่ได้ปิดหน้าจอ และไมโครโฟนของ VoiceService ทำงานตอนจอดับอยู่แล้ว การประชุมหรือเรื่องเล่ายาวจึงไม่ขาดเพราะจอดับ หน้าจอไม่ได้บังคับให้จอเปิดค้าง
- พื้นที่ในเครื่องเหลือต่ำกว่า 50 MB ระหว่างบันทึก → หยุดและเก็บไฟล์เอง พร้อมบอกเหตุผล

**หน้าจอ:** หน้าต่างแบบเดียวกับตัวจัดการไฟล์ (แถบหัวข้อกรมท่า ไอคอนไมโครโฟน ปุ่ม X 48dp "กลับหน้าหลัก" 56dp ล่างสุด) ขนาดทั้งหมดจาก `UiScale` (ข้อ 5ช)
- กล่องลึกบนสุด: สถานะเป็นคำ ("พร้อมบันทึก" / "กำลังบันทึก" / "หยุดชั่วคราว" / "กำลังเก็บไฟล์…") 16sp ตัวหนา, เวลาที่บันทึก `TEXT_DISPLAY` 24sp, "ระดับเสียง" กับแถบระดับ (แถบกรมท่าในช่องขาว สูง 24dp สเกลเดซิเบล −50 ถึง 0 dBFS) ถ้าไม่มีเฟรมเกิน 2 วินาทีขึ้นข้อความสีแดงว่ายังไม่ได้รับเสียง
- ปุ่มใหญ่ 64dp มีไอคอนเหนือคำ: พร้อม → [● เริ่มบันทึก] (`ic_pixel_record` จุดแดง `#CC0000` ของแบตเตอรี่ต่ำ) · กำลังบันทึก → [หยุดชั่วคราว][หยุดและเก็บไฟล์] · หยุดชั่วคราว → [บันทึกต่อ][หยุดและเก็บไฟล์]
- ระหว่างบันทึกมีบรรทัดบอกว่าจาร์วิสไม่ฟังคำปลุก และออกจากหน้านี้ = หยุดและเก็บไฟล์ ไม่มีอะไรหาย ตอนพร้อมบอกที่เก็บไฟล์
- ปุ่มเริ่มจางและบอกเหตุผลเมื่อบันทึกไม่ได้: ยังไม่ได้สิทธิ์ "เข้าถึงไฟล์ทั้งหมด" หรือไมโครโฟนของจาร์วิสไม่ได้เปิด
- **รายการ** "รายการบันทึก (N)" ใหม่สุดก่อน แต่ละแถว: ไอคอน `ic_pixel_type_audio` ชื่อ (ไม่มีนามสกุล) 15sp แล้ว "Fri 25/09/2026 2:03 PM · 1:23 · 240 KB" (วันที่และเวลาแบบข้อ 7) ความยาวอ่านบน worker ถ้ายังไม่รู้ตัดช่องนั้นทิ้ง ระหว่างบันทึกแถวจางและกดไม่ได้พร้อมบรรทัดบอกเหตุผล ไฟล์ที่กำลังเขียนไม่อยู่ในรายการ
- แตะแถว → รายละเอียด: วันที่บันทึก ความยาว ขนาด อยู่ที่ ปุ่มหลัก "เล่น" 56dp (เครื่องเล่นเพลงแบบไฟล์เดียว ไม่แตะ playlist ข้อ 5ฌ) แล้ว [เปลี่ยนชื่อ][ลบ]
- **ลบ (ข้อ 5ฎ):** ถาม "ลบ “…” ใช่หรือไม่…" ในที่เดิม → [ยืนยันตัวตนแล้วลบ] เปิดหน้ายืนยันตัวตน ผ่านจึงลบ ไม่ผ่าน ยกเลิก หรือยังไม่ได้ตั้ง → ไม่ลบและบอกเหตุผล (`RecorderScreenTest`)

**log (`logcat -s KioskRec:I`):** สถานะและตัวเลขเท่านั้น ห้ามชื่อไฟล์หรือ path: `rec start holding=true`, `mic handed to recorder`, `rec pause ms=…`, `rec stop ms=… reason=button|screen-closed|jarvis|no-space|error`, `mic back frames=… held_back=…`, `rec saved ms=… ok=true`, `rec file open format=ogg`

## 5ฑ. Google Drive (0.61.0, Poom)

**ต่อตรงจากมือถือ ไม่ผ่าน broker** (CLAUDE.md ข้อ 3) เป็นหน้าจอของแอปเอง `drive/DriveActivity` เข้าจากปุ่ม "Google Drive" (ไอคอนเมฆขาวมีลูกศรกรมท่า `ic_pixel_drive` วาดเอง ไม่ใช่โลโก้ของ Google) ในหน้าเลือกที่เก็บของตัวจัดการไฟล์ ถัดจาก NAS หน้าตาและการเดินเหมือนตัวจัดการไฟล์ทุกอย่าง: หน้าต่างเดียวกัน โฟลเดอร์มาตรฐาน `FolderBrowser` (ผ่าน `drive/DriveSource`) breadcrumb เริ่มที่ "Drive ของฉัน" Back ถอยทีละชั้น ชั้นบนสุดกลับไปหน้าเลือกที่เก็บ

**ทำอะไรได้:** เปิดโฟลเดอร์ (โฟลเดอร์ก่อน เรียงชื่อ อ่านทุกหน้าของรายการ ไม่เกิน 10,000 แถว) สร้างโฟลเดอร์ เปลี่ยนชื่อ ย้ายไปถังขยะ ดาวน์โหลดลงเครื่องที่ `Download/Drive` (ตัวจัดการไฟล์เห็นทันที ชื่อซ้ำตั้ง "ชื่อ (2)") อัปโหลดจากเครื่อง: หน้ารายละเอียดของไฟล์ในเครื่องมีปุ่ม "อัปโหลดไป Google Drive" แล้วเลือกโฟลเดอร์ปลายทางใน Drive ด้วยแถบกรมท่า [อัปโหลดที่นี่][ยกเลิก] (ไฟล์เท่านั้น ยังไม่อัปโหลดทั้งโฟลเดอร์)
- **เพลง วิดีโอ รูป:** แตะแล้วดาวน์โหลดลง cache ของแอปก่อน แล้วเปิดในเครื่องเล่นหรือตัวดูรูปของ kiosk **แบบไฟล์เดียว** (ข้อ 5ฌ ไม่แตะ playlist) cache เก็บแค่ไฟล์ล่าสุดไฟล์เดียว ไฟล์ที่ต้องใช้ VLC จึงเล่นได้เหมือนไฟล์ในเครื่อง
- **ไฟล์ของ Google (Docs ชีต สไลด์ ฟอร์ม ทางลัด):** แสดงเป็นแม่กุญแจ "ระบบสงวนไว้" ไม่มีปุ่มเปิดหรือดาวน์โหลด หน้ารายละเอียดบอกว่าเปิดได้เฉพาะในโปรแกรมของ Google เปลี่ยนชื่อและย้ายไปถังขยะได้
- งานยาวใช้หน้าความคืบหน้าแบบเดียวกับตัวจัดการไฟล์ พร้อมปุ่ม "หยุด" **หยุดแล้วไม่เหลือของครึ่ง ๆ:** ดาวน์โหลดเขียนเป็น `.part` แล้วเปลี่ยนชื่อเมื่อครบ อัปโหลดแบบ resumable ไฟล์จึงเกิดใน Drive เมื่อได้ครบทุกไบต์เท่านั้น

**ลบ = ย้ายไปถังขยะของ Drive เท่านั้น (Poom ล็อก):** `files.update trashed=true` กู้คืนได้ที่ drive.google.com ภายใน 30 วัน ไม่มีคำสั่งลบถาวรหรือล้างถังขยะในแอปเลย และไม่มี HTTP DELETE (`DriveTest` อ่านโค้ดตรวจ) ลำดับเหมือนข้อ 5ฎ: ถาม "ย้าย … ไปถังขยะของ Google Drive หรือไม่" ในที่เดิม แล้วยืนยันตัวตน ผ่านจึงย้าย

**สิทธิ์และ token:** ขอผ่าน Google Identity Services `AuthorizationClient` ขอ scope `…/auth/drive` (เต็ม) ไม่มี client id หรือ secret ในแอป Google จับคู่กับ OAuth client แบบ Android ใน Cloud Console ด้วยชื่อแพ็กเกจกับ SHA-1 ของใบรับรองที่เซ็น APK **token อยู่ในหน่วยความจำเท่านั้น** ไม่เขียนลงไฟล์ ไม่ลง log (ขอใหม่แบบเงียบจาก Google Play services ทุกครั้งที่หมดอายุ) สิ่งเดียวที่บันทึกคือค่าจริง/เท็จ "เจ้าของกดยกเลิกการเชื่อมต่อ"
- **หน้าขออนุญาตครั้งแรกเป็นหน้าจอของ `com.google.android.gms`** ซึ่ง lock task ไม่ให้ขึ้น ต้องเพิ่มแพ็กเกจนี้ชั่วคราวระหว่างหน้านั้นแบบปุ่ม WiFi (ข้อ 5ก) โค้ดทำไว้แล้วแต่ปิดด้วย `DriveAuth.SIGN_IN_ALLOWED = false` **รอ Poom อนุมัติ** เพราะเป็นการขยายขอบเขตของ kiosk ระหว่างที่ปิดอยู่ ปุ่ม "เชื่อมต่อ" ไม่เปิดอะไร และบอกว่าผู้ดูแลระบบต้องอนุมัติก่อน
- **ยังไม่ได้เชื่อมต่อต้องบอกเหตุผล ห้ามค้าง "กำลังโหลด":** "ยังไม่ได้เชื่อมต่อ Google Drive" แล้วเหตุผลเป็นคำ (`drive/DriveStatus`): ต้องอนุญาตครั้งแรก / ผู้ดูแลยังไม่อนุมัติ / Google ยังไม่รู้จักแอป (ยังไม่ได้สร้าง client) / ไม่มีบัญชี Google / ไม่ได้ต่ออินเทอร์เน็ต / Google Play ไม่พร้อม / Google ไม่ตอบ / ยกเลิกการเชื่อมต่อแล้ว / ปิดหน้าขออนุญาต / อื่น ๆ ทุกการรอ Google มีเพดาน 20 วินาที ทุกคำขอ Drive มี timeout ต่อ 15 วินาที อ่าน 30 วินาที
- **"ยกเลิกการเชื่อมต่อ Google Drive"** อยู่ที่ชั้น "Drive ของฉัน" ถามยืนยันในที่เดิม แล้วหยุดใช้ทันทีในเครื่อง ล้าง token และขอ Google ถอนสิทธิ์ (`revokeAccess`) ถ้าแจ้ง Google ไม่สำเร็จ บอกว่าถอนเองได้ที่ myaccount.google.com/permissions
- log (`KioskDrive`) มีแค่ชนิดงาน สถานะ และรหัส HTTP ห้ามชื่อไฟล์ path บัญชี หรือ token

**ขนาด APK:** `play-services-auth` เพิ่มราว 1 MB (ไลบรารีส่วนใหญ่ที่ต้องใช้มากับ ML Kit อยู่แล้ว) เรียก Drive ด้วย HttpURLConnection + org.json ไม่ใช้ google-api-client
- **หน้าขออนุญาตของ Google (Poom อนุมัติ 2026-09-25):** `SIGN_IN_ALLOWED = true` Play services เข้า lock task เฉพาะตอนหน้านั้นเปิด ถอนทันทีเมื่อ Google ตอบ (เชื่อมเสร็จ) หรือยกเลิก หรือครบ 5 นาที (`CONSENT_MAX_MS` ปิดหน้าของ Google ด้วย) log `KioskDrive: consent allowlist added / withdrawn reason=answered|cancelled|timeout` และทุกครั้งที่หน้า Drive กลับมาหรือแอปเริ่ม รายการกลับเป็น 3 แพ็กเกจเดิม
- **Cloud project:** ใช้ project เดิมของปฏิทิน (Poom แก้ 2026-09-25)
- **5 นาที ตัดสินด้วย `ConsentWindow` (นาฬิกาส่งเข้าไป ทดสอบด้วยเวลาจำลองใน DriveTest):** นอกจากตัวจับเวลา หน้า Drive ตรวจซ้ำทุกครั้งที่กลับมา (`withdrawIfExpired`) นาฬิกาถอยหลังถือว่าหมดเวลา

## 5ฏ. วิทยุ (0.61.0, Poom)

**หน้าที่:** ฟังวิทยุออนไลน์ของไทย เข้าจากไอคอน "วิทยุ" ในแผงควบคุม (`radio/RadioActivity`) เล่นต่อในเบื้องหลังเหมือนเพลง
- **ตัวเล่นอยู่ใน `radio/RadioService` (foreground service ชนิด mediaPlayback) ไม่ใช่ในหน้าจอ** ปิดหน้าวิทยุด้วย Back, X หรือ Hey Jarvis วิทยุเล่นต่อ การแจ้งเตือนบอกชื่อสถานีและสถานะ มีปุ่ม "หยุด" แตะแล้วเปิดหน้าวิทยุ
- **จาร์วิสพักผ่าน `WakePause` แบบเดียวกับเพลง:** hold ตั้งแต่กดเล่น (รวมช่วงกำลังเชื่อมต่อ) ต่ออายุทุก 30 วินาที ปล่อยเมื่อหยุดหรือเล่นไม่ได้ การ์ดจาร์วิสบอก "พักระหว่างเล่นวิทยุ" (`WakePause.Source.RADIO`)
- **จาร์วิสพูดทับแล้ววิทยุลดเสียง (Poom):** ถามด้วยปุ่มจาร์วิสระหว่างวิทยุเล่น วิทยุ **ไม่หยุด** แต่ลดเหลือ 15% ของเสียงตั้งแต่เริ่มฟังคำถามจนจาร์วิสตอบจบ แล้วกลับเป็นเสียงเดิม (`RadioService.DUCK`) ต่างจากเพลงและวิดีโอที่หยุดชั่วคราว เพราะวิทยุเป็นรายการสด หยุดแล้วไม่มีที่ให้เล่นต่อ
- **เสียงเดียวในเวลาเดียวกัน:** เริ่มวิทยุแล้วเพลงหรือวิดีโอที่เล่นอยู่หยุดชั่วคราว (ทางเดียวกับที่เพลงกับวิดีโอหลีกกัน) เริ่มเพลงหรือวิดีโอแล้ววิทยุ **หยุด** (ไม่มีหยุดชั่วคราวสำหรับรายการสด) audio focus ใช้ของ ExoPlayer แอปอื่นเริ่มเล่นเสียงหรือถอดหูฟัง วิทยุหยุดและบอกเหตุผล
- **ไม่ค้างและไม่ลองซ้ำไม่หยุด:** กดเล่นแล้ว 20 วินาทียังไม่มีเสียง หรือเล่นอยู่แล้วสัญญาณขาดเกิน 30 วินาที วิทยุหยุดพร้อมเหตุผล ตัวเล่นลองโหลดซ้ำเองไม่เกิน 2 ครั้ง จากนั้นรอคนกดเล่นใหม่ ไม่มีการลองซ้ำอัตโนมัติ
- **ไม่ log ชื่อสถานี ที่อยู่ หรือชื่อเพลง:** `logcat -s KioskRadio:I` มีแค่ลำดับของสถานีในรายการ ("station #3") ชนิดของ stream สถานะ และชนิดข้อผิดพลาด (`RadioScreenTest`)
- **เครื่องร้อน:** ขั้นหยุดชั่วคราวและขั้นหยุดของ `HeatWatch` หยุดวิทยุ (เหมือนเพลง) และกดเล่นไม่ได้จนกว่าเครื่องจะเย็นลง

**หน้าต่าง (กรอบแผงควบคุม, ขนาดทั้งหมดจาก `UiScale`):**
- แถบหัวข้อ `ic_pixel_radio_light` "วิทยุ" ปุ่ม X 48×48dp ล่างสุด "กลับหน้าหลัก" 56dp
- **จอดำตัวเขียว** (สีจอ LCD ของเครื่องเล่นเพลง): ชื่อสถานี 18sp ตัวหนา บรรทัดเดียว ใต้ลงมาเป็นสถานะเป็นคำเสมอ "หยุดอยู่" / "กำลังเชื่อมต่อ…" / "กำลังเล่น" / "เล่นไม่ได้: เหตุผล" (เหตุผลเป็นสีส้ม `amp_bar_high` บนดำ แต่คำบอกอยู่แล้ว) และ "เพลง: …" เมื่อสถานีส่งชื่อเพลงมา (ICY)
- **ปุ่มใหญ่ปุ่มเดียว** เต็มความกว้าง 56dp รูปและคำ "► เล่น" / "■ หยุด" เล่นสถานีที่เลือกล่าสุด ถ้ายังไม่เคยเลือก ปุ่มจางและจอบอกให้แตะชื่อสถานี
- แท็บ 48dp "สถานี" · "ค้นหาสถานีไทย"
- **รายการสถานี:** แถวสูงอย่างน้อย 56dp ปุ่มดาว ★/☆ 48×48dp ชิดซ้าย (เครื่องอ่านหน้าจอได้ยิน "ติดดาว"/"ถอดดาว" และชื่อสถานี) **สถานีติดดาวอยู่บนสุด** ที่เหลือเรียงตามลำดับที่เพิ่ม แตะแถวเพื่อเล่น แตะแถวที่เล่นอยู่เพื่อหยุด แถวที่เล่นอยู่เป็นพื้นกรมท่าตัวขาว มี "►" และสถานะใต้ชื่อ
- **แก้ไขรายการ:** ปุ่ม "แก้ไขรายการ" / "เสร็จ" และ "เพิ่มสถานีเอง" อยู่บนรายการ ในโหมดแก้ไขแต่ละแถวมี "แก้ไข" (ชื่อและที่อยู่) และ "เอาออก" ซึ่งถามยืนยันในแถวเดียวกันก่อน **เอาสถานีออกจากรายการไม่ได้ลบไฟล์ของใคร จึงไม่ต้องยืนยันตัวตน** (ต่างจากข้อ 5ฎ)
- **เพิ่มหรือแก้ไข:** ชื่อ (ไม่เกิน 40 ตัวอักษร) และที่อยู่ของเสียง (https:// หรือ http:// ตามด้วยชื่อเว็บ) ผิดตรงไหนบอกในหน้าเดียวกันด้วยสี `retro_bad` ที่อยู่ซ้ำกับสถานีที่มีอยู่แล้วบันทึกไม่ได้
- **ค้นหาสถานีไทย:** ถาม Radio Browser (ฐานข้อมูลสถานีวิทยุแบบเปิด ไม่ต้องใช้ key) เฉพาะสถานีไทยที่ตรวจล่าสุดว่าใช้ได้ เรียงตามคะแนนโหวต ครั้งละไม่เกิน 100 สถานี ค้นเองครั้งแรกที่เปิดแท็บ หลังจากนั้นค้นเมื่อกด "ค้นหา" เท่านั้น ใส่ชื่อเพื่อกรองได้ แต่ละแถวบอกชนิดเสียง kbps และจำนวนโหวต ปุ่ม "เพิ่ม" หรือ "อยู่ในรายการแล้ว" ค้นไม่ได้บอกว่าต่อไม่ได้ ไม่ค้าง
- ทุกสถานะมีคำ: รายการว่าง (บอกทางเพิ่มสองทาง) / กำลังค้นหา / ค้นไม่ได้ / ไม่พบ / พบ n สถานี

**รายการสถานี (`radio/RadioBook`, `radio/RadioStore`):**
- เก็บใน `files/radio_stations.json` ของแอป ครั้งแรกคัดลอกจาก `assets/radio_stations.json` ในแอป (สถานีดังที่วัดแล้วว่าเล่นได้ ตาราง `tools/radio/RESULTS.md`) หลังจากนั้นเป็นของผู้ใช้ อัปเดตแอปไม่ทับ ไฟล์ที่อ่านไม่ได้ถูกเก็บไว้เป็น `radio_stations.bad.json` ไม่ลบ แล้วใช้รายการตั้งต้น
- ไม่เกิน 200 สถานี รายการที่เสียบางแถวข้ามเฉพาะแถวนั้น (`RadioBookTest`)

**http ยังเล่นไม่ได้ (รอ Poom ตัดสิน):** แอปนี้ไม่อนุญาตการเชื่อมต่อแบบไม่เข้ารหัส (ค่าเริ่มต้นของ Android) สถานีที่ส่งเสียงทาง http:// จึงเล่นไม่ได้ จอบอก "สถานีนี้ส่งเสียงแบบไม่เข้ารหัส (http) ซึ่งแอปนี้ยังไม่อนุญาต" แถวในรายการมีหมายเหตุ "http: ยังเล่นไม่ได้" ผลค้นหาซ่อนสถานี http และบอกจำนวนที่ซ่อน รายการตั้งต้นมีแต่ https

**ไอคอน:** `ic_pixel_radio` วิทยุพกพา เสาอากาศ ตัวเครื่องเทาเข้ม `#808080` ลำโพงกลมมีรู หน้าปัดทอง `#FFD700` มีเข็ม ปุ่มหมุนสองปุ่ม (บนพื้นเทาในแผงควบคุม, 3 สี) และ `ic_pixel_radio_light` แบบสีขาวกับหน้าปัดทอง บนแถบกรมท่าและการแจ้งเตือน วาดเองจากสี่เหลี่ยมบนกริด 16×16 ไม่ใช่วิทยุหรือโลโก้ของยี่ห้อใด (`RadioScreenTest`)

- **http (Poom 2026-09-25):** แอปไม่อนุญาต http ทั้งแอป ยกเว้นโดเมนของสถานีในรายการเริ่มต้นของแอปเท่านั้น (`res/xml/network_security_config.xml` = `StreamKind.PLAIN_ALLOWED_HOSTS`, ตอนนี้คือ ลูกทุ่งเน็ตเวิร์ค `media.login.in.th`) สถานี http ที่เพิ่มเองหรือค้นเจอ ยังขึ้น "http: ยังเล่นไม่ได้" สถานีใหม่ในรายการเริ่มต้นถูกเพิ่มให้เครื่องที่มีรายการอยู่แล้วครั้งเดียว สถานีที่เจ้าของเอาออกจะไม่กลับมา
## 5ณ. เวลา: นาฬิกาปลุก · จับเวลา · นับถอยหลัง (0.63.0, Poom; เดิม 0.62.0)

**0.63.0 (Poom): รวมเป็นแอปเดียว "เวลา"** ไอคอนเดียวในโฟลเดอร์เครื่องมือ (`timer/TimerActivity`) 3 หน้าแบบปัด ด้วย `calc/SlideDeck` ตัวเดียวกับเครื่องคิดเลข (ผ่าน interface `calc/SlideHost`) ■ □ □ ในแถบหัวข้อ: นาฬิกาปลุก · จับเวลา · นับถอยหลัง
- **หน้านาฬิกาปลุก** แสดงรายการ เปิด/ปิด ลบ (ถามครั้งเดียวในที่) ผ่าน `AlarmStore` เหมือนเดิม · เพิ่ม/แก้ไข เปิด **ตัวแก้ของแผงควบคุมตัวเดิม** เฉพาะการปลุกนั้น (`SettingsActivity.EXTRA_EDIT_ALARM`) บันทึก ยกเลิก Back และ X กลับมาหน้าเวลา — ปลุกที่ตั้งไว้ การดังตอนจอดับ คำสั่งเสียง ใช้โค้ดเดิมทั้งหมด (ตรวจ: ไฟล์ปลุกไม่เปลี่ยนหลังติดตั้ง)
- หมดเวลานับถอยหลัง → เปิดหน้านับถอยหลัง (ไม่มีหน้าให้ปัดระหว่างดัง) · ปุ่มปิดกลับหน้าที่มา
- **คำสั่งเสียง (0.63.0):** "จับเวลาห้านาที" / "นับถอยหลัง 30 วินาที" / "ตั้งเวลา 1 ชั่วโมงครึ่ง" เริ่มนับ (ความยาวใหม่แทนที่ของเดิม) · "หยุดจับเวลา" · "นับถอยหลังเหลือเท่าไหร่" มือถือตอบเวลาที่เหลือเอง · "จับเวลาหน่อย" ถามกลับว่ากี่นาที · ไม่เกิน 24 ชั่วโมง · ปุ่มจาร์วิสหน้านี้: "ห้านาที" "หยุด" "เหลือเท่าไหร่" (broker `timers.py`, มือถือ `timer/TimerVoice`)

### เดิม (0.62.0)

**หน้าที่:** นาฬิกาจับเวลาและนับถอยหลังในแอปเดียว (`timer/TimerActivity`) สลับด้วยแถวตัวเลือก ● นาฬิกาจับเวลา ○ นับถอยหลัง บนสุด เปิดจากไอคอน `ic_pixel_stopwatch` ในแผงควบคุม หมวด "ในบ้านและใช้บ่อย" ถัดจากนาฬิกาปลุก ไม่มีคำสั่งเสียง หน้าต่างแบบเดียวกับบันทึกเสียง (`ui/ToolWindow`: แถบหัวข้อกรมท่า X 48dp ตาม `ui/Origin` "กลับหน้าหลัก" 56dp ล่างสุด) ขนาดทั้งหมดจาก `UiScale`
- **จอตัวเลข:** พื้นดำ ตัวเขียว `retro_lcd` Press Start 2P `UiScale.LCD_DIGITS` 32sp (ตัวเลขล้วน ข้อยกเว้นแบบนาฬิกา taskbar ข้อ 3) ใต้ตัวเลขเป็นสถานะเป็นคำ 16sp ตัวหนา ("พร้อมจับเวลา" / "กำลังจับเวลา" / "หยุดชั่วคราว" / "ตั้งเวลา" / "กำลังนับถอยหลัง") และระหว่างนับถอยหลัง "ครบเวลาตอน 2:35 PM" สีจาง
- **นาฬิกาจับเวลา:** "05:07.3" (ถึงทศนิยมหนึ่งตำแหน่ง ตัดทิ้งไม่ปัด) เกินชั่วโมงเป็น "1:05:07.3" ปุ่มหลัก 56dp ตามสถานะ: พร้อม → [เริ่ม] · กำลังจับ → [จับรอบ][หยุดชั่วคราว] · หยุด → [เริ่มต่อ][ล้างเวลา] รายการรอบใหม่สุดบน แต่ละแถว "รอบ 3 · เวลารอบ · รวม …" และคำ "เร็วสุด"/"ช้าสุด" (เมื่อมี 2 รอบขึ้นไปและไม่เท่ากันหมด ไม่ใช้สีอย่างเดียว) ไม่เกิน 99 รอบ ปุ่มเป็น "ครบ 99 รอบแล้ว" สีจาง ตอนว่างบอกวิธีจับรอบ
- **นับถอยหลัง:** ตั้งด้วยวงล้อ ชั่วโมง/นาที/วินาที (▲ เหนือตัวเลข เหนือ ▼ ปุ่ม 64×48dp กดค้างเลื่อนต่อเนื่องแบบหน้าตั้งปลุก วนในช่วงของตัวเอง สูงสุด 23:59:59) และปุ่มตั้งเร็ว 1 3 5 10 15 นาที (อันที่ตรงกับเวลาที่ตั้งแสดงเป็น "[5]" ค่าเริ่มต้น 5 นาที) แล้ว [เริ่มนับถอยหลัง] · กำลังนับ → [หยุดชั่วคราว][ยกเลิก] · หยุด → [นับต่อ][ยกเลิก] ตั้ง 0 → ปุ่มเริ่มสีจางพร้อมเหตุผลสีแดง ตัวเลขปัดขึ้นเป็นวินาที จึงเห็น 00:00 ตอนครบพอดี
- โหมดที่ไม่ได้แสดงแต่กำลังเดิน มีบรรทัดเล็กบอกใต้แถวตัวเลือก ("นับถอยหลังกำลังเดิน: เหลือ 04:12") สลับโหมดไม่หยุดอะไร
- **ปิดหน้าจอไม่หยุดการนับ (ตัดสินเอง):** การนับเป็นของ process (`timer/TimerClock`) ไม่ใช่ของหน้าจอ X, "กลับหน้าหลัก", Back และ Hey Jarvis ที่ปิดทุกหน้า (ข้อ 11) แค่ปิดหน้าจอ ทั้งสองโหมดเดินต่อ เปิดกลับมาเห็นเวลาเดิม นาฬิกาจับเวลาก็เดินต่อแบบมือถือทั่วไป
- **ไม่พลาดตอนครบ:** เวลาทั้งหมดเป็น `elapsedRealtime` (นับต่อตอนเครื่องหลับ ไม่เปลี่ยนตามนาฬิกาเครื่อง) ตอนเริ่มจอง AlarmManager สองตัวแบบ `alarm/AlarmScheduler`: `setAlarmClock` (ตัวที่ Android รับประกันว่าตรงเวลาใน Doze, `USE_EXACT_ALARM` มีอยู่แล้ว) และ `setExactAndAllowWhileIdle` แบบ elapsed สำรองกรณีนาฬิกาเครื่องถูกเปลี่ยนกลางทาง ตัวไหนมาก่อนก็ดัง ตัวที่มาเร็วเกิน 1 วินาทีจะจองใหม่ ระหว่างหน้าจออยู่ข้างหน้าก็ดังเองที่ 00:00 ทุกการเปลี่ยนเขียนลง SharedPreferences `timer` process ตายหรืออัปเดตแอป → `MainActivity` เรียก `TimerClock.restore` จองใหม่ รีบูต → รู้จาก boot count แล้วหาเวลาครบจากนาฬิกาเครื่อง ถ้าครบไประหว่างเครื่องปิด ดังทันทีที่ kiosk ขึ้นมา
- **ตอนครบ = ดังแบบนาฬิกาปลุกทุกอย่าง (`VoiceService.startTimerRing`):** จอติด เสียงปลุกของเครื่องผ่าน `alarm/AlarmRinger` (USAGE_ALARM วนซ้ำ) `VoiceState.alarmRinging` ถูกตั้ง จึง **คำปลุกหูหนวกตลอดที่ดัง** (`WakeGate.deaf` เดียวกับนาฬิกาปลุก เสียงปลุกไม่มีทางถูกได้ยินเป็นคำปลุก และไม่ต้องใช้ `WakePause`) ปุ่มจาร์วิสหยุดเสียง การ์ดนาฬิกาปลุกบนหน้าหลักปักหมุดพร้อมปุ่มหยุด หยุดเองหลัง 10 นาที แล้วหน้านี้ขึ้นมาข้างหน้า (ทับหน้าอื่นของแอป) หน้า "หมดเวลา": ไอคอนกระดิ่ง "00:00" "หมดเวลา" 24sp "นับถอยหลัง 5 นาที ครบแล้ว" ปุ่ม [หยุดเสียง] 64dp และบรรทัดบอกว่าจาร์วิสไม่ฟังคำปลุกระหว่างมีเสียง จอติดค้างระหว่างมีเสียง เสียงหยุดเองแล้วปุ่มเป็น [ตกลง] หน้ายังบอก "หมดเวลา" จนกว่าจะกด
  - หยุดด้วยมือ (ปุ่มนี้ การ์ดหน้าหลัก ปุ่มจาร์วิส หรือปิดหน้าจอระหว่างดัง) = เห็นแล้ว กลับเป็น "ตั้งเวลา" ด้วยความยาวเดิม พร้อมเริ่มใหม่ ครบ 10 นาทีแล้วเสียงหยุดเอง ไม่นับว่าเห็น
  - ถ้านาฬิกาปลุกกับนับถอยหลังดังพร้อมกัน ปุ่มหยุดหยุดทั้งคู่ (ใช้เสียงและปุ่มชุดเดียวกัน)
- log `KioskTimer` บอกสถานะและจำนวนวินาทีเท่านั้น (`countdown start s=…`, `countdown booked in_s=…`, `countdown ended source=alarm|screen|restore`, `countdown acknowledged`)

- **ครบตอนเครื่องปิด (Poom 2026-09-25):** เปิดเครื่องแล้วดังเฉพาะถ้าเลยเวลาไม่เกิน 1 ชั่วโมง (`Countdown.LATE_RING_MAX_MS`) เกินนั้นไม่ดัง หน้าจับเวลาขึ้นมาเองพร้อมบรรทัด "นับถอยหลังครบเวลาเมื่อ … ตอนมือถือปิดอยู่ เลยมาเกิน 1 ชั่วโมงจึงไม่ส่งเสียง" จนกว่าจะเริ่มนับใหม่

## 5ด. เข็มทิศ · ระดับน้ำ (0.62.0, Poom อนุมัติ)

**หน้าที่:** เข็มทิศและระดับน้ำในแอปเดียว (`compass/CompassActivity`) สลับด้วยแถวตัวเลือก ● เข็มทิศ ○ ระดับน้ำ (จำโหมดล่าสุด) เปิดจากไอคอน `ic_pixel_compass` ในแผงควบคุม หมวด "เครื่องมือ" หน้าต่าง `ui/ToolWindow` แบบเดียวกับจับเวลา แนวตั้งเท่านั้นตาม manifest
- **เซนเซอร์เปิดเฉพาะตอนหน้านี้อยู่ข้างหน้า:** ลงทะเบียนใน `onResume` ปล่อยทุกตัวใน `onPause` (ออก จอดับ Hey Jarvis ปิดทุกหน้า) และเปิดเฉพาะของโหมดที่แสดง log `KioskCompass` บอกว่ามีเซนเซอร์อะไรและความแม่นยำเป็นคำ ไม่มีทิศ
- **เข็มทิศ:** ใช้ rotation vector ถ้าเครื่องมี ไม่มีใช้ accelerometer + magnetometer (`getRotationMatrix`) ไม่มี magnetometer → บอกว่าใช้เข็มทิศไม่ได้ ระดับน้ำยังใช้ได้ วางราบ = ทิศที่หัวมือถือชี้ ถือตั้ง (เอียงเกิน 55° กลับเมื่อต่ำกว่า 35°) = ทิศที่ด้านหลังมือถือหัน (remap แกน) บรรทัดใต้ตัวเลขบอกว่าอ่านแบบไหน
  - จอ LCD: "123° SE" Press Start 2P 32sp แล้วคำไทย 18sp ตัวหนา 8 ทิศ ช่องละ 45° (เหนือ = 337.5° ถึงก่อน 22.5°) ตัวเลข 0–359 (359.6 เป็น 0 ไม่ใช่ 360) ค่ากรองแบบวิ่งทางสั้น (359° → 1° ผ่าน 0 ไม่ผ่านใต้)
  - หน้าปัด 240dp (`UiScale.DIAL`) หมุนให้เหนืออยู่ทิศเหนือ คำ "เหนือ" "ออก" "ใต้" "ตก" บนหน้าปัด เข็มเหนือยาวสีแดง เข็มใต้สั้นสีดำ (ต่างกันที่รูปร่างด้วย) ลูกศรกรมท่าคงที่ด้านบน = ทิศของมือถือ
  - "ความแม่นยำ: สูง/ปานกลาง/ต่ำ/ไม่น่าเชื่อถือ/กำลังตรวจ" จาก magnetometer ต่ำหรือไม่น่าเชื่อถือ → กล่องตัวหนาสีแดง "เข็มทิศยังไม่แม่น: โบกมือถือเป็นรูปเลข 8…" สนามแม่เหล็กนอกช่วง 20–70 µT → "มีแม่เหล็กหรือโลหะอยู่ใกล้…"
  - เป็นทิศเหนือแม่เหล็ก หน้าจอบอกไว้ (ในไทยต่างจากเหนือจริงราว 1° หรือน้อยกว่า) ไม่ใช่เครื่องมือสำรวจ
- **ระดับน้ำ:** จาก TYPE_GRAVITY ถ้ามี ไม่มีใช้ accelerometer กรอง low-pass วางราบ = สองแกน (ซ้าย–ขวา บน–ล่าง) ตั้งบนขอบล่าง = แกนเดียว ตะแคงหรือกลับหัว = บอกให้วางราบหรือตั้งบนขอบล่าง
  - **"ได้ระดับ" เมื่อเอียงไม่เกิน ±1° (รวม 1.0)** หัวข้อ 24sp ตัวหนา "ได้ระดับ" (สีเขียว) / "ยังไม่ได้ระดับ" ใต้ภาพบอกเป็นคำทุกแกน "ขวาสูงกว่า 1.4°" / "ตรง (0.3°)"
  - ภาพ: วางราบเป็นวงกลมมีเส้นกากบาทและวง ±1° ตรงกลาง ตั้งเป็นหลอดมีขีด ±1° ฟองไปทางด้านที่สูงเหมือนระดับน้ำจริง เต็มขอบที่ 10° **ได้ระดับ = ฟองทึบ ไม่ได้ = ฟองโปร่ง** (รูปร่างบอก สีแค่ช่วย)
  - หมายเหตุบนจอ: วัดโดยถอดเคส (เคสทำให้ด้านหลังไม่เรียบ)



- **0.61.0 (Poom 2026-09-25): ระดับน้ำอย่างเดียว** A07 ไม่มีเซนเซอร์แม่เหล็ก (`magnet=false`) โหมดเข็มทิศซ่อน (`CompassActivity.COMPASS_OFFERED = false`) ชื่อในแผงควบคุมและหัวหน้าต่างเป็น "ระดับน้ำ" ไอคอน `ic_pixel_level` (ไม้ระดับมีฟองอากาศ) โค้ดเข็มทิศยังอยู่สำหรับเครื่องที่มีเซนเซอร์

## 5ถ. ปฏิทิน (0.63.0, Poom)

`calendar/CalendarActivity` ไอคอนในโฟลเดอร์เครื่องมือ · Google Calendar ของ Poom **ผ่าน broker** (`/v1/calendar/list|save|delete`, `server/kiosk_broker/calendar_app.py`) ด้วยการเชื่อมเดิม — token อยู่ที่ VPS เท่านั้น
- **ส่วนตัวเหมือนคำถามปฏิทินด้วยเสียง:** ทุกคำขอต้องมี grant จากการยืนยันตัวตน (2 นาที) ดู เพิ่ม แก้ = ยืนยันเมื่อ grant หมด · **ลบ = ยืนยันตัวตนใหม่ทุกครั้ง** · ข้อมูลที่ดึงมาอยู่ในหน้านี้เท่านั้น ปิดแล้วหาย · ไม่ผ่านโมเดล AI · log มีแค่จำนวน
- **รายเดือน:** ปี พ.ศ. ◄ เดือน ► · 6 สัปดาห์เริ่มวันอาทิตย์ · ช่องวันละ 56dp · **เครื่องหมายต่างที่รูปร่าง:** ● มีนัด · ◆ วันหยุดราชการ · ◇ วันสำคัญ/วันอื่นในปฏิทินวันหยุด (+ ตัวเลขวันหยุดสีแดง) · วันนี้เป็นช่องยุบ ตัวหนา
- **วันหยุดไทย:** จากปฏิทินวันหยุดไทยของ Google (`th.th#holiday@group.v.calendar.google.com`) แยกวันหยุดราชการ/วันสำคัญจากคำอธิบายที่ Google ให้ ถ้าไม่บอก = "อยู่ในปฏิทินวันหยุด (ไม่ได้ระบุว่าหยุดหรือไม่)" · ดึงวันหยุดไม่ได้ → แสดงนัดพร้อมบอกว่าวันหยุดขาด · หมายเหตุ "วันหยุดพิเศษที่ประกาศกะทันหันอาจขึ้นช้า"
- **รายวัน:** วันหยุดของวันนั้น นัด (ทั้งวันก่อน แล้วตามเวลา) [แก้ไข] [ลบ] · ว่าง = "วันนี้ยังไม่มีนัด" + ปุ่มหลัก "เพิ่มนัดในวันนี้"
- **เพิ่ม/แก้:** ชื่อนัด (≤80) · วันที่ ◄ ► · มีเวลา/ทั้งวัน · เวลาเริ่ม/จบ ทีละ 15 นาที · สิ่งที่พิมพ์ไม่หายเมื่อกดอย่างอื่น · ข้อผิดพลาดบอกทีละข้อจาก broker · บันทึกแล้วไปหน้าวันนั้น "บันทึกนัดแล้ว"
- **วันพระ (0.65.0, Poom เลือก ข):** คำนวณบนมือถือ (`calendar/Lunar.kt`) ไม่มีหน่วยงานใดเผยแพร่รายการที่อ่านได้โดยไม่ต้องสมัคร
  - ปีจันทรคติเริ่มขึ้น 1 ค่ำ เดือนอ้าย เดือนสลับ 29/30 วัน / อธิกมาส: ดิถีของ จ.ศ. อยู่ใน 24–29 หรือ 0–5 และปีถัดไปไม่อยู่ / อธิกวาร: ปีที่ไม่มีอธิกมาส และดิถีเฉลี่ยของวันแรกของปีต่ำกว่า 1 (ต่ำกว่า 0.91 ในปีที่ต่อจากอธิกมาส)
  - ตรวจแล้ว: 49 วันพระของปี 2569 ตามรูปของ สวท.ประจวบคีรีขันธ์ (วันที่รูปพิมพ์ผิด 26 เม.ย. ใช้ 24 เม.ย. ตาม myhora) และทุกวันพระปี 2541–2570 ของ myhora (1,488 วัน ตรงทั้งวันและเดือนจันทรคติ) ทั้งสองแหล่งไม่ใช่ประกาศทางการ
  - บนจอ: ▲ ในช่องวัน / หน้าวันบอกชื่อจันทรคติ / ใต้เดือนเขียนว่า "คำนวณจากปฏิทินจันทรคติ ไม่ใช่ประกาศทางการ" บอกอธิกมาส/อธิกวารของปีนั้น และปีหลัง 2570 ที่ค่าก้ำกึ่งจะบอกว่า "อาจคลาด 1 วัน" / คำนวณไว้ปี 2541–2600
- ปุ่มจาร์วิสหน้านี้: "พรุ่งนี้มีอะไร" = นัดพรุ่งนี้ · "วันหยุดครั้งหน้าวันไหน" ตอบจากปฏิทินวันหยุด (ทุกหน้า ไม่ต้องยืนยันตัวตน: ข้อมูลสาธารณะ)

## 5ต. โน้ต / รายการซื้อของ (0.62.0, Poom อนุมัติ)

**หน้าที่:** จดของที่ต้องซื้อและโน้ตสั้น ๆ พิมพ์เองหรือบอกจาร์วิส เป็นหน้าจอของแอปนี้ (`notes/NotesActivity`) เข้าจากไอคอน "โน้ต" (`ic_pixel_note`) ในหมวด "ในบ้านและใช้บ่อย" ของแผงควบคุม
- **รายการ:** "รายการซื้อของ" และ "โน้ต" มีมาในตัว ลบหรือเปลี่ยนชื่อไม่ได้ สร้างรายการของตัวเองเพิ่มได้ (ชื่อไม่เกิน 30 ตัวอักษร ไม่ซ้ำ ไม่เกิน 20 รายการ)
- **เก็บในไฟล์ของแอปเท่านั้น** (`files/notes.json`, `notes/NoteStore`) ไม่อยู่ในที่เก็บไฟล์ร่วม ไม่ log ไม่ส่งไปไหน ยกเว้นสิ่งที่พูดกับจาร์วิส และตอนสั่งให้อ่าน ข้อความที่อ่านผ่านเสียงของ broker (ไม่เก็บ ไม่ log) ไฟล์ที่อ่านไม่ได้ถูกเก็บไว้เป็น `notes.bad.json` ไม่ลบ บันทึกไม่สำเร็จ = ไม่เปลี่ยนอะไรและบอกเหตุผล
- บรรทัดละไม่เกิน 120 ตัวอักษร (สั่งด้วยเสียงไม่เกิน 60 เท่าที่ broker ส่ง) รายการละไม่เกิน 300 บรรทัด บรรทัดเดียวกัน (ไม่สนช่องว่างและตัวพิมพ์) ที่ยังไม่ติ๊กเพิ่มซ้ำไม่ได้ ถ้าติ๊กไปแล้ว การเพิ่มซ้ำคือเอาติ๊กออกแล้วย้ายไปท้าย

**หน้าจอ (กรอบแผงควบคุม ขนาดทั้งหมดจาก `UiScale`):** เปิดมาที่ "รายการซื้อของ" (งานที่ทำบ่อยที่สุด) หนึ่งหน้าหนึ่งเรื่อง: รายการ → แก้บรรทัด ปุ่ม "◄ รายการทั้งหมด" และ Back ขึ้นไปหน้ารวมทุกรายการ (Back จากหน้ารวม = ปิด) X กลับหน้าที่เปิดมา (`ui/Origin`) "กลับหน้าหลัก" 56dp ล่างสุด
- บนสุด: ช่องพิมพ์ + ปุ่ม "เพิ่ม" (48dp) กด Done บนแป้นก็เพิ่ม เพิ่มแล้วช่องว่างรอบรรทัดถัดไป ผิดพลาดขึ้นใต้ช่องสี `retro_bad` ไม่ล้างที่พิมพ์
- "ยังไม่ได้ซื้อ (N)" (รายการอื่น "ยังไม่ติ๊ก (N)") ตามลำดับที่เพิ่ม แล้ว "ติ๊กแล้ว (M)" กับปุ่ม "ล้างที่ติ๊กแล้ว"
- **แถว:** ช่องติ๊ก 48dp ชิดซ้าย (`ic_pixel_check_off/on`) แล้วข้อความ 15sp แถวสูงอย่างน้อย 56dp **ติ๊กแล้ว = ☑ + เส้นขีดทับ + สีจาง** ต่างกันที่รูปร่าง ไม่ใช่สีอย่างเดียว เครื่องอ่านหน้าจออ่าน "ยังไม่ติ๊ก: … แตะเพื่อติ๊ก" / "ติ๊กแล้ว: … แตะเพื่อเอาติ๊กออก" แตะข้อความ → หน้าแก้ไข [บันทึก] 56dp, [เอาออกจากรายการ][ยกเลิก]
- **ไม่ต้องยืนยันตัวตนและไม่ถาม** สำหรับเอาบรรทัดออกหรือล้างที่ติ๊กแล้ว เพราะไม่ได้ลบไฟล์ของใคร (แบบเดียวกับเอาเพลงออกจาก playlist, CLAUDE.md ข้อ 3) **ลบทั้งรายการ** (เฉพาะรายการที่สร้างเอง) ถาม "ลบรายการ … และทุกอย่างในรายการใช่หรือไม่ ย้อนกลับไม่ได้" ในที่เดิมก่อน ไม่ต้องยืนยันตัวตน
- ทุกสถานะมีคำ: ว่าง (บอกวิธีเพิ่ม) / ติ๊กครบแล้ว / เพิ่มแล้ว / มีอยู่แล้ว / บันทึกไม่สำเร็จ และบรรทัดบอกคำสั่งเสียงของรายการนั้น
- สั่งด้วยเสียงระหว่างหน้านี้เปิด (ปุ่มจาร์วิส) บรรทัดใหม่ขึ้นทันที ที่พิมพ์ค้างไว้ไม่หาย

**คำสั่งเสียง (broker แยกคำในโค้ด ไม่ใช้โมเดล `server/kiosk_broker/notes.py`):**
- เพิ่ม: "เพิ่ม นม ในรายการซื้อของ", "จดว่าซื้อไข่", "ซื้อ น้ำปลา ด้วย", "จดว่า พรุ่งนี้จ่ายค่าไฟ" (→ โน้ต) → action `note_add {list, text}` **มือถือเพิ่มก่อนพูด** (`DONE_BEFORE_SPEAKING`) เพิ่มได้จึงพูด "เพิ่ม นม ในรายการซื้อของแล้วครับ" ไม่ได้ก็พูดเหตุผลแทน (มีอยู่แล้ว / ยาวเกิน / เต็ม / บันทึกไม่สำเร็จ) ไม่พูดข้อความยาวเกิน 30 ตัวอักษรซ้ำ และไม่ตัดกลางคำ
- อ่าน: "อ่านรายการซื้อของ", "ต้องซื้ออะไรบ้าง", "ในรายการซื้อของมีนมไหม" → `note_read {list}` มือถืออ่านจากไฟล์ของตัวเอง คำนั้นคือคำตอบ (`ANSWERED_ON_PHONE`) อ่านเฉพาะที่ยังไม่ติ๊กตามลำดับ ไม่เกิน 180 ตัวอักษร ที่เหลือบอก "และอีก N อย่าง ดูได้ที่หน้าโน้ตครับ" broker ไม่เห็นรายการเลย
- ลบ/ติ๊ก/ล้างด้วยเสียง **ยังไม่มี**: ประโยคที่ขอแบบนั้นกับรายการ ตอบในโค้ดว่า "ลบหรือขีดรายการด้วยเสียงยังทำไม่ได้ครับ ทำได้ที่หน้าโน้ตในแผงควบคุม" โมเดลจึงไม่มีทางอ้างว่าลบแล้ว
- ไม่จับคำถามธรรมดา ("ซื้ออะไรดี", "ไปซื้อของด้วยกันไหม") และไม่แย่งประโยคของปฏิทิน ("เพิ่มนัด…") ปลุก กล้อง ไฟ เพลง วิดีโอ
- log: เหตุผลเท่านั้น (`notes=` ท้ายบรรทัด intent ของ broker, `action note_add list=shopping added=true` ในมือถือ) ไม่มีข้อความของบรรทัดหรือชื่อรายการ

**ไอคอน:** `ic_pixel_note` สมุดจด ขอบดำ ห่วงดำสามห่วง แถบเทา กระดาษขาว เส้นบรรทัดกรมท่าสามเส้น (เส้นสุดท้ายสั้น) 4 สีจากจานสีหน้าต่าง ใช้ทั้งบนแถบกรมท่าและพื้นเทา วาดเองบนกริด 16×16 (`NotesScreenTest`)
## 5ท. Facebook และ Instagram (0.65.0, Poom 2026-09-25)

- โฟลเดอร์ "โซเชียล" ในแผงควบคุม ไอคอนวาดเอง (สองคน / รูปถ่าย) ไม่ใช้โลโก้ของแบรนด์
- กดไอคอน → หน้าบอกว่าต้องสแกนใบหน้าก่อน และกลับเข้าตู้ได้อย่างไร → กล้องยืนยันตัวตนเปิดทันที → ผ่านแล้วแอปนั้นถูกเพิ่มใน allowlist ของ lock task **เฉพาะครั้งนั้น** (`social/SocialVisit`) → เปิดแอป
- จบการเยี่ยมชม allowlist กลับเป็นของตู้ทันที และงานของแอปถูกปิด: หน้าจอของตู้ขึ้นมาด้านหน้า (กดย้อนกลับจนออก หรือ "Hey Jarvis") / ปิดหน้าจอ / ครบ 30 นาที (ตัดสินเอง: เลื่อนครั้งละ 5 นาทีถ้าแอปใช้ไมโครโฟนอยู่ เพื่อไม่ตัดสาย)
- ครั้งละหนึ่งแอป ลิงก์ไปเบราว์เซอร์หรือแอปอื่นเปิดไม่ได้ ตู้ไม่หลุด lock task / นอกการเยี่ยมชม ไม่มีทางเปิดสองแอปนี้ได้ (ลิงก์ การแชร์ การแจ้งเตือน)
- การแจ้งเตือนของ Facebook Lite, Facebook และ Instagram ถูกปฏิเสธด้วย policy ของ Device Owner (POST_NOTIFICATIONS) ทุกครั้งที่ตู้เริ่มและทุกการเยี่ยมชม
- Facebook Lite ก่อน (มีใน Play Store ไทย) แอปเต็มใช้เฉพาะเมื่อไม่มี Lite / Instagram ที่เครื่องติดมาเป็นตัวว่าง (stub) นับว่ายังไม่ได้ติดตั้ง
- ติดตั้งจาก Play Store เท่านั้น: ปุ่ม "สแกนใบหน้าแล้วติดตั้งจาก Play Store" เปิดหน้าของแอปนั้น โดย Play Store อยู่ใน allowlist เฉพาะครั้งนั้น
- ระหว่างแอปใช้ไมโครโฟน คำปลุกพัก ("แอปพลิเคชันอื่นใช้ไมโครโฟน") แล้วกลับมาเมื่อเลิกใช้
- Claude Code ไม่พิมพ์รหัสผ่านใดๆ Poom เข้าสู่ระบบเอง

## 6. ไอคอน

- **กริด 16×16** เป็น `<vector>` ที่ viewport 16 วาดจากสี่เหลี่ยม (`M x,y h w v h h -w z`) เท่านั้น ห้ามใช้เส้นโค้งหรือ anti-alias ความโค้งต้องเป็นขั้นบันไดแบบ sprite 8-bit
- **ขนาดที่แสดง:**
  - แถบหัวข้อ 13dp (จาร์วิส 15dp)
  - แถวพระอาทิตย์และแบตเตอรี่ 14dp
  - ไอคอนเหรียญในบรรทัด = 0.95 เท่าของขนาดตัวอักษร (คริปโตขั้น 4, 12sp)
- **ไม่เกิน 4 สีต่อไอคอน** (ไม่นับโปร่งใส) และใช้สีจากตารางในข้อ 2
- **แสงมาจากบนซ้ายเหมือนกรอบหน้าต่าง:** ของนูนมีเงาเข้มทางล่างและขวา (ดูเหรียญ)
- **วาดให้เข้ากับพื้นที่วาง:** ไอคอนบนแถบกรมท่าใช้สีสว่าง ไอคอนบนแผงเทาใช้เส้นเข้ม (ดูพระอาทิตย์ขึ้น/ตก)
- **ความหมายต้องไม่อยู่ที่ไอคอนอย่างเดียว:** ทุกไอคอนมีคำกำกับข้าง ๆ (ชื่อการ์ด, "ขึ้น"/"ตก", ชื่อเหรียญ) และมี `contentDescription`
- **รายการไอคอน:**
  - อากาศ: `ic_pixel_sun` / `ic_pixel_moon` (ตาม `is_day` จาก broker)
  - ทอง/สินค้าโภคภัณฑ์: `ic_pixel_gold`
  - คริปโต: `ic_pixel_coin` สำหรับการ์ด และเป็นค่าเริ่มต้นของเหรียญที่ยังไม่มีไอคอน
  - เหรียญ: `ic_pixel_btc`, `ic_pixel_eth`, `ic_pixel_bnb`, `ic_pixel_xrp`
  - จาร์วิส: `ic_pixel_jarvis`
  - อุปกรณ์ในบ้าน (เดิม Google Home): `ic_pixel_home` สีของจอนี้ ไม่ใช่สีแบรนด์
  - แผงควบคุม หมวดที่มาข้อมูล: `ic_pixel_sources` (หนังสือเปิด)
  - ยืนยันตัวตน: `ic_pixel_face` (หน้าในกรอบช่องมองภาพ)
  - แบตเตอรี่: `ic_pixel_battery`, `_low`, `_charging`
  - พระอาทิตย์: `ic_pixel_sunrise`, `ic_pixel_sunset`
  - นาฬิกาปลุก: `ic_pixel_bell` (การ์ด), `ic_pixel_alarm_clock` (หมวดในแผงควบคุม)
  - แผงควบคุม: `ic_pixel_control_panel`, ปุ่มปิด `ic_pixel_close`, ช่องติ๊ก `ic_pixel_check_on` / `ic_pixel_check_off`
  - แผงควบคุม WiFi: `ic_pixel_wifi` (โค้งขั้นบันได 3 ชั้นสีกรมท่า `#000080` เหนือจุดดำ) และไฟฉาย: `ic_pixel_torch` (ตัวไฟฉายเทา เลนส์สีทอง `#FFD700`) (0.42.0)
  - ไอคอนแผงควบคุมวาดเองเป็นแผงที่มีปุ่มเลื่อน **ไม่ใช่** ไอคอน Control Panel ของ Microsoft
  - เครื่องเล่นเพลง (0.53.0): `ic_pixel_music` โน้ตคู่สีกรมท่ามีขีดทองบนคาน (บนพื้นเทา) และ `ic_pixel_music_light` โน้ตสีขาว (บนแถบกรมท่าและการแจ้งเตือน เพราะโน้ตกรมท่าหายไปบนแถบกรมท่า เห็นบน A07) ปุ่ม `ic_pixel_prev` `ic_pixel_play` `ic_pixel_pause` `ic_pixel_stop` `ic_pixel_next` สีดำสีเดียว มีคำใต้รูปเสมอ
  - เครื่องคิดเลข (0.52.0): `ic_pixel_calculator` ตัวเครื่องดำ หน้าเทาเข้ม จอเขียว (เขียวของแบตเตอรี่ `#2E8B2E`) ปุ่มขาว 9 ปุ่ม
  - จัดการไฟล์ (0.59.0, Poom): **แฟ้มสีเหลือง `ic_pixel_folder` ทุกที่** ทั้งช่องในแผงควบคุมและแถบหัวข้อ ให้เข้าชุดกับโฟลเดอร์ในรายการ
  - ไฟล์ zip: `ic_pixel_zip` (เดิมชื่อ `ic_pixel_files` และเป็นไอคอนจัดการไฟล์จนถึง 0.58) กล่องสีดำ ฝาเทา ซิปขาวตรงกลาง ที่ดึงทอง
    - Poom ขอให้ "หน้าตาคล้าย 7zip": ได้บรรยากาศกล่องสี่เหลี่ยมดำของโปรแกรมบีบอัด แต่ **ไม่มีตัว "7z" และไม่ใช้รูปทรงใดจากโลโก้ 7-Zip** ตามข้อ 8
  - ชนิดไฟล์ (0.59.0) **ต่างกันที่รูปร่าง ไม่ใช่แค่สี:** `ic_pixel_type_audio` โน้ตตัวเดียว, `ic_pixel_type_video` แถบฟิล์มมีรูหนามเตย, `ic_pixel_type_image` กรอบรูปมีเนินและดวงอาทิตย์, `ic_pixel_file` กระดาษมีเส้น (เอกสาร), `ic_pixel_type_other` กระดาษเปล่าไม่มีเส้น
  - ในรายการไฟล์: `ic_pixel_folder` (แฟ้มสีเหลืองมีแถบ), `ic_pixel_file` (กระดาษพับมุม), `ic_pixel_lock` (แม่กุญแจ สำหรับโฟลเดอร์ที่ระบบไม่อนุญาต มีคำกำกับเสมอ), `ic_pixel_phone` (หน่วยความจำเครื่อง), `ic_pixel_nas` (ตู้เทามีช่องไดรฟ์และไฟเขียว `#2E8B2E` ของแบตเตอรี่)
  - `FileManagerTest` ตรวจว่าไอคอนชุดนี้เป็นกริด 16×16 สี่เหลี่ยมล้วน ไม่เกิน 4 สี
  - จับเวลา (0.62.0): `ic_pixel_stopwatch` นาฬิกาจับเวลา ปุ่มบนและปุ่มข้างสีทอง `#FFD700` ตัวเรือนกลมขั้นบันไดสีเทา `#808080` หน้าขาว เข็มดำสองเข็ม / เข็มทิศ (0.62.0): `ic_pixel_compass` ตัวเรือนเทา `#808080` หน้าขาว เข็มเหนือแดง `#CC0000` เข็มใต้กรมท่า — ตัวเรือนเทาแทนกรมท่าเพื่อให้เห็นทั้งบนแถบหัวข้อกรมท่าและบนแผงเทา ไอคอนเดียวใช้ได้ทั้งสองที่ (`TimerCompassScreenTest` ตรวจกริดและจำนวนสี)

**ไอคอนทุกตัวเป็นงานวาดเองของโปรเจกต์นี้:** ไม่ได้ลอก ไม่ได้ trace และไม่ได้ดัดแปลงจากงานของใคร

- **เหรียญคริปโต:** เป็นเหรียญกลมทั่วไปที่มีอักษรหนึ่งตัว ไม่ใช่โลโก้ของโครงการ
  - BTC = B ที่มีขีดบนล่าง (สัญลักษณ์สกุลเงิน ₿ ใน Unicode)
  - ETH = Ξ
  - BNB = B
  - XRP = X
  - เลือกสีให้แยกกันออก แต่ไม่ได้คัดลอกลวดลายโลโก้
- **บ้าน Google Home:** เดิมใช้สี่สีของแบรนด์ Google เปลี่ยนเป็นสีของจอนี้แล้ว เพราะรูปทรงเป็นของเราก็จริง แต่ชุดสีทำให้ดูเหมือนยืมแบรนด์มา

## 7. ข้อความภาษาไทย

- **ห้ามตัดกลางคำ:**
  - ข้อความหลายบรรทัดปล่อยให้ Android ตัดบรรทัด (ICU ตัดภาษาไทยตามพจนานุกรมคำ)
  - ห้ามใส่ `\n` กลางคำ และห้ามตัด string ด้วยจำนวนตัวอักษรในโค้ด
  - ถ้าต้องตัดจริง (เช่น ชื่อสถานที่ยาว) ให้ใช้ `ellipsize=end` บนบรรทัดเดียวเท่านั้น
- **ห้ามแยกสระหรือวรรณยุกต์ออกจากพยัญชนะ:** ถ้าจะตัด string ในโค้ด ให้ตัดที่ช่องว่างหรือจบประโยคเท่านั้น (ดู `server/kiosk_broker/shorten.py` ฝั่งเสียง)
- **ตัวเลขเป็นเลขอารบิก** เช่น "22 องศา" ทั้งบนจอและในคำตอบ
- **เวลาและวันที่ (Poom, 2026-09-23) ใช้รูปแบบเดียวกันทุกที่บนจอ** ทั้ง taskbar การ์ดปลุก แผงควบคุม และวันที่ราคาน้ำมัน
  - **เวลาบนจอ:** 12 ชั่วโมงพร้อม AM/PM เช่น "1:40 PM" (`DashboardState.clock12`, `Locale.US`, ชั่วโมงไม่มีเลข 0 นำ)
  - **วันที่บนจอ:** ภาษาอังกฤษ "Wed 23/09/2026" (`ui/ScreenDate`) ปีเป็น ค.ศ. วันที่จากแหล่งที่เขียนเป็น พ.ศ. ให้แปลงก่อน
  - **เวลาที่ผู้ใช้พูด:** แบบไทย เช่น ตีห้า สิบเอ็ดโมง บ่ายสอง ทุ่มนึง (ทุ่มนึง = 7:00 PM, บ่ายสอง = 2:00 PM) แยกคำสั่งที่ `server/kiosk_broker/alarms.py`
  - **เวลาที่จาร์วิสพูด:** แบบไทยเหมือนกัน ห้ามพูด AM/PM หรือ "13:45" บรรทัดเวลาที่ให้โมเดลจึงมีแต่แบบไทย (`clock.context_line`)
- **ถ้อยคำใช้ภาษาเขียนสุภาพตาม skill ux-ui-design** แต่คำพูดของจาร์วิสเป็นเสียงของตัวละคร (ลงท้าย "ครับ") และคุมด้วย persona ฝั่ง server
- **ห้ามแสดงพิกัด, token, key หรือข้อมูลเทคนิค** บนจอที่หันเข้าหาห้อง ข้อมูลวินิจฉัยอยู่ใน dumpsys

**Taskbar แสดงชนิดการเชื่อมต่อ (0.42.0):** คำเดียวใน tray ก่อนแบตเตอรี่ ขนาดและสีเดียวกับแบตเตอรี่ (ขั้น 6, 10sp, `retro_dim`): "WiFi", "5G", "4G", "3G", "มือถือ" หรือ "ออฟไลน์" (`ui/NetworkWatch`)
- เป็นคำ ไม่ใช่ไอคอนหรือขีดสัญญาณอย่างเดียว
- 5G แบบ NSA (ใช้ร่วมกับ LTE) นับเป็น 5G ตามที่แถบสถานะของเครื่องแสดง
- ที่ว่างได้จากลด padding ของปุ่มจาร์วิสจาก 14dp เป็น 8dp ต่อข้าง ระยะใน tray (padding 5dp, ระยะหลังแต่ละคำ 5dp) และตัดระยะหน้า status ที่ซ่อนอยู่ รูปแบบวันที่ "Wed 23/09/2026" ไม่เปลี่ยน
**วันที่ย้ายไปแถบหัวข้อของการ์ดจาร์วิส (0.47.0, Poom 2026-09-24):**
- อยู่ชิดขวาแถวเดียวกับ "จาร์วิส · พร้อมฟัง" สไตล์เดียวกับป้ายมุมขวาของการ์ดอื่น (`retro_badge`, 10sp, IBM Plex Sans Thai) รูปแบบเดิม "Thu 24/09/2026"
- สถานะจาร์วิสกินที่ที่เหลือ บรรทัดเดียว ตัดท้ายถ้าจำเป็น วันที่ไม่ถูกตัด
- taskbar เหลือ ชนิดการเชื่อมต่อ · แบตเตอรี่ · เวลา
- `HomeCardTest` ตรวจว่า `taskbar_date` ไม่มีแล้ว และ `jarvis_date` อยู่ในแถบหัวข้อเดียวกันด้วยสไตล์ป้าย
- **ตรวจ tray ตอนเวลา 10–12 นาฬิกาเสมอ:** นาฬิกา Press Start 2P กว้างเท่ากันทุกตัว "10:20 PM" (8 ตัว) จึงกว้างสุด 0.42.0 ตรวจตอน "9:46 PM" แล้วพลาด บน A07 ตอน 10:20 PM "PM" ถูกตัดเหลือ "PI" (แก้ใน 0.43.0)

## 8. สิ่งที่ห้ามทำ

- ใช้ asset, ไอคอน, ฟอนต์ หรือภาพหน้าจอของ Microsoft (รวมถึงฟอนต์ระบบของ Windows และไอคอน Windows 95 จริง) เราทำแค่ "บรรยากาศ" ด้วยสีและกรอบนูนที่วาดเอง
- คัดลอกหรือ trace โลโก้ของแบรนด์ใด ๆ (Google, Xiaomi, โครงการคริปโต, ร้านทอง, ปั๊มน้ำมัน)
- ใช้ icon pack หรือรูปจากเว็บ แม้จะอ้างว่าฟรี
- ใช้ emoji แทนไอคอน
- สื่อความหมายด้วยสีอย่างเดียว เช่น เขียว/แดงโดยไม่มีเครื่องหมาย +/−
- ลดขนาดตัวอักษรเพื่อให้ข้อความพอดีการ์ด
- ย้าย ซ่อน หรือวางของทับ `exit_corner`

## 9. ตรวจก่อนรายงานทุกครั้งที่แก้ UI

1. ติดตั้ง APK จาก CI ลง A07 ด้วย `adb install -r -t`
2. ถ่ายหน้าจอจริงแล้วดูเอง: ข้อความล้นไหม ถูกตัดไหม การ์ดแน่นไหม ไอคอนอ่านออกไหม
3. ถ้าเป็นไปได้ ตรวจทั้งสถานะกลางวันและกลางคืน รวมถึงสถานะที่ไม่มีข้อมูล
4. ตรวจว่า Hey Jarvis, ปุ่มจาร์วิส, ปลุกจอ, Maps, แอปกล้อง, lock task และทางออก 10 ครั้งยังทำงาน
5. ถ้ามีกติกาใหม่ ให้แก้ไฟล์นี้ใน commit เดียวกัน

## 10. ข้อมูลสุขภาพเครื่องสำหรับ soak test (0.41.0, phase 6)

ไม่มีอะไรเปลี่ยนบนจอ กติกาของสิ่งที่เครื่องส่งออกไปเองโดยไม่มีใครสั่ง:

1. ส่งได้เฉพาะตัวเลขและคำบอกสถานะของแอปเอง (รายชื่ออยู่ที่ `server/kiosk_broker/soak.py` FIELDS) ห้ามมีสิ่งที่ได้ยินหรือพูด ชื่อสถานที่ พิกัด ปฏิทิน หรือบัญชี
2. เซิร์ฟเวอร์ทิ้งทุกช่องที่ไม่อยู่ในรายชื่อ และเก็บเฉพาะตอนที่ Poom สั่งเริ่ม soak
3. การส่งทำบน thread ของตัวเอง (`kiosk-soak`) ห้ามใช้ thread ของบทสนทนา เพื่อให้เน็ตค้างแล้วไม่ทำให้ Jarvis ตอบช้า
4. ทุก 15 นาที ถ้าส่งไม่ถึงให้ลองใหม่ทุก 1 นาที ห้ามถี่กว่านี้

## 11. Hey Jarvis กลับหน้าหลักเสมอ (0.43.0, Poom)

- เรียก Hey Jarvis (หรือปุ่มจาร์วิส หรือคำสั่ง adb) เมื่อไหร่ ถ้าหน้าหลักไม่ได้อยู่ข้างหน้า **ทุกหน้าของแอปเราปิดก่อน แล้วหน้าหลักขึ้นมา**
  - ครอบทุกหน้า: หน้ายืนยันตัวตน แผงควบคุม หน้าตั้งค่าย่อย และแอปอื่น (Maps, Xiaomi Home, แผง WiFi)
  - เหตุการณ์จริง: เปิดหน้ายืนยันตัวตนค้างไว้แล้วเรียก Hey Jarvis จาร์วิสตอบ แต่จอค้างที่หน้ายืนยันตัวตน เพราะเดิมกลับหน้าหลักเฉพาะหลังเปิด Maps หรือแอปกล้อง
- **หน้าใหม่ครอบอัตโนมัติ:** `KioskApp` ลงทะเบียน `KioskScreens` กับทุก activity ในแอป ตั้งแต่ตอนสร้าง ไม่ต้องจำไปใส่รายชื่อ
  - หน้าไหนต้องปิดแบบพิเศษให้ implement `KioskScreens.Leavable`
- **หน้ายืนยันตัวตนปิดอย่างปลอดภัย:** ปิดกล้องก่อน และนับเป็น "ยกเลิก" เสมอ ไม่ถือว่าผ่าน การลงทะเบียนหรือการลบที่ค้างอยู่จะไม่เกิดขึ้น
- ถ้าอยู่หน้าหลักอยู่แล้ว จะไม่เปิด activity ซ้ำ
- `dumpsys` ของ VoiceService บรรทัด `screens :` บอกว่าหน้าไหนเปิดอยู่ และหน้าหลักอยู่ข้างหน้าหรือไม่

## 11ข. สัญลักษณ์สถานะจาร์วิสและปุ่มจาร์วิสในแอป (0.61.0, Poom)

**สัญลักษณ์สถานะบนทุกหน้า แทนเสียงบี๊บหลังคำปลุก** (`ui/JarvisBadge`, `voice/JarvisStatus`): ที่เดียวกันทุกหน้า — ในแอป ซ้ายของปุ่ม X ในแถบหัวข้อ (36dp) · หน้าแรก ท้ายแถบหัวข้อหน้าต่างแรก (28dp, `weather_titlebar` เว้นขวา 34dp ให้) · ใส่ให้ทุกหน้าอัตโนมัติจาก `KioskScreens` หน้าใหม่ลืมไม่ได้
- **6 สถานะต่างกันที่รูปร่าง ไม่ใช่สี** (ตาราง 12×12 วาดจากสี่เหลี่ยมแบบไอคอนอื่น): ○ พร้อมฟัง · ● มีขีดเสียงสองข้าง กำลังฟัง · ••• กำลังคิด · กล่องคำพูด กำลังพูด · ❚❚ พัก (เล่นสื่อ/บันทึกเสียง) · ⊘ ใช้ไม่ได้ (ไมค์ใช้ไม่ได้ ไม่มีเน็ต ไม่มีโทเค็น) · ลำดับเดียวกับ `DashboardState.jarvisState` การ์ดหน้าแรกกับสัญลักษณ์จึงไม่ขัดกัน · TalkBack อ่านเป็นคำ ("จาร์วิสพร้อมฟัง") · อัปเดตทุก 300 ms
- เสียงบี๊บหลังคำปลุก **ปิด** (`VoiceState.beep` ค่าเริ่ม false) เพราะบี๊บทับพยางค์แรก (วัด 0.61.0: มีบี๊บ "พรุ่งนี้" ผิด 4/4 ไม่มีบี๊บถูก 4/5) · **ปุ่มจาร์วิสยังบี๊บ** (กดแล้วรู้ว่าติด ไม่มีคำปลุกให้ทับ)

**ปุ่มจาร์วิสในแอปที่มีคำสั่งเสียง:** เพลง วิดีโอ โน้ต วิทยุ จับเวลา แผงควบคุม — สัญลักษณ์เดียวกันกลายเป็นปุ่มนูน (รูปทรงบอกว่ากดได้) สีตามแอป: เพลง = โลหะดำทองแบบ Winamp · วิดีโอ = เงินกับจอเขียวแบบ PowerDVD · จัดการไฟล์/Drive = น้ำเงินฟ้าแบบ Norton (ป้ายเท่านั้น) · อื่นๆ ปุ่มเทาแบบ retro · หน้าอื่นเป็นป้ายแบนกดไม่ได้
- กด = ฟังเหมือนปุ่มใน taskbar (`ACTION_BUTTON_LISTEN`) และบอก broker ว่าถามจากหน้าไหน: header `X-Kiosk-Screen` คำตายตัวหนึ่งคำ (music video notes radio timer panel) **ไม่เคยส่งเนื้อหาบนจอ** · ล้างทุกครั้งที่จบตาหรือเมื่อคำปลุกเริ่มตาใหม่ · log `KioskVoice: jarvis button pressed screen=…` และ `turn screen=…`
- broker (`server/kiosk_broker/screen_context.py`): คำสั่งห้วนบนหน้าของมัน ("ต่อไป" "หยุด" "เบาลง" "เปิด X" บนหน้าเพลง, "หยุด" "เปิด X" บนหน้าวิดีโอ, "เพิ่ม X" "อ่าน" บนหน้าโน้ต) เติมคำที่ขาดเป็นประโยคเต็ม **ใช้ก็ต่อเมื่อตัวจับคำสั่งในโค้ด (music/video/notes.py) รับประโยคเต็มนั้น** ไม่งั้นส่งต่อตามที่พูด คำถามจริงจากหน้าเพลงจึงไม่ถูกเปลี่ยน · วิทยุและจับเวลายังไม่มีคำสั่งเสียงของตัวเอง จึงยังไม่มีอะไรถูกเติม · บรรทัด intent ของ broker ลงท้าย `screen=none|screen:<name>:used|unused`
- **0.63.0 (Poom): ปุ่มจาร์วิสของแอปถามแล้วอยู่ในแอปต่อ** (log `staying on the app's screen`) สถานะฟัง/คิด/พูดเห็นที่สัญลักษณ์ของแอป · Hey Jarvis ยังกลับหน้าแรก (ข้อ 11)
- **0.63.0: สัญลักษณ์อยู่ในแถบหัวข้อจริง** แทรกหน้าปุ่ม X ในแถวเดียวกัน (หา X จากตำแหน่งมุมขวาบน) แถวจึงเว้นที่ให้ — เดิมวางลอยทับ ทับ ■□□ ของเครื่องคิดเลข (Poom เจอบนเครื่อง) · พื้นที่กด 48dp วาดปุ่ม 36dp ตรงกลาง · หน้าแรกอยู่ในแถวหัวหน้าต่างอากาศ 21dp · **วิดีโอเต็มจอ: ซ่อนพร้อมปุ่มควบคุม** · แอปที่มีบริบทหน้าจอเพิ่ม: วิทยุ ("ต่อไป" "หยุด" "เปิด X") จับเวลา ปฏิทิน

## 11ก. พยางค์แรกของคำสั่ง: เก็บเสียงช่วงก่อนคำปลุกจบ (0.49.0, Poom อนุมัติ 2026-09-24)

**ปัญหา:**
- การอัดเสียงคำสั่งเริ่มตอนตัวจับคำปลุกทำงาน ซึ่งทำงานหลังคำว่า "Jarvis" จบไปแล้วราว 100–300 ms
- คนที่พูด "Hey Jarvis เปิดไฟหน้าบ้าน" ต่อกันโดยไม่หยุด จะพูด "เปิ…" ไปก่อนที่การอัดจะเริ่ม
- บน production ประโยคนี้ถูกถอดเป็น "ปิดหน้าบ้าน" สองครั้ง แล้วไฟถูกสั่งปิด

**วิธี (`voice/PreRoll.kt`):**
- มือถือเก็บเสียงไมค์ล่าสุด 1 วินาที (16 เฟรม เฟรมละ ~62 ms) ไว้ใน RAM
- เมื่อคำปลุกทำงาน จะเอาเสียง **หลังช่วงเงียบที่ตามคำว่า "Jarvis"** มาต่อหน้าไฟล์เสียงคำสั่ง
  - ช่วงเงียบต้องยาวอย่างน้อย 2 เฟรม (~125 ms) เฟรมเงียบเฟรมเดียวอาจเป็นช่วงหยุดระหว่าง "เปิด" กับ "ไฟ"
  - มองย้อนไม่เกิน 5 เฟรม (~312 ms) ซึ่งสั้นกว่าคำว่า "Jarvis" เอง จึงไม่มีทางไปเจอช่วงเงียบระหว่าง "Hey" กับ "Jarvis"
  - **ไม่เจอช่วงเงียบ = ไม่เก็บอะไรเพิ่ม** (แก้บน A07 วันที่ออก) รุ่นแรกเก็บ 375 ms เมื่อไม่เจอช่วงเงียบ แต่ตัวจับคำปลุกทำงานตรงท้ายคำพอดี (climb_ms 0–1 ทุกครั้งบน A07) เฟรมล่าสุดจึงเป็นคำว่า "Jarvis" เอง คำถามปกติที่เว้นจังหวะถูกถอดเพี้ยนจนไปเปิดแอปกล้อง
  - ❓ **ข้อสังเกตจาก log เดียวกัน:** ถ้าตัวจับคำปลุกทำงานตรงท้ายคำ เสียงที่หายก่อนเริ่มอัดมีน้อยมาก ผู้ต้องสงสัยอีกตัวคือ**เสียงบี๊บที่ดังทับพยางค์แรก** ต้องตรวจจากไฟล์เสียงจริงของ Poom (`analysis on --audio`) ว่า "เปิ" อยู่ในไฟล์ใต้เสียงบี๊บหรือไม่
  - ถ้าส่วนที่ต่อมามีเสียงพูดอยู่แล้ว ตัวควบคุมการอัดจะนับว่าเริ่มพูดแล้ว คำสั่งสั้น ๆ จึงไม่ถูกยกเลิกเป็น "ไม่ได้ยินคำถาม"
- broker ตัดคำปลุกที่ติดมาต้นข้อความทิ้ง ("จาร์วิส", "Hey Jarvis", "วิส ") เฉพาะรอบที่เริ่มจากคำปลุก (`speech_gate.strip_wake`) log บอกแค่ `wake_cut=yes|no` ไม่มีข้อความ
- รอบที่เริ่มจากปุ่มจาร์วิสไม่ใช้วิธีนี้ เพราะกดปุ่มก่อนพูด
- เปิดเป็นค่าเริ่มต้น และปิดเปิดชั่วคราวได้ด้วย `TEST_PREROLL` (debug build) เพื่อวัดก่อนและหลังใน APK เดียวกัน `dumpsys` บอก `pre-roll : on last=… ms`

**ความเป็นส่วนตัว (Poom ให้บันทึก):**
- **ไม่ได้เก็บอะไรเพิ่มจากเดิม** เสียงชุดนี้คือเสียงที่ตัวจับคำปลุกได้ยินอยู่แล้วทุกเฟรม
- อยู่ใน RAM ของมือถือเท่านั้น ถูกเขียนทับทุก ~62 ms และไม่เคยถูกเขียนลงดิสก์
- ออกจากเครื่องได้ทางเดียว คือเป็นส่วนต้นของเสียงคำสั่งที่คำปลุกเริ่มแล้วเท่านั้น
- ถ้าการอัดถูกยกเลิก (ไม่มีคำถาม) ก็ทิ้งไปพร้อมกัน
- ตอนไมค์ "หูหนวก" (จาร์วิสพูดอยู่ ปลุกดัง เล่นสื่อ) ไม่เก็บ และล้างทิ้ง

**คำตอบต้องตรงความจริง: การปลุก (0.49.0):**
- การปลุกอยู่ในมือถือ จึงมีแต่มือถือที่รู้ว่า "เปิดปลุก…" เจอการปลุกหรือไม่
- ตั้งแต่ 0.49.0 มือถือ **ทำก่อนแล้วค่อยพูด** (`DONE_BEFORE_SPEAKING` ใน TurnPipeline) ถ้าทำไม่ได้ คำตอบที่พูดและขึ้นจอคือเหตุผลจริง เช่น "ไม่พบการปลุกนั้นครับ" ไม่ใช่คำว่า "…แล้วครับ" ของ broker
- บน A07 เคยตอบ "เปิดปลุก…แล้วครับ" ทั้งที่ไม่มีการปลุกให้เปิด
- แผนที่และแอปกล้องยังพูดก่อนแล้วค่อยเปิด เพราะเปิดแอปอื่นทับหน้าจอ


**เสียงบี๊บ (0.61.0, Poom หลังผลวัด):** เรียกด้วยคำปลุก **ไม่มีบี๊บ** (บี๊บ 1,200 Hz ดังช่วง 150–210 ms ของเสียงคำถาม ชิดพยางค์แรก "พรุ่งนี้" ผิด 4/4 เมื่อมีบี๊บ ถูก 4/5 เมื่อไม่มี) แถบหัวการ์ดจาร์วิสเปลี่ยนเป็นสีทองตัวดำ "● จาร์วิส · กำลังฟัง พูดได้เลย" ตลอดที่ฟัง (สี + จุด + คำ) กดปุ่มจาร์วิสยังบี๊บ debug `TEST_BEEP --es value on` เปิดบี๊บหลังคำปลุกกลับมาเพื่อวัดเทียบ
## 11ค. ตัวถอดเสียงและคำตอบของจาร์วิส (0.63.0, Poom)

- **Qwen3-ASR-Flash (Singapore) เป็นตัวหลัก** (เทียบบน A07 118 คลิป: ส่วนสำคัญถูก 94% vs Groq+คำใบ้ 33%) · **Groq+คำใบ้เป็นตัวสำรองอัตโนมัติ** เมื่อ Qwen ล้ม หมดเวลา (5 วินาที) หรือโควตาหมด — ไม่สลับเมื่อไม่มีเสียง (ถอดได้ว่าง) และไม่สลับเมื่อขอผู้ให้บริการเจาะจง (การทดสอบเทียบ) · log `stt fallback … reason=` ทุกครั้ง
- **Stop-on-Exhaust (Poom เปิดใน console):** Qwen ตอบ 403 `AllocationQuota.FreeTierOnly` = โควตาหมด → ใช้ Groq ทันที ข้าม Qwen 1 ชั่วโมงแล้วลองใหม่ (เปิดบิลแล้วกลับมาใช้ได้เองภายในชั่วโมง) · log "qwen free quota used up"
- **โควตาฟรี 36,000 วินาที หมดอายุ 23 ธ.ค. 2026** หลังจากนั้นต้องเปิดบิล (Poom ทำเอง) · เตือนใน log วันละครั้งเมื่อเหลือ <20% ตามการนับของ broker (`qwen free quota low`) และ `python -m kiosk_broker usage`
- endpoint: `dashscope-intl.aliyuncs.com` หรือ `{workspace}.ap-southeast-1.maas.aliyuncs.com` — Singapore ทั้งคู่ (ตรวจแล้ว ไม่ต้องแก้)
- **prompt (Poom):** บอกประโยคคำสั่งเสียงที่ใช้ได้และว่าแอปอื่นอยู่ในแผงควบคุม "ทำยังไง" ตอบวิธีจริง ไม่ปฏิเสธ · นิทาน/คำแนะนำทั่วไปตอบได้ในกรอบเดิม · ยังระบุว่าตัวโมเดลทำเองไม่ได้และห้ามบอกว่าทำแล้ว · ราคาทองใส่ใน prompt เฉพาะเมื่อถามเรื่องทอง (ค่าบนจอ) · +179 ตัวอักษร (~$0.27/เดือน ที่ 50 คำถาม/วัน)
- **คำสั่งเสียงวิทยุ (0.63.0):** "เปิดวิทยุ" (สถานีล่าสุด) · "เปิดวิทยุ <ชื่อ>" (ชื่อตรง/บางส่วน/ใกล้เคียง พูดชื่อจริง สองสถานีใกล้กันถามกลับ) · "ปิดวิทยุ" · "สถานีถัดไป/ก่อนหน้า" (broker `radio_cmd.py`, มือถือ `radio/RadioVoice`)

## 12. จาร์วิสพักระหว่างเล่นสื่อ (0.44.0, Poom 2026-09-24)

Poom ตัดสินแทนการทดสอบว่าเพลงกวนคำปลุกแค่ไหน:
- **ระหว่างเล่นเพลงหรือวิดีโอ คำปลุกปิด** (detector ถูกป้อนความเงียบเหมือนตอนจาร์วิสพูด)
- **การ์ดจาร์วิสบอกว่าพักอยู่เพราะอะไร:** แถบหัวข้อ "จาร์วิส · พักระหว่างเล่นเพลง" / "…เล่นวิดีโอ" (บรรทัดเดียว 11sp เหมือนเดิม) และข้อความชวนในการ์ดเปลี่ยนเป็น "ระหว่างเล่นเพลง จาร์วิสไม่ฟังคำปลุก กรุณากดปุ่มจาร์วิสด้านล่างเพื่อถาม" คือบอกทั้งสาเหตุและทางที่ยังใช้ได้
  - ระหว่างถามด้วยปุ่ม แถบหัวข้อบอกสิ่งที่จาร์วิสทำอยู่จริง (กำลังฟัง / กำลังคิด / กำลังพูด) แล้วกลับมาเป็น "พัก…"
- **ปุ่มจาร์วิสใช้ได้เสมอ:** การตัดสินของปุ่ม (`WakeGate.buttonMayStart`) ไม่ดูการพักเลย `WakePauseTest` ตรวจทั้งตัวตัดสินและโค้ดของปุ่มใน VoiceService
- **ถามด้วยปุ่มระหว่างเล่นสื่อ:** สื่อถูกสั่งให้เงียบ (หยุดหรือหรี่) ตั้งแต่เริ่มฟังคำถาม ตลอดการคิดและพูด แล้วเล่นต่อเมื่อจบรอบ ไม่ว่าจบแบบไหน (ตอบ ยกเลิก ผิดพลาด) สื่อที่ถูกหยุดระหว่างนั้นไม่ถูกเปิดต่อ สื่อที่เริ่มกลางคำถามเงียบทันที
- **กลับมาฟังคำปลุกเองเสมอ สามทาง:**
  1. เครื่องเล่นปล่อย (หยุด จบ ปิดหน้าจอ)
  2. **lease 90 วินาที:** เครื่องเล่นต้องต่ออายุทุก 30 วินาทีขณะเล่น (รวมตอนถูกจาร์วิสสั่งเงียบ) ถ้าตายโดยไม่ปล่อย จาร์วิสพักนานสุดราว 90 วินาที
  3. **อยู่ในหน่วยความจำเท่านั้น** VoiceService ล้างทุกครั้งที่เริ่ม แอปรีสตาร์ต process ถูกฆ่า หรือรีบูต เริ่มฟังคำปลุกเสมอ เครื่องเล่นที่เล่นต่อหลังรีสตาร์ตต้องขอพักใหม่เอง
- **โครงกลาง:** `voice/WakePause` (ไม่มี Android ในนั้น) เครื่องเล่นเพลงและวิดีโอรอบถัดไปเรียก `hold(Source.MUSIC, media)` ตอนเล่น, `renew(hold)` ทุก `RENEW_MS`, `release(hold)` ตอนหยุด และให้ `Media.quietForJarvis()/resumeAfterJarvis()` ไว้ให้จาร์วิสเรียก (บน main thread)
- `dumpsys` ของ VoiceService บรรทัด `wake-pause :` บอกว่าพักอยู่ไหม เพราะอะไร และ lease เหลือเท่าไร
- debug build มีสวิตช์ `TEST_MEDIA_HOLD` จำลองเพลงกำลังเล่น (TESTING.md)

**Hey Jarvis กับหน้าเครื่องเล่น (Poom 2026-09-24 ตอบคำถามจากรอบ 0.44.0):**
- **ไม่ยกเว้นหน้าเครื่องเล่น** Hey Jarvis ปิดหน้าจอของเครื่องเล่นได้ตามข้อ 11 เหมือนหน้าอื่น
- **แต่เพลงต้องเล่นต่อในเบื้องหลัง** ไม่หยุดเพราะหน้าจอปิด ดังนั้นตัวเล่นต้องอยู่นอก activity (service ของแอป) และ hold ของ `WakePause` เป็นของตัวเล่น ไม่ใช่ของหน้าจอ
  - **วิดีโอไม่เล่นเบื้องหลังตั้งแต่ 0.58.0 (Poom)** ปิดหน้าเครื่องเล่นแล้ววิดีโอหยุดและเก็บตำแหน่งไว้ (ข้อ 5ซ)
- **มีการ์ดคุมเพลงบนหน้าหลัก** ระหว่างมีเพลงเล่นอยู่หรือหยุดชั่วคราว: ชื่อเพลง และปุ่ม [หยุด/เล่นต่อ] [ข้าม] สูงอย่างน้อย 48dp เป็นคำ ไม่ใช่ไอคอนอย่างเดียว การ์ดหายเมื่อไม่มีเพลงค้างอยู่
  - ปุ่มหยุดคือ release hold ของ `WakePause` ทันที คำปลุกกลับมาเอง ปุ่มเล่นต่อคือ hold ใหม่
