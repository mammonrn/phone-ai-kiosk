# คำปลุก: "Hey Jarvis"

ผู้ช่วยบนมือถือชื่อ **จาร์วิส** ปลุกด้วยคำว่า **"Hey Jarvis"** ใช้โมเดล
pretrained ของ openWakeWord ตรงๆ **ไม่เทรนเอง**

> ผู้ช่วยใน Telegram ยังชื่อ **สายฝน** เหมือนเดิม ไม่ถูกแตะต้องจากงานนี้เลย
> คนละตัว คนละเครื่อง คนละความจำ

## สถานะ

| ขั้น | สถานะ |
|---|---|
| โมเดล 3 ตัวอยู่ใน APK | ✅ `app/src/main/assets/models/` |
| detector ฝั่ง Kotlin | ✅ `voice/WakeWord.kt` |
| parity กับ openWakeWord Python | ✅ ผ่าน ทั้งเฟรม 1280 และ 1000 |
| ต่อเข้า CaptureMachine | ✅ ไม่เปิด AudioRecord ซ้อน |
| กันปลุกตัวเองตอนพูดตอบ | ✅ ปิดหูระหว่างเล่นเสียง + พัก 700 ms |
| วัดผลบน A07 จริง | ⏳ **ยังไม่มีตัวเลข** ดู "เกณฑ์" ท้ายไฟล์ |

## ⚠️ license — อ่านก่อนคิดจะเอาไปขาย

**โค้ด** ของ openWakeWord เป็น Apache-2.0 แต่ **โมเดล pretrained ทุกตัว รวม
`hey_jarvis_v0.1` เป็น CC BY-NC-SA 4.0** — ห้ามใช้เชิงพาณิชย์ และงานต่อยอด
ต้องใช้สัญญาเดียวกัน

Poom ตัดสินใจแล้วว่า **commit โมเดลลง repo และประกาศ NonCommercial** และยืนยันว่า
โปรเจกต์นี้ใช้ในบ้านตัวเองเท่านั้น ไม่มีแผนเชิงพาณิชย์

ทั้ง repo จึงอยู่ใต้ CC BY-NC-SA 4.0 ดู [`LICENSE`](LICENSE) และ [`NOTICE`](NOTICE)

**ถ้าวันหนึ่งจะขาย ลง Play Store แบบมีรายได้ หรือใช้ในธุรกิจ — ต้องเอาไฟล์โมเดล
ออกก่อน** นี่ไม่ใช่การตีความแบบระมัดระวังเกินไป NonCommercial คือเงื่อนไขที่เขาให้มา

## ทำงานยังไง

สามโมเดลต่อกัน ซึ่งเป็นการออกแบบของ openWakeWord ไม่ใช่ของเรา:

```
เสียง 16 kHz mono
  -> melspectrogram.onnx   -> mel 32 ช่อง เฟรมละ 10 ms
  -> embedding_model.onnx  -> เวกเตอร์ 96 ตัว ทุก 80 ms
  -> hey_jarvis_v0.1.onnx  -> คะแนน 1 ตัว จาก embedding 16 อันล่าสุด
```

ตัวกลางคือ `speech_embedding` ของ Google ซึ่งเป็นเหตุผลที่โมเดลคำปลุกเล็กขนาดนี้
แล้วยังทำงานได้ และเป็นตัวที่โมเดลคำปลุกทุกตัวของ openWakeWord ใช้ร่วมกัน

**ทุกอย่างรันบนมือถือ** ไม่มีเสียงออกจากห้องก่อน detector บอกว่าเจอคำปลุก

### ค่าคงที่ที่ต้องตรงเป๊ะ

ทุกตัวอ่านมาจาก `utils.py` ของ openWakeWord v0.6.0 ไม่ได้เลือกเอง:

| ค่า | ค่าที่ใช้ | ทำไม |
|---|---|---|
| ค่าเสียงที่ป้อน | **int16 ดิบ** ไม่หาร 32768 | โมเดล mel ถูก export มาแบบรับค่าดิบ |
| แปลงหลัง mel | `x / 10 + 2` | `melspec_transform` ของต้นฉบับ มีไว้ให้ตรงกับ TF ของ Google |
| overlap ต่อการเรียก mel | **480 samples** (`160*3`) | ทำให้เฟรมแบบ streaming ต่อกันติด |
| ค่าเริ่มของ mel buffer | **1.0** ไม่ใช่ 0.0 | `np.ones((76, 32))` |
| หน้าต่าง embedding | 76 เฟรม ก้าว 8 | |
| อินพุตตัวจำแนก | `(1, 16, 96)` | |
| เฟรมอุ่นเครื่อง | 5 เฟรมแรกคืน 0 | ต้นฉบับทำแบบนี้ |

**จุดที่ตั้งใจทำต่างจากต้นฉบับหนึ่งข้อ:** openWakeWord เริ่ม feature buffer ด้วย
เสียงสุ่ม 4 วินาที เราเริ่มด้วยศูนย์ — kiosk ที่ให้คะแนนเสียงสุ่มของตัวเองคือ
kiosk ที่ปลุกตัวเองได้ก่อนมีใครพูด

## parity test — ทำไมถึงเชื่อได้

`WakeWordParityTest` ไม่ได้ทดสอบว่าโค้ดเราตรงกับตัวเอง แต่เทียบกับ **ตัวเลขที่
openWakeWord ตัวจริงผลิตออกมา**

`wakeword/make_parity_fixture.py` รัน openWakeWord Python บนเสียงคงที่ แล้วจด
คะแนนทุกก้อนลง `app/src/test/resources/parity/expected.json` เทสต์ฝั่ง Kotlin
ป้อนเสียงชุดเดียวกันผ่านไฟล์ ONNX ชุดเดียวกัน แล้วต้องได้ตัวเลขเดิม

ใช้ onnxruntime **เวอร์ชันเดียวกัน** ทั้งฝั่ง JVM (เทสต์) และ Android (มือถือ)
เพื่อให้เทสต์รัน kernel ตัวเดียวกับที่มือถือรัน ไม่ใช่ของใกล้เคียง

**ทำไมไม่เทียบก้อนแรกๆ:** openWakeWord เริ่ม feature buffer ด้วยเสียงสุ่มจริง
(`np.random.randint`) ซึ่งไม่มีใครทำซ้ำได้ ต้องรอ 16 ก้อนให้เสียงจริงดันออกไปหมด
เทสต์จึงเทียบตั้งแต่ก้อนที่ 20

สร้าง fixture ใหม่ (ต้องมี openWakeWord ติดตั้ง):

```bash
python wakeword/make_parity_fixture.py          # เขียนทับ
python wakeword/make_parity_fixture.py --check  # เทียบว่ายังตรง
```

**ถ้าเทสต์นี้แดง อย่าปรับ tolerance ให้ผ่าน** มันแปลว่ามือถือได้ยินไม่เหมือนที่
openWakeWord ได้ยิน ซึ่งทำให้ threshold, อัตราปลุกผิด และความแม่นทั้งหมด
เป็นตัวเลขของคนอื่นที่ใช้กับเราไม่ได้แล้ว

## ขนาด APK — วัดจริงหลัง clean build

| | ขนาด |
|---|---|
| ก่อนหน้า (versionCode 6) | 2.5 MB |
| ตอนนี้ (versionCode 7) | **22.8 MB** |
| **เพิ่มขึ้น** | **+20.3 MB** |

แยกเป็น onnxruntime arm64 **17.5 MB** + โมเดลสามตัว **3.7 MB**

Poom อนุมัติไว้ที่ ~+17.7 MB ซึ่งเป็นตัวเลขของ **onnxruntime อย่างเดียว** —
ส่วนที่เกินคือไฟล์โมเดลเอง ซึ่งเลี่ยงไม่ได้

🔴 **onnxruntime ถูกตรึงไว้ที่ 1.22.0 เพราะขนาด** วัดเฉพาะ .so ของ arm64:

| เวอร์ชัน | ขนาด | | เวอร์ชัน | ขนาด |
|---|---|---|---|---|
| 1.17.3 | 16.0 MB | | 1.26.0 | 26.2 MB |
| 1.20.0 | 17.6 MB | | 1.30.0 | **31.6 MB** |
| **1.22.0** | **17.5 MB** | | | |

ถ้าอัปเป็น 1.30.0 APK จะกลายเป็น ~37 MB — เกือบสองเท่าของที่ตกลงกันไว้ เพื่อ
operator และ execution provider ที่โมเดลเล็กสามตัวนี้ไม่ได้แตะเลย
มีด่านใน CI กันไว้ที่ 28 MB

## เกณฑ์ — ยังไม่ผ่าน และจะไม่อ้างว่าผ่าน

เกณฑ์ที่ Poom ตั้ง: **แม่น ≥90% ที่ 3 เมตร** และ **ปลุกผิด ≤1 ครั้งต่อ 8 ชั่วโมง**

❓ **ยังไม่มีตัวเลขจาก A07 เลย** parity test พิสูจน์ว่าเลขที่มือถือคำนวณตรงกับ
openWakeWord เท่านั้น ไม่ได้พิสูจน์ว่าโมเดลนี้ได้ยินเสียงคุณในห้องคุณดีแค่ไหน
ขั้นตอนวัดอยู่ใน [TESTING.md](TESTING.md)

`hey_jarvis` เป็นคำปลุกภาษาอังกฤษที่เทรนด้วยเสียงสังเคราะห์ภาษาอังกฤษ
🔶 คนไทยออกเสียง "เฮ้ จาร์วิส" อาจได้คะแนนต่ำกว่าเจ้าของภาษา — ต้องวัด ไม่ใช่เดา
ถ้าแม่นไม่พอ ทางแก้แรกคือลด threshold แล้ววัดอัตราปลุกผิดใหม่

## งานเทรนคำปลุก "สายฝน" — ยกเลิกแล้ว

ย้ายไป [`archive/wakeword-saifon/`](archive/wakeword-saifon/) ทั้งหมด:
Colab notebook, pipeline, CI ที่รัน notebook, และคำสั่ง `wake-samples` ของ broker

เก็บไว้เป็นบันทึก ไม่ได้ลบ — แต่ไม่มีอะไรเรียกใช้แล้ว และ CI ไม่รันมันอีก
ค่าใช้จ่ายที่เคยลงบัญชีไว้ยังอ่านได้ด้วย `training-usage`

ตัวเลือกนี้ถูกยกเลิกเพราะมีโมเดลสำเร็จรูปที่ใช้ได้เลย ไม่ใช่เพราะทำไม่ได้
